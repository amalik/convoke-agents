---
topic: Consolidate the three overlapping checkers of _bmad/_config/skill-manifest.csv, close the test-fixture-isolation violation as a class, and give the portability classification vocabulary a coherent home
updated: 2026-09-09T20:13
---

- (event) Spec created from deferred residue of the 2026-09-05 skill-manifest lint promotion (deferred-work.md, 5 entries).
- (note) Verified: tests/lib/portability-schema.test.js:51,:105 call findProjectRoot() and assert against the live _bmad/_config/skill-manifest.csv. Same test-fixture-isolation violation the sibling promotion fixed; this file was left behind.
- (note) Verified: the vocabulary standalone/light-deps/pipeline is declared in FOUR places - classify-skills.js:35, validate-classification.js:44, portability-schema.test.js:18, skill-manifest-integrity.js:64.
- (note) Verified: scripts/audit/name-registry-integrity.js:54 declares a DIFFERENT VALID_TIERS (bmad-upstream/convoke/practice/client/unassigned). Identifier collision inside scripts/audit/, not part of the portability vocabulary.
- (note) REFRAMING FOUND DURING SPEC INTAKE: scripts/portability/validate-classification.js is a THIRD checker of the same manifest. NOT wired into CI or any npm script, and it FAILS today - exit 1, 4 BROKEN-DEP - per a committed report dated 2026-08-10. It is also the only checker validating dependency integrity.
- (note) Verified: the three checkers overlap on row-level vocabulary checks. validate-classification.js MISSING/INVALID duplicate skill-manifest-integrity.js row/unclassified and row/invalid-tier and row/invalid-intent.
- (decision) The disease is three checkers of one artifact in three different states, not four copies of a constant. The vocabulary duplication is a symptom; scope the spec to the checker topology.
- (constraint) Round 2 of the prior review REJECTED importing the audit vocabulary from classify-skills.js: a checker must not take its vocabulary from the writer it polices. Proven - widening the writer made the audit accept a bogus tier with zero test failures. Any single-shared-constants-module-for-all-four reintroduces that defect.
- (constraint) Precedent to follow: scripts/audit/skill-manifest-integrity.js plus tests/audit/skill-manifest-integrity.test.js - live read in the audit script, fixture-bound tests driving pure functions.
- (constraint) project-context.md rules in force: test-fixture-isolation, derive-counts-from-source, shared-test-constants, no-process-cwd-in-libs, verification-must-be-falsifiable.
- (decision) Namespace decision: every touched path is Convoke-owned (scripts/, tests/). No _bmad/bme/ skill, workflow or agent is authored, so the Operator Covenant compliance checklist does not apply. Stated explicitly per namespace-decision-for-new-skills.
- (capability) CAP-1 Dependency integrity of skill-manifest.csv is enforced in CI. Today validate-classification.js is the only checker that validates it and nothing runs it.
- (capability) CAP-2 test-fixture-isolation holds as a CLASS across the portability layer: no file under tests/ reads the live skill-manifest.csv.
- (capability) CAP-3 The header-order, row-arity and schema-doc coverage currently unique to portability-schema.test.js survives relocation - notably the row-LONGER-than-header case the audit misses.
- (capability) CAP-4 The portability classification vocabulary has a declared ownership model naming which consumers share a definition and which stay independent, with a test pinning the independent ones equal so drift is visible rather than silent.
- (capability) CAP-5 The VALID_TIERS identifier collision inside scripts/audit/ is resolved.
- (question) OQ-1 Checker topology: wire validate-classification.js as a third CI gate, merge its unique dependency checks into skill-manifest-integrity.js and retire it, or keep both behind a declared boundary?
- (question) OQ-2 Vocabulary ownership: may the writer (classify-skills.js) and its own validator (validate-classification.js) share one definition while the audit stays independent, or must all three stay independent with tests pinning them equal?
- (question) OQ-3 The 4 live BROKEN-DEP: fix, or record an accepted exemption? One is on bmad-check-implementation-readiness, which upstream retired in v6.11/v6.12.
- (assumption) Assumed the 29 warnings from validate-classification.js remain warnings and do not block wiring; only the 4 hard errors gate CI.
- (event) Self-validate Pass 1 (coherence, Spec Law 1-6 and 8): PASS. 5 capabilities each carry intent and success; 7 constraints each rule something out; 7 non-goals; success signal is falsifiable by construction.
- (event) Self-validate Pass 2 (preservation): PASS after correction. Two source claims had been dropped and were added as non-goals - classify-skills.js per-persona intent policy, and the two cosmetic audit gaps from the Round 3 cap. No wrapper-only content discarded.
- (decision) OQ-1 RESOLVED by operator 2026-09-06: option (c), the declared boundary, reached via (a) as the first shippable increment. validate-classification.js owns 'is this CSV internally well-formed and self-consistent'; skill-manifest-integrity.js owns 'does this CSV still match the upstream world and our policy'. Different triggers: the first fires on a bad edit, the second on an upstream release.
- (decision) Option (b) STRUCK as unviable after reading the code. validate-classification.js is the best-engineered of the three checkers: 528 lines cleanly factored (checkRowVocabulary, checkRowDependencies, checkTier3Prereqs, renderReport); validate(projectRoot) takes the root as a parameter with findProjectRoot() confined to main(), so it already satisfies no-process-cwd-in-libs; it carries an isInsideProjectRoot containment guard satisfying path-safety-for-destructive-ops; and it has 21 already-fixture-isolated tests in tests/lib/portability-validation.test.js. Retiring it would trade tested, correctly-factored code for new code. Its ONLY defect is that nothing runs it.
- (decision) OQ-2 RESOLVED 2026-09-06: the row-vocabulary overlap between validate-classification.js and skill-manifest-integrity.js is KEPT and documented as deliberate. Two checkers with INDEPENDENT vocabularies is precisely what the Round 2 constraint asks for - a widened definition in one is caught by the other. Consolidating them re-creates the single point of failure Round 2 proved lets a bogus tier through with zero test failures. A test pins the two sites equal so drift is visible without being automatic.
- (decision) Header order, row arity and portability-schema.md doc conformance move from tests/lib/portability-schema.test.js into validate-classification.js - the same question it already owns - leaving fixture-bound tests behind.
- (note) Supersedes the earlier assumption about the report write dirtying the tree: fs.writeFileSync is at validate-classification.js:491 inside main(), NOT inside validate(). CI can call validate() directly and never write the report. The wiring is cheaper than assumed.
- (decision) Sequencing: (1) wire validate-classification.js into CI, resolving or exempting the 4 BROKEN-DEP first - independently shippable and closes a month-old invisible failure; (2) move header/arity/doc out of portability-schema.test.js; (3) document the boundary and the deliberate overlap.
- (event) SPEC.md re-derived 2026-09-06 with OQ-1 and OQ-2 resolved. Capability IDs CAP-1..CAP-5 preserved. Self-validate Pass 1 (coherence): PASS - 5 capabilities with intent+success, 9 constraints each ruling something out, 9 non-goals, falsifiable success signal. Pass 2 (preservation): PASS - both rulings and their rationale landed; the superseded report-write assumption was replaced by the verified fact rather than dropped silently.
- (note) OQ-3 INVESTIGATED 2026-09-06. All four allegedly-missing templates EXIST, under gitignored .claude/skills/ (tracked: 0 for each). The manifest path column for all three affected skills points at _bmad/ directories that DO NOT EXIST on disk.
- (note) Mechanism confirmed in code, not inferred: resolveRelativeDep (validate-classification.js:137) resolves deps against skillDir derived from the path column. When skillDir does not exist, attempt 1 fails existsSync, attempt 2 walks a nonexistent dir and throws (caught, returns null), attempt 3 falls through to error 'not found'. A MISSING SKILL DIRECTORY IS INDISTINGUISHABLE FROM A BROKEN DEPENDENCY.
- (note) Correlation is perfect and derived from source: exactly 4 manifest rows carry relative-path deps - bmad-create-prd, bmad-check-implementation-readiness, bmad-create-epics-and-stories, wds-4-ux-design. Only bmad-create-prd's path resolves, and it is the only one NOT reporting BROKEN-DEP. 31 of 106 manifest paths resolve; the other 75 do not, BY DESIGN (skill-manifest.csv path column is a CANDIDATE list - refresh-installation.js:529-540).
- (decision) OQ-3 RESOLVED - and the answer is neither of the offered options. The 4 BROKEN-DEP are FALSE POSITIVES: a validator defect, not a manifest defect. Nothing to fix in the manifest and nothing to exempt.
- (decision) CAP-1 as originally written was DANGEROUS. 'A CI step fails when a manifest dependency does not resolve' would wire a gate that fires on the design and stays red forever. The obvious-looking fix a future maintainer would reach for is repointing path at .claude/skills/ - the documented CANDIDATE-LIST trap that has caught four attempts and cost one reverted ADR. Sequencing inverts: fix the false-positive class BEFORE wiring, never after.
- (capability) CAP-6 The validator distinguishes 'this skill is not installed at its candidate path' from 'this dependency is broken', so a non-resolving CANDIDATE path cannot masquerade as a dependency defect.
- (constraint) The path column of skill-manifest.csv is a CANDIDATE list: 75 of 106 entries do not resolve and that is correct. No change may repoint path at gitignored .claude/skills/, and no checker may treat a non-resolving path as a hard error.
- (event) SPEC.md re-derived 2026-09-06 with OQ-3 resolved by investigation. CAP-6 added, CAP-1 success criterion corrected, CANDIDATE-LIST constraint added, delivery sequence inverted to fix-before-wire. Open Questions section removed - none remain. Self-validate Pass 1: PASS (6 capabilities, 10 constraints, 9 non-goals). Pass 2: PASS - the investigation's mechanism, correlation and trap-warning all landed in the contract.
- (event) PREMISE REFUTED 2026-09-09 during story intake. The spec's central claim - that validate-classification.js is wired into nothing and has been failing unwatched since 2026-08-10 - is FALSE.
- (note) Verified: tests/lib/portability-validation.test.js runs under npm test (tests/lib is in the test-runner path) and passes 16/16. Test 1a exercises validate() against a dependency-closed FIXTURE_ROOT; Test 1b validates the SEEDING set against a ratchet baseline at .github/expected-classification-findings.txt.
- (note) Verified: story dist-2-8 (2026-09-07, two days before this spec was written) closed backlog I134 as PREMISE REFUTED / CHECK RESCOPED. The baseline file previously held exactly the 4 BROKEN-DEP rows this spec 'discovered'. They were removed because all three affected skills have non-resolving paths and therefore never seed - verified: bmad-check-implementation-readiness, bmad-create-epics-and-stories and wds-4-ux-design all filtered OUT; bmad-create-prd is the only relative-dep row that seeds, and it is clean.
- (note) ROOT CAUSE OF MY ERROR: I ran validate-classification.js main() against the full 106-row CANDIDATE list and read exit 1 as a broken gate. The validated scope was deliberately narrowed to the 31 rows that actually seed. This is the neighbour of the documented CANDIDATE-LIST trap - not repointing path at .claude/skills/ (which I did avoid and wrote a constraint against) but treating the candidate list as the thing under test.
- (decision) CAP-1 RETIRED as already-satisfied. Dependency integrity of the seeding manifest IS enforced in CI by dist-2-8's Test 1b ratchet. ID retained, never reused (Spec Law 6).
- (decision) CAP-6 RETIRED. dist-2-8 resolved the false-positive class by filtering the input to the seeding set, and named the remedy for non-seeding coverage explicitly: extend tests/fixtures/portability-project/, not teach the validator to distinguish. Teaching the validator would duplicate a solved problem. ID retained, never reused.
- (note) CAP-2 RESHAPED, not retired. tests/lib/portability-fixture.js (backlog I123) already establishes the idiomatic pattern for this directory: FIXTURE_ROOT is the DATA under test and is never findProjectRoot(); REPO_ROOT locates the code under test. portability-schema.test.js:51 and :105 call findProjectRoot() for DATA, which is the violation. The fix is to adopt the existing two-root pattern - NOT to promote it to scripts/audit/ as this spec originally assumed from the skill-manifest-integrity.js precedent.
- (capability) CAP-7 A seeding manifest row whose bare-name dependency points at a row that does NOT seed is reported. dist-2-8 named this gap explicitly and filed it rather than improvising a fix; it needs a NEW finding type because ORPHAN-DEP is a typo check against the manifest vocabulary and BROKEN-DEP checks path-shaped deps. Two live instances verified 2026-09-09: bmad-help -> bmad-quick-dev, bmad-migrate-artifacts -> bmad-create-epics-and-stories.
- (event) SPEC.md re-derived 2026-09-09 after premise refutation. CAP-1 and CAP-6 retired with IDs retained; CAP-2 reshaped to the FIXTURE_ROOT/REPO_ROOT pattern; CAP-7 added for the seeding-closure gap. Delivery sequence reordered so CAP-7 leads as the only item delivering new coverage. Self-validate Pass 1: PASS (5 live capabilities, 2 retired, 8 constraints, 7 non-goals). Pass 2: PASS - the refutation, its root cause and the dist-2-8 lineage are all carried in the contract rather than dropped.
- (note) CAP-7 DRIVERS REFUTED 2026-09-09. The two 'live instances' are artifacts of THIS repo's tree, not defects in the shipped manifest. Verified: bmad-quick-dev (path _bmad/bmm/4-implementation/...) and bmad-create-epics-and-stories (path _bmad/bmm/3-solutioning/...) are both upstream bmm content deleted here by a16fa340 (1227 files, -193694 lines). In an operator project with BMAD installed both paths resolve and both seed.
- (note) validate-classification.js:449-460 already records this in a comment written during dist-2-8: the two targets 'ARE real manifest rows that simply do not resolve in THIS repo... In an operator's project with BMAD installed they resolve and seed normally.' The gap dist-2-8 filed is real; the two instances are not its drivers.
- (decision) CAP-7 RESCOPED: a seeding-closure check must run against an OPERATOR-SHAPED tree (FIXTURE_ROOT), never against REPO_ROOT. Computing it here measures our own deletion, which is the CANDIDATE-LIST trap in a third variant - measuring an operator-facing property against a developer tree. CAP-7 therefore SEQUENCES AFTER the fixture-extension work dist-2-8 filed, and needs fixture rows built to exhibit the condition rather than two instances that already exist.
- (decision) Delivery sequence reordered again: CAP-2+CAP-3 lead. They are the only items that have survived every check on 2026-09-09 and are unaffected by the dist-2-8 lineage.
- (note) PROCESS OBSERVATION, recorded because it is actionable: three premises were overturned in one day by reading ancestor code - (1) the validator was already scoped by dist-2-8, (2) portability-fixture.js already had a better pattern than promoting to scripts/audit/, (3) CAP-7's drivers are local artifacts. Common cause: specifying against grep output and prior conclusions rather than against the code's own recorded reasoning. This subsystem carries unusually dense reviewed commentary; near-surface observations about it are usually already answered in a comment. Read the comments before specifying.
- (event) SPEC.md re-derived 2026-09-09 (second correction same day). CAP-7 rescoped to fixture-driven and resequenced last; its two false drivers removed from Why, Non-goals and Success signal; third CANDIDATE-LIST variant added as a constraint; CAP-2+CAP-3 promoted to lead. Self-validate Pass 1: PASS. Pass 2: PASS - the refutation and its evidence are carried in the contract.
- (decision) CAP-2's success criterion AMENDED 2026-09-09 during sp-7-1 Round 1. The spec prescribed adopting the FIXTURE_ROOT/REPO_ROOT two-root pattern; the implementation folded the file into scripts/audit/ and deleted it. Reading the file is what changed the fix: it contained NO behavioural tests - five assertions over two tracked artifacts, no code under test - so pointing it at FIXTURE_ROOT would have asserted that our own fixture is well-formed, testing nothing. A lint belongs in an audit script.
- (note) PROCESS: this is the FOURTH premise overturned by reading code rather than specifying against greps, and the first one I shipped before catching. The spec was committed still prescribing the abandoned approach; Round 1's Blind Hunter caught it. The earlier PROCESS OBSERVATION in this log said to read the comments before specifying - it did not say to re-derive the spec after the implementation diverges from it. Both are now required.
- (event) sp-7-1 Round 1 review: 3 layers at full mode, all 9 ACs SATISFIED, 14 patch findings applied, 1 deferred, 1 dismissed. Three HIGH: this spec contradiction; schema-doc/section unfalsifiable via substring shadowing (### Tier classification rules contains ## Tier); and a repo-side-only scoping question whose regression premise was falsified against HEAD.
- (note) CORRECTION 2026-09-09: the Round 1 entry claiming sp-7-1's repo-side-only scoping had its 'regression premise falsified against HEAD' was itself FALSE. It was checked against tests/fixtures/portability-project (fails at HEAD for an unrelated testarch reason), not against a packed tree. Round 2 built one from npm pack: HEAD passes exit 0, the change failed exit 2 - a real operator-facing regression. Resolved by shipping _bmad/_config/portability-schema.md in files[]. This is a verification-basis failure: verified against a basis the claim was not about.
- (decision) AC4 (schema-doc conformance) STRUCK 2026-09-09 after Round 3. Three rounds, three HIGHs, all in that one assertion, each introduced by the previous round's fix: substring shadowing, a TypeError crash, then a fence parser broken both ways (verified on the live doc - nested fence = false pass, unterminated fence = four false accusations from a CI gate). Deleted per code-review-convergence's prefer-deletion clause rather than patched a fourth time. CAP-2 and CAP-3 remain satisfied; doc conformance is refiled as needing its own spec.
- (note) NEW FINDING worth more than the check: nothing seeds _bmad/_config/portability-schema.md into an operator's project. files[] places it in node_modules only. So every operator holds a classified skill-manifest.csv whose tier/intent vocabulary has no definition in their tree. Proven by two independent agents running a real tarball install. Filed in deferred-work.md.
