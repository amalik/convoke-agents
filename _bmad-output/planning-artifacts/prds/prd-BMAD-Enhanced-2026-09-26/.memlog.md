---
topic: Channel Integrity: how Convoke arrives
updated: 2026-09-26T18:39
---

- (decision) Route chosen by Amalik 2026-09-26: full PRD to architecture to epics, NOT the incident-driven mini-epic precedent (fic / tfr-epic-1 / lint-epic-1). Consequence he accepted: the doctrine comes IN as requirements - the unit decision, S2-prime (promoting 30 workflows + 14 contracts to personaless skills), T87 agent naming, the ratified tiny name-registry baseline, and an unlisted skills.sh pack as a measured test. None of them are deferrals any more.
- (event) bmad-create-epics-and-stories was invoked first and HALTED at step 1: its prerequisites are a PRD and an architecture document and neither exists. Nothing was written by that run. This PRD is the upstream artifact it was missing.
- (assumption) OQ-1 scoping assumption: the PRD states what must be TRUE of the distribution unit (segments served, flow addressable, failure legible); the unit DECISION itself is a mechanism and belongs to the architecture document and addendum.md per this skill's capabilities-not-implementation rule. If Amalik wants the decision ruled inside the PRD, this assumption is the thing to overturn.
- (event) Two config/convention divergences noted at bind time, neither blocking: _bmad/bmm/config.yaml still carries project_name BMAD-Enhanced (product renamed Convoke), which is what named this run folder prd-BMAD-Enhanced-2026-09-26; and this skill writes prds/<run>/prd.md while every existing PRD in planning-artifacts is a flat convoke-prd-<qualifier>.md. Both want an operator ruling before finalize.
- (override) CORRECTION 1, overrides what this session asserted twice: an exclusion mechanism DOES exist and is shipped. vercel-labs/skills README lines 399-408: 'metadata.internal: Set to true to hide the skill from normal discovery. Internal skills are only visible and installable when INSTALL_INTERNAL_SKILLS=1 is set.' Documented env var at line 539. Issue #572 is open because it asks for a REPO-LEVEL mechanism; the per-skill flag already landed. The earlier claim 'no exclusion mechanism exists' was read off the open issue instead of the README.
- (override) CORRECTION 2, overrides the item-0 severity claim: a default repo-level 'npx skills add' does NOT sweep the 38 test fixtures. README Skill Discovery: the CLI walks a FIXED LIST of container dirs (root, skills/, .claude/skills/, .agents/skills/, ~20 more) 'up to three levels deep', and 'Use --full-depth to also discover SKILL.md files outside these container directories (e.g. under examples/ or tests/).' tests/fixtures/ is not a container dir, so the fixtures need an explicit --full-depth. Caveat that keeps the row alive: the same passage ends 'If no skills are found in standard locations, a recursive search is performed' - Convoke DOES have skills in standard locations (2 tracked SKILL.md under .claude/skills/, whitelisted by .gitignore lines 66-71), so the fallback does not fire today, but it is one gitignore change from firing.
- (decision) CORRECTION 3 and the real mechanism: Wade was discoverable because CONVOKE'S OWN MANIFEST declares him. README: 'Skill paths declared in a manifest are searched at their declared depth and are not subject to the bounded depth-3 catalog walk.' .claude-plugin/marketplace.json declares ./_bmad/bme/_vortex/agents/lean-experiments-specialist, five levels deep, outside every container dir. This also explains why only Vortex could ever be found and Gyre never could - the manifest IS the discovery surface, and it lists 7 of 11 agents. So the public discovery surface of the repo is 9 skills: 2 container-dir + 7 manifest-declared. Not 75, not 115.
- (event) UNVERIFIED BY THIS SESSION, must be checked before item 0 is designed: research reports that metadata.internal:true suppresses the CLI but NOT the skills.sh web listing (issue #1578, a skill named lavish-design listed publicly despite the flag). If true, the flag protects installs and not the storefront, and there is no self-serve retraction - Vercel staff delist by hand. The 'listing never forgets' point stands either way.
- (change) addendum.md written: verified channel mechanics for both channels, 8 comparable suites and how each solves coherence, published failure modes with figures, and four options for the unit (S4 router-stub is NEW from research - one installer plugin on the marketplace, full catalog on skills.sh, the only pattern HF and NVIDIA actually run).
- (change) prd.md trigger rewritten on the corrected mechanism: the listing was not a crawl, Convoke's own marketplace.json declared Wade, and manifest-declared paths bypass the depth-3 container walk. Reframing that follows: the listing did not CREATE the exposure, it RENDERED it - Convoke already publishes deliberately to a public channel, 7 of 11 agents, 0 workflows, 0 contracts. Defect table now carries the corrected severity for item 0, and OQ-2 (does metadata.internal suppress the web listing) and OQ-3 (which channel is primary for the 60% addon segment, given the asymmetry) are logged as open.
- (decision) Amalik said 'ok' without answering brain dump / stakes / working mode, so Fast path was taken with three tagged assumptions rather than re-asking: stakes internal-to-launch, entry point Vision+Features, unit decision stays in architecture. Draft now carries 4 goals, 3 non-goals, 6 success metrics with 4 counter-metrics, 24 FRs in 6 feature groups, and 6 NFRs. Deliberately NOT authored: user journeys - the skill captures those from the user's narration and every one of the six defects lives inside the find-install-first-run journey, so FR18-FR21 rest on inference until Amalik narrates two protagonists (one who already runs BMAD, one who does not). SM3 and SM4 have no target because setting them IS OQ-1.
- (decision) Amalik reviewed the draft and accepted it 2026-09-26 without narrating the two protagonists, so FR18-FR21 STAND ON INFERENCE by operator acceptance rather than by capture. The journeys gap stays recorded in 'Gaps this draft does not fill' so a later reader knows those four requirements were never validated against a narrated session.
- (event) Reviewer round complete: 5 independent read-only agents (rubric, falsifiability, mechanics, reconcile-ledger, reconcile-rules), ~90 findings, five review files in this folder. Three BLOCKERS agreed across reviewers: (a) 'declared' carries two opposite meanings - Trigger says the manifest DECLARED Wade while SM1's today column says '0 declared', and the two readings imply two different architectures; (b) FR4 would ship a gate that cannot fail, and the repo already ruled on exactly this at ci.yml:228 - 'a check over an ignored path passes vacuously in CI' - while name-registry-integrity.js says 'an empty enumeration is a FAILURE TO RUN, never a clean bill of health'; (c) OQ-1 is NOT open - FR13 forecloses S3, FR14/FR15 and the measured table's column header foreclose S1 and S4, leaving only S2, which the memlog confirms was the intent but the PRD presents as the architecture's choice. FR18 likewise answers OQ-3 while OQ-3 is declared open.
- (override) WORST ERROR IN THE DOCUMENT, found by review and then found to be worse than reported: NFR1's 'Convoke tracks upstream at N-1, four to six weeks behind' is not merely unratified - the project's own architecture already analysed and REJECTED it. convoke-arch-bmad-v6.4-v6.8-absorption.md:230 verbatim: 'the product floor is v6.3 against a v6.11 head, i.e. N-8 - so a literal N-1 cap would be in breach on day one and would soft-warn permanently, which trains operators to ignore it.' NFR1 must be struck, not softened. Also unratified-stated-as-fact: the N-1 framing's only other record is a cadence proposal note.
- (decision) OQ-2 ANSWERED, and it breaks item 0's design rather than enabling it. metadata.internal:true is shipped (src/skills.ts:121, nests under metadata:) and CLI suppression is proven by three independent operators in issue 1578. It does NOT remove an already-indexed skills.sh web listing - 11 named URLs still list internal-marked skills, open since 2026-07-03, no maintainer reply. Two caveats that kill FR3 as written: src/add.ts:1330-1338 sets includeInternal on any explicit --skill <name>, so an internal skill installs BY NAME with no env var; and open #2249 / unmerged PR #2294 show the strict === true check makes the value spec-invalid (Agent Skills spec types metadata as string->string) while the spec-compliant "true" reads as NOT internal. Evidence limit that matters: all three proven cases were indexed FIRST then marked internal - nothing establishes the behaviour for a skill marked internal BEFORE indexing, which is Convoke's actual case.
- (override) Derived corrections to the PRD's own figures, all re-run today: workflows are 29 not 30 (Vortex 22 live + 7 Gyre; ls gives 23 because it counts _deprecated/, the very directory holding the ledger's 13 dangling _designos paths). Non-product SKILL.md is 38 but carries THREE classes with three remedies - 32 bmad-named, 6 synthetic skill-with-*, and 1 in _bmad-output/exp3-smoke-test/ which FR3 never reasons about. Of the 38, only 7 sit at depth <=5 and 31 at depth 6-8, and the recursive fallback caps at maxDepth 5, so --full-depth cannot reach 31 of them. 'One .gitignore change from firing' is FALSE and must be struck: the condition is skills.length===0 || fullDepth and manifest paths are walked before that check, so with 7 manifest skills it is never 0. The measured table's 'personaless skills 7 addressable' cell is wrong - they sit at depth 5, in no container dir and in no manifest, so 0; 7+7=14 also contradicted the nine-skill surface two lines below. FR16 is already violated at HEAD: 13 duplicate frontmatter names across 20 extra copies among 75 tracked SKILL.md (3x bmad-help, 3x bmad-shard-doc, 3x bmad-party-mode...), all vendored upstream copies, so read literally FR16 demands renaming upstream's skills.
- (event) Reviewers were wrong on two paths and I corrected them by execution: name-registry.csv is at _bmad/bme/_config/ (not _bmad/_config/) and agent-registry.js at scripts/update/lib/ (not scripts/lib/). Both underlying claims survive. BUDGET: all three meta-model baseline deliverables have SHIPPED - the registry exists, name-registry-integrity.js is wired at ci.yml:231, ADR-001 accepted 2026-09-05 and records 'Three deliverables, no epic, budget held.' So NFR2 asserts an available budget that is SPENT; the honest reading is zero new ADRs, registries or checks. FR13 is already satisfied under the ruled definition - GYRE_AGENTS exists and install-gyre-agents.js:68 generates .claude/skills/bmad-agent-bme-<id>/SKILL.md for each - so FR13's real gap is channel-addressability, not existence, and its 'exist nowhere in the tree' parenthetical is false. Rule preflight-soft-warn EXISTS at project-context.md:419 and my rule inventory missed it because its heading is not prefixed 'Rule:'; it names this PRD's case verbatim - false-positive hard-blocks 'would trap operators with legitimate non-standard installs (git-clone, monorepo, alternative distribution channels)'.
- (event) PRD SUPERSEDED the day it was drafted, on the parent session's recommendation and Amalik's go-ahead. Restructure shipped instead: (1) convoke-note-channel-integrity-findings-2026-09-26.md - the verified diagnosis, every figure carrying its derivation command, 14 corrected figures, OQ-2 answered, the three fixture classes separated, and the ledger's six false statements tabulated; (2) adr/channel-integrity/adr-001-the-distribution-unit.md - the unit question with all four options costed against seven VERIFIED constraints and the ruling left OPEN for Amalik; (3) nine surgical in-place corrections to the client-facing maturity ledger, following its own 'Reclassified 2026-09-17' convention so the original wording stays visible. This file is retained, not deleted - the five reviews cite it.
