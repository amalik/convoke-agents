---
baseline_commit: 90ce726eb0072489c68cfddb13464ea27ae01ec4
---

# Story 2.13: Make a generated team correct on arrival

Status: done

**Epic:** tf-epic-2 — Team Factory Guided Workflow (in-progress) · **Origin:** `T131` (6.0) + `T133` (3.0), both filed 2026-09-10 from the first end-to-end factory run (`tf-2-11`).

**Epic placement note.** `tf-epic-2-retrospective` already reads `done`, so this is the second story added to a retrospected epic (after `tf-2-12`). Recorded deliberately rather than left to look like drift, and the alternative was considered: an incident-driven mini-epic, per the `lint-epic-1` / `cov-epic-1` / `i97-bug-epic-1` / `ci-hygiene-epic-1` precedent. Epic 2's scope — *Guided Workflow: Factory Discovery & Generation* — genuinely covers generation correctness, so a third epic was judged worse than a recorded oddity. If a fourth story lands here, revisit that.

**Scope boundary.** `tf-2-12` made the flow **executable**. This story makes its **output correct**. It does NOT touch: `T136` (the `run:` blocks still cannot be pasted verbatim — shell transport, framework-wide), `T137`/`T138` (activation-validator hardening), `T128` (the terminal gate), `T127` (ruled 2026-09-11: accept v5 output), `T139` (`tf-epic-3`'s false completion).

## Story

As a framework contributor,
I want a team generated by the factory to arrive with a correct config path, unique command codes, a truthful file manifest, a real output directory and a populated agent persona,
so that a new team needs no hand-correction after generation, and the registry entry for a factory-built agent is not a hollow shell beside the hand-written ones.

## Context

`tf-2-11` drove the factory end to end for the first time and found the generated team wrong in six ways that only show up *after* generation succeeds. Every one was re-verified live on 2026-09-13 as a staleness pre-flight before this story was authored; all six are still present.

The persona defect (`T131`) is the one that decides whether the factory is worth using. **It was filed on a wrong premise and re-ruled on 2026-09-11:** it read as "the factory never ASKS for persona", which framed it as a collection problem blocked on NFR2, since `step-01-scope.md` sits at `Concept count: 3/3`. But `step-04-generate.md` §3a **already instructs BMB** to author a *"Persona section (role, identity, communication_style, principles)"* into every agent file. The factory commissions the persona and then reads an empty spec field instead. **It is a wiring defect, not a scope gap** — which is why this story adds **zero** operator questions and NFR2 never binds.

## Acceptance Criteria

1. **Given** a generated team's `config.yaml`
   **When** it is written
   **Then** `output_folder` carries the `{project-root}/` prefix, matching `step-02-connect.md:71`'s own documented template and every shipped module (`_gyre`, `_vortex`, `_team-factory`).

2. **Given** a team whose workflow names share their first two words
   **When** `module-help.csv` is written
   **Then** every `code` in the file is unique.

3. **Given** an agent spec carrying both `role` and `title`
   **When** its registry entry is built
   **Then** the entry's `title` holds the spec's `title`, not its `role`.

4. **Given** a completed generation
   **When** the file manifest is produced
   **Then** it lists every file that was created and no file that was not — specifically including `README.md`, the user guides and the workflow step files, and excluding any `SKILL.md` the generator did not write.

5. **Given** a team whose `integration.output_directory` is set
   **When** generation completes
   **Then** that directory exists on disk.

6. **Given** an agent file BMB has just authored
   **When** its registry entry is built
   **Then** `persona.identity`, `persona.communication_style` and `persona.expertise` are populated from that file, and the extractor reads **both** v5 `<persona>` XML and v6.3 markdown sections.

7. **Given** the repair
   **When** `npm test` runs
   **Then** 0 failures, the pre-existing skip count is unchanged, and each of AC#1-#6 has a test that has been demonstrated to fail before its fix.

## Tasks / Subtasks

- [x] **Task 1: `config.yaml` output path** (AC: #1)
  - [x] `config-creator.js:82` writes `output_folder: specData.integration.output_directory` — bare. Prefix it, matching `step-02-connect.md:71` (`'{project-root}/{output_directory}'`) and the three shipped modules
  - [x] Decide and record whether the prefix belongs in `buildConfigData` or is expected pre-applied in the spec. **Nothing in code constrains this** — verified 2026-09-13: `spec-parser.js:171-172` checks only that `output_directory` is *present*, and there is no `startsWith('_bmad-output/')` anywhere in `lib/`. The `_bmad-output/` rule exists solely as prose in `step-02-connect.md`'s §3. So the choice is genuinely open — **but whichever you pick, add the missing enforcement in code**, or the next generator drifts the same way this one did. State the choice and the enforcement in the Dev Agent Record

- [x] **Task 2: unique command codes** (AC: #2)
  - [x] `csv-creator.js:142` `deriveCode` returns the first letters of the first two words, so `run-check-a` and `run-check-b` both yield `RC`. Reproduced live 2026-09-13
  - [x] Make codes unique *within a module*, deterministically — the same spec must always produce the same codes (NFR4 idempotency is scoped within-version; do not introduce a counter that depends on iteration order)
  - [x] Check the collision against codes already present in the CSV, not only within the batch being written

- [x] **Task 3: `title` stops being overwritten by `role`** (AC: #3)
  - [x] `registry-writer.js:185` — `title: agentSpec.role || agentSpec.title || agentSpec.id`. Role wins; the spec's `title` is discarded
  - [x] **No second site to fix** — verified 2026-09-13: `registry-appender.js:57` calls the same `buildAgentEntry` imported from `registry-writer` (`:7-11`) and only formats the result at `:147`. One helper, one fix, both paths. Confirm this still holds before you edit, rather than assuming it

- [x] **Task 4: a truthful manifest** (AC: #4)
  - [x] `manifest-tracker.js:30-33` asserts a `SKILL.md` per workflow directory. `grep -c "SKILL.md" step-04-generate.md` is **0** — the generator writes `workflow.md` + `steps/`. The tracker was written to a **v6.3** expectation while the generator emits **v5**; per `T127`'s ruling the generator stays v5, so the tracker moves
  - [x] No branch exists for `README.md` or `guides/` (`grep -ci readme` and `grep -ci guide` both return 0) though §1 and §7 list both as generated
  - [x] The `agent-registry.js` "modified" entry is pushed **unconditionally**, whether or not the registry was written — make it conditional on the wiring result
  - [x] **The manifest is the abort path's removal instructions** (`step-05-validate.md` §8). A manifest that omits a created file leaves it behind on abort; one that names an uncreated file sends the operator after nothing

- [x] **Task 5: create the output directory** (AC: #5)
  - [x] Nothing creates `integration.output_directory`. The three `ensureDir` calls (`spec-writer.js:43`, `config-creator.js:41`, `csv-creator.js:37`) each create the parent of the file being written; `output_directory` is only ever read — validated, written into config, displayed
  - [x] `tf-2-11` Risk #3 predicted exactly this. Create it during §5 integration wiring, and add it to the manifest (Task 4) so cleanup removes it

- [x] **Task 6: populate the persona from the generated agent file** (AC: #6)
  - [x] **Design ruled 2026-09-11 by Amalik — do not re-open.** Extract at registry-write time from the file BMB just produced. Zero new operator questions; NFR2 never binds; one source of truth
  - [x] Map `<role>`→`persona.role`, `<identity>`→`persona.identity`, `<communication_style>`→`persona.communication_style`, `<principles>`→`persona.expertise`
  - [x] **Handle both shapes.** v5 agents carry XML tags; the 3 converted v6.3 agents carry `## Identity` / `## Communication Style` / `## Principles` and **zero** XML tags. The factory emits v5 today (`T127`) but the extractor must not hardcode one format
  - [x] `registry-writer.js:188-191` currently reads `agentSpec.persona?.*` and falls back to `''`. Keep a spec-supplied persona winning over an extracted one if present — extraction is the fallback that makes the empty case correct, not a replacement for an explicit value

- [x] **Task 7: tests, each proven able to fail** (AC: #7)
  - [x] One test per AC#1-#6 in the existing suites: `config-creator.test.js`, `csv-creator.test.js`, `registry-writer.test.js`, `manifest-tracker.test.js`
  - [x] **For each, demonstrate red before green** — break the fix, confirm the test fails, restore. Per `project-context.md` rule `verification-must-be-falsifiable`. `tf-2-12` R2 found two defects that a 2,256-test suite could not see; assertions that cannot fail are how that happens
  - [x] `npm test` — 0 failures, skip count unchanged

- [x] **Task 8: end-to-end confirmation** (AC: #1-#6)
  - [x] Generate a throwaway team and assert all six properties on the real output, not on unit fixtures
  - [x] Delete it per `tf-2-11` AC8 — module dir, registry block, spec file, **and the output directory Task 5 now creates** — and verify `agent-registry.js` returns to a **zero git diff**. `git checkout` would test git, not removability
  - [x] **Do not grep the framework while the throwaway exists.** `tf-2-11` published a wrong census that way, and its R1 correction was wrong too

## Dev Notes

### Do not re-open the two settled decisions

- **T131's design is ruled** (2026-09-11): derive from the agent file. A step-01a/01b split on the `oc-2-1` paced-rounds precedent was rejected — it asks the contributor for prose BMB is about to write anyway, creating two sources nothing reconciles. Using step-02/step-03's spare concept budget was rejected — Connect is integration wiring, Review is a summary gate.
- **T127 is ruled** (2026-09-11): the factory keeps emitting v5, because it delegates to BMB and BMB has no v6.3 path. So Task 4 moves the tracker to match the generator, **not** the generator to match the tracker.

### Staleness pre-flight — run 2026-09-13, all six confirmed live

`config-creator.js:82` bare `output_directory` · `csv-creator.js:142` `deriveCode` first-two-initials · `registry-writer.js:185` role-over-title · `manifest-tracker.js` SKILL.md asserted 4×, README 0, guides 0 · no `ensureDir` of `output_directory` · `registry-writer.js:188-191` reads `agentSpec.persona?.*`.

### Traps this module has already sprung

- **`buildAgentEntry` is shared by `registry-writer` and `registry-appender`**, so Tasks 3 and 6 propagate to the add-agent path for free — the opposite of `T139`'s orphaned-appender shape, and worth preserving. Do not fork the helper to fix one caller.
- **Idempotency (NFR4) is scoped within-version.** Task 2's uniqueness must be deterministic for a given spec — a counter driven by iteration order breaks re-runs and express mode.
- **`createConfig` and `createCsv` are additive-only** and refuse to overwrite (`config-creator.js:26`, `csv-creator.js:28`). Task 8's throwaway cannot be regenerated in place; delete first.
- **Measuring the framework while your own artefact is on disk** produced two wrong counts during `tf-2-11`. Task 8's grep prohibition is not pedantry.

### Testing standards

`tests/team-factory/` uses `node:test`. All four target suites already exist. `project-context.md` rule `verification-must-be-falsifiable` makes Task 7's red-then-green demonstration mandatory, not optional.

## Dev Agent Record

### Agent Model Used

Claude Opus 5 (1M context), via `bmad-dev-story`.

### Completion Notes List

**All 8 tasks, AC#1-#7. `npm test` 2409 / 2408 pass / 0 fail / 1 pre-existing skip (up 26). Every AC proven RED before GREEN and killed by mutation afterwards.**

| AC | RED | mutation kills |
|---|---|---|
| 1 prefix | 2 fail | 1 |
| 1 enforcement | 2 fail | 2 |
| 2 codes | 1 fail | 1 |
| 3 title | 1 fail | 1 |
| 4 manifest | 4 fail | 1 |
| 5 output dir | 3 fail | 1 |
| 6 persona | 5 fail | 3 |

**Task 1 — the real finding was the missing enforcement, not the missing prefix.** `config-creator.js` wrote `output_folder` bare. But the `_bmad-output/` rule existed **only as prose** in `step-02-connect.md` §3 — there was no `startsWith('_bmad-output/')` anywhere in `lib/`, which is *why* it drifted. The prefix is now applied at write time (idempotent) **and** enforced in `spec-parser.js`. That module had **zero test coverage** before today despite being the parse-and-validate core; `tests/team-factory/spec-parser.test.js` is new.

**Task 2 — a second caller would have been missed.** `csv-appender.js:75` called `deriveCode` with no awareness of codes already in the file, so fixing only the creator would have left the add-agent path colliding. True RED proved it: `duplicate codes after append: RC, RC`. Disambiguation is derived from the name in a fixed candidate order — never a counter over iteration order, which would break re-runs and express mode (NFR4 is scoped within-version).

**Task 3 — one helper, both paths.** `registry-appender.js:57` calls the same `buildAgentEntry`, so the precedence fix reaches the add-agent path for free. Deliberately not forked.

**Task 4 — two judgement calls, both changing pre-existing tests, both recorded because a reviewer should challenge them.** (i) My first registry condition required `success === true`, which dropped the entry whenever a caller simply omitted the field, breaking three tests. Corrected to **suppress only on explicit failure**: for an abort path over-claiming is the safer error — the operator checks a file needlessly rather than leaving one edited — and absence means the caller did not say, which is not "did not happen". (ii) `golden-manifest.json` carried two `SKILL.md` entries and the count test hardcoded `11` with the comment *"2 workflows * 2 files"*. Both encode a **v6.3 expectation the generator never satisfied**; `T127` ruled the generator stays v5, so the tracker moved. Golden trimmed (still parses), count corrected 11→9 and created 9→7, reason written into the test.

**Task 5 — `ensureOutputDirectory` is new.** Nothing created the team's artifact directory; the three existing `ensureDir` calls each make the parent of the file being written. Takes `projectRoot` explicitly per `no-process-cwd-in-libs`. Wired as §5a-ii, and its path recorded in `{generation_context}.output_directory_path` so the abort manifest removes it.

**Task 6 — implemented exactly as ruled, and verified on real agents of both formats.** `extractPersonaFromAgentFile` reads v5 `<persona>` XML and v6.3 `## Identity` / `## Communication Style` / `## Principles`. Against shipped agents: v5 Scout yields 975/323/415 chars, v6.3 Mila 787/800/919. An explicit spec persona still wins — extraction is the fallback that makes the empty case correct, not a replacement. Wiring was a separate defect: `writeRegistryBlock` knew nothing of the agent files, so the extractor was inert until §5d passed `agentFiles`.

**R2 CORRECTIONS to this record, all three found by the Acceptance Auditor.** (i) *"up 26"* — the suite gained **25** (2384 → 2409); it is 2423 after R2. (ii) *"the changed files carry 7 pre-existing problems"* — they carried **4**; 7 is the count for the whole `_bmad/bme/_team-factory/lib` directory, which includes `registry-appender.js`, a file the original commit did not touch. R2 does touch it, so the changed set now carries 6 including that same pre-existing error. The substantive half — **zero new problems from this story** — is verified true. (iii) **The RED table's first row was not reproducible.** `does not double-prefix a value that already carries it` **passes at baseline**, so AC#1 was 1 RED, not 2 — and under AC#7's own definition that test is not falsifiable by the defect. It is now reframed as what it actually guards: the CLI bypass path.

**Task 8 — all six ACs asserted on real generated output**, not unit fixtures: `output_folder: '{project-root}/_bmad-output/e2e-probe-artifacts'`, codes `RC,RB` unique, title `'E2E Probe Agent'`, persona populated, manifest carrying README/guides/outdir and no `SKILL.md`, output directory present on disk. Throwaway removed; `agent-registry.js` hand-restored to a **zero git diff** with `require()` re-verified.

**Process notes, recorded because they were nearly failures.** Three of my test files first failed on *my* import errors rather than the defect — each time I read the failure instead of treating RED as success. Two export insertions failed **silently** because I used `.replace()` without an `assert` on those two lines only; caught by checking `Object.keys(require(...))` rather than assuming. And one falsifiability mutation did not apply at all — `sed` choked on `|` characters and reported a meaningless pass; redone with an asserted anchor. **A RED you do not read is indistinguishable from a test that cannot run.**

**Lint, stated precisely:** `npm run lint` is clean and says nothing about this work — `_bmad/` is excluded (`I126`). Run with `--no-ignore`, the changed files carry **4 pre-existing problems and zero from this story** (*corrected: this paragraph said 7, which is the whole `lib/` directory — it contradicted the R2 correction three paragraphs above and is fixed here*); one is a genuine eslint **error** at `registry-appender.js:325` (`no-useless-assignment`) in code this story only read. Left alone; it is `I126`'s cost made concrete.

### Round 2 Review — 2026-09-13 (three independent adversarial layers)

Run via `/bmad-code-review` with independent subagents. **26 findings. All 7 ACs verified MET by the auditor's own execution**, both operator rulings followed, scope clean, and the golden-fixture edit independently confirmed legitimate — fixture and fix fail together at baseline rather than the fixture being bent to fit. **5 code fixes and 3 record corrections applied here.** `npm test` 2423 / 2422 / 0 fail.

**I called for a revert before the third layer landed. That was premature and wrong** — recorded because the two hunters' framing (four HIGH defects) and the auditor's (all ACs met, defects are robustness and one inverted justification) are both accurate, and acting on the first without the second would have discarded working code.

**R2-1 — HIGH. My own justification was backwards, and it shipped a destructive instruction.** I relaxed the manifest's registry claim from `success === true` to `success !== false`, arguing *"over-claiming is the safer error — the operator checks a file needlessly."* **The manifest does not ask anyone to check.** `formatAbortInstructions` emits `git checkout -- "scripts/update/lib/agent-registry.js"` for every `modified` entry — a **destructive revert that discards uncommitted work**. And the absent case is not hypothetical: step-04 §5c runs *before* §5d, so on the abort this story itself documents, the key is absent — handing that command to an operator whose registry the factory never touched. `writeRegistryBlock` carries a dirty-tree check *precisely because* that file holds uncommitted edits. **Restored to `success === true`.** The three pre-existing tests that broke the first time were right; my reasoning for overriding them was not. This time the fixture moved: `buildTestContext` now models a completed run, and the absent case has its own two explicit tests — including one asserting no `git checkout` is ever emitted for an untouched file.

**R2-2 — HIGH. Path traversal, against a rule this repo already has.** *(R3 correction: the justification below is FALSE about the CLI — see §Round 3. The CLI calls `createConfig` → `buildConfigData` and never calls `ensureOutputDirectory`, so the hole stayed live until R3 guarded the composing function. The §5a-ii half of the argument was valid.)* `startsWith('_bmad-output/')` is a string test wearing a path test's clothes: `_bmad-output/../../escaped` passed validation, `ensureOutputDirectory` created that directory **outside the project root**, the escaping path was written into the generated `config.yaml`, and the abort manifest recorded it as an `rm` target. `project-context.md` rule `path-safety-for-destructive-ops` requires resolve + normalise + contains-check — I did none of the three. Now enforced in **both** places, deliberately: `spec-parser` via a new `isContainedOutputDirectory`, and `ensureOutputDirectory` itself, because step-04 §5a-ii calls it directly and the shipped CLI bypasses `parseSpec` entirely — an upstream-only guard is no guard for either caller. The bare prefix `_bmad-output/` is also now rejected; it resolved to the shared artifacts root and made the abort manifest claim the whole tree.

**R2-3 — HIGH. My new rule rejected the module's own canonical fixture, and nothing caught it.** `test-team-spec.yaml:59` carried `{project-root}/_bmad-output/test-team-artifacts` — the **config shape in a spec field**. Five suites reference that fixture and every one loads it with `yaml.load`, never `parseSpec`, so the module's example spec was invalid under its own validator, silently. **The rule was right and the fixture was wrong**, decided on evidence rather than convenience: `step-02-connect.md:50` defaults the field to repo-relative, `:52` validates that shape, `:71` adds the `{project-root}/` prefix when composing config, and shipped `_gyre`/`_vortex` carry the prefix only in `output_folder`. Fixture corrected; `golden-help-csv.csv` followed, justified against `_bmad/bme/_team-factory/module-help.csv`, which carries `_bmad-output/planning-artifacts` — repo-relative — rather than against my own output.

**R2-4 — HIGH. Persona keying voided every v6.3 agent — the failure T131 exists to remove.** `path.basename(file, '.md')` is right for v5 (`agents/<id>.md`) and catastrophic for v6.3 (`agents/<id>/SKILL.md`): every agent keyed as `SKILL`, collided, and `buildModuleBlock`'s lookup by `a.id` missed every time. Proven end-to-end against the real Vortex agents: **`success: true`, personas empty, no warning.** My "handles both shapes" claim was half true — the extractor did; the wiring discarded v6.3. Fixed with `agentIdFromPath`, and the regression test runs against the actual shipped `SKILL.md` files rather than a fixture.

**R2-5 — MEDIUM. A comment of mine was false, so I made it true.** `registry-writer.js` claimed the persona wiring reached "both paths" via the shared `buildAgentEntry`. True of the `title` flip; **false of the persona**, because `registry-appender.js:57` passed two arguments — so add-agent produced hollow entries while add-team produced full ones. The appender now extracts when given `agentFilePath`, and omitting it preserves prior behaviour.

**Filed, not fixed (5, all confirmed):** `buildExtensionManifest` still claims a `SKILL.md` per workflow and still pushes the registry unconditionally — the identical false claim 130 lines below the fix, but it is the epic-3 add-agent path this story excludes via `T139`; `deriveCode` exhausts its candidate pool and silently reissues a duplicate (the hunter's own note: it could not be triggered with realistic names, so it is a contract violation with a contrived trigger, and the real defect is that the fallback is silent); the shipped `config-creator` CLI bypasses `parseSpec`, so `{project-root}` alone still double-wraps there; unquoted commas in `workflow_names` shift `csv-appender`'s column reads, which also breaks the **pre-existing** duplicate-agent check; and `registry-appender.js:325` carries a real eslint **error** (`no-useless-assignment`) that `npm run lint` cannot see — `I126`'s cost, in a file R2 now touches but did not introduce it to.

**What the layers could not have found, and I should have:** every one of R2-1 through R2-4 is a case where I reasoned about behaviour instead of executing it — the abort path's actual output, the resolved path, the fixture under the new rule, the v6.3 file layout. That is the same fault as this session's three miscalibrated censuses, at a higher cost.

### Round 3 Review — 2026-09-14 (last allowed round)

Two independent layers. **Auditor verdict: all 7 ACs still MET after R2, no R2 fix misrepresents what the code does, safe to leave `review`** — and it reproduced the persona character counts digit for digit and re-derived all three record corrections independently. **Two defects fixed here on operator instruction; the remainder filed.** `npm test` 2432 / 2431 / 0 fail.

**The fault named precisely.** R2 called this "reasoning about behaviour instead of executing it". R3 shows it is narrower and worse: **I reason about the CONSUMER without reading it.** Twice in one story — R2-1 claimed the abort path "asks the operator to check a file" when it emits `git checkout`; R2-2 claimed the guard belonged in `ensureOutputDirectory` "because the CLI bypasses parseSpec" when **the CLI never calls that function**. Both justifications were confident, written at length, and wrong about who consumes the thing.

**R3-1 — the third wrong predicate on one line, now asking a different question.** `success === true` is still not a confirmed write: `registry-writer.js:42` returns `{success:true, written:[], skipped:['block already exists']}` when the prefix block is present, **and that early return precedes the dirty-tree check at :75** — so R2's stated defence never runs on that path. `derivePrefix` collapses `data-ops` / `data_ops` / `Data-Ops` onto `DATA_OPS`, so a genuinely different team lands on an existing block, leaves the file byte-identical, and is handed a destructive `git checkout`. The progression — unconditional → `!== false` → `=== true` → `written.length > 0` — is three predicates asking about the *call* and one asking about the *file*. Per `code-review-convergence`'s "restructure, do not patch", the fourth is a different question rather than a tweak.

**The fixture was the incomplete thing all along.** The same four tests broke on every tightening, and each time the fix was to complete `buildTestContext`: first it carried no registry result (the §5c-abort shape), then a bare `{success:true}`, and now the five export names `writeRegistryBlock` actually returns. Three fixture corrections, one per predicate — the signal that the *model* was wrong, not the predicates.

**R3-2 — the traversal hole R2's record implied was closed.** Verified by running the CLI, not by reading it: before, `node config-creator.js --spec-file <spec with _bmad-output/../../escaped>` returned `success: true` and wrote `output_folder: '{project-root}/_bmad-output/../../escaped'`. The guard now sits on `buildConfigData`, the function that composes the value and that every path reaches, and throws — `buildConfigData` is synchronous with no error channel, and `createConfig` already converts throws into `{success:false, errors:[…]}`. Re-verified by CLI: traversal now returns `success:false` with a specific error and writes no config; legitimate paths unaffected.

**Filed, not fixed (7, all confirmed by execution):** `rel.startsWith('..')` in `ensureOutputDirectory` false-rejects a legitimate `_bmad-output/..foo` that the parser accepts — the two R2 guards disagree; `agentIdFromPath` is case-insensitive on the basename but case-sensitive on the extension, so `SKILL.MD` defeats it and a v5 `agents/SKILL.md` resolves to `agents`; `writeRegistryBlock` never warns when `agentFiles` is absent or unusable, so the whole T131 fix can no-op silently; **R2-5 shipped with zero test coverage and is currently unreachable** — no workflow sets `agentFilePath` and `grep -rn agentFilePath tests/` is empty, which AC#7 exists to forbid and the R2 record failed to disclose; `step-04` §5d never instructs the executor to record `registry_wiring_result`, on which R3-1's safety now depends (mitigated — `end-to-end-validator.js:239` fails on its absence); `rm "<directory>"` in the abort instructions still cannot execute; and the shipped CLI resolves `bmeRoot` one level too high, creating `_bmad/_X/` instead of `_bmad/bme/_X/` — observed twice during this review as stray directories, both removed.

**Overstatements in the R2 record, corrected:** "five suites reference that fixture" — three load it, two only mention it in comments; the substantive claim is unaffected. And "26 findings / three independent layers" is **unverifiable from the repo** — no review report is persisted under `_bmad-output/`, which the auditor rightly flagged as provenance it cannot check.

### File List

- `_bmad/bme/_team-factory/lib/writers/config-creator.js` — prefix helper, `ensureOutputDirectory`, exports
- `_bmad/bme/_team-factory/lib/writers/csv-creator.js` — `deriveCode` disambiguation, `usedCodes` seed
- `_bmad/bme/_team-factory/lib/writers/csv-appender.js` — seed from codes already in the CSV
- `_bmad/bme/_team-factory/lib/writers/registry-writer.js` — title precedence, `extractPersonaFromAgentFile`, persona wiring, exports
- `_bmad/bme/_team-factory/lib/manifest-tracker.js` — SKILL.md removed, README/guides/steps/outdir added, conditional registry claim
- `_bmad/bme/_team-factory/lib/spec-parser.js` — `_bmad-output/` enforcement; R2: `isContainedOutputDirectory`
- `_bmad/bme/_team-factory/lib/writers/registry-appender.js` — **R2**: persona wiring, making the shared-helper claim true
- `tests/team-factory/fixtures/test-team-spec.yaml` — **R2**: `output_directory` made repo-relative
- `tests/team-factory/golden/golden-help-csv.csv` — **R2**: output-location made repo-relative
- `_bmad/bme/_team-factory/workflows/add-team/step-04-generate.md` — §5a-ii added, §5d passes `agentFiles`, placeholder table extended
- `tests/team-factory/spec-parser.test.js` — **new**, first coverage for that module
- `tests/team-factory/{config-creator,csv-creator,csv-appender,registry-writer,manifest-tracker}.test.js` — tf-2-13 suites
- `tests/team-factory/golden/golden-manifest.json` — two SKILL.md entries removed
- `_bmad-output/implementation-artifacts/sprint-status.yaml` — row → review

## Change Log

| Date | Change |
|------|--------|
| 2026-09-13 | Story authored via `bmad-create-story` from `T131` + `T133`, after a staleness pre-flight confirmed all six defects still live. Both of its design questions were already ruled (T131 2026-09-11, T127 2026-09-11) so it carries no open decisions. Scope excludes T136/T137/T138/T128/T139. Epic placement recorded: second story into a retrospected epic, mini-epic alternative considered and declined. |
| 2026-09-13 | Reviewed before registering: 12/12 citations verified, 7 ACs / 8 tasks with no orphans. One defect found — Task 1 claimed `spec-parser.js` validates the `_bmad-output/` prefix. It does not, and nothing did; the rule was prose-only, which is the real finding. |
| 2026-09-13 | Implemented. All 8 tasks, AC#1-#7. Seven fixes across six source files, each RED-proven and mutation-killed; `spec-parser.js` gained its first tests. Two pre-existing tests and one golden fixture updated because they encoded a v6.3 expectation the generator never satisfied — recorded rather than buried. End-to-end verified on real output; throwaway removed to a zero registry diff. `npm test` 2409/2408/0 fail. |
| 2026-09-13 | **R2 — three independent layers, 26 findings; 5 code fixes and 3 record corrections.** All 7 ACs verified MET by the auditor's own execution and the golden edit confirmed legitimate, so no revert — but four HIGH defects, every one from reasoning about behaviour instead of executing it. The worst was mine twice over: I relaxed the manifest's registry condition on an argument (`the operator checks a file needlessly`) that is **inverted for its actual consumer**, which emits a destructive `git checkout` — restored to `success === true`, and this time the fixture moved rather than the condition. Also: path traversal escaped the repo against a rule this project already has; my new validation rejected the module's own canonical fixture while five suites bypassed the parser; persona keying by basename voided every v6.3 agent; and a comment claiming the persona reached 'both paths' was false until R2 made it true. `npm test` 2423/2422/0 fail. |
| 2026-09-14 | **R3 (last allowed round) — two layers; 2 fixes on operator instruction, 7 filed.** Auditor: all 7 ACs still MET after R2, safe to leave `review`. The fault named precisely: **I reason about the consumer without reading it** — R2-1 claimed the abort path asks the operator to *check* (it emits `git checkout`), R2-2 claimed the guard covered the CLI (the CLI never calls that function, and the traversal hole stayed live until R3). Fixed: the registry claim now requires `written.length > 0` — the fourth predicate and the first to ask about the FILE rather than the CALL; and `buildConfigData` now refuses an escaping `output_directory`, verified by running the CLI. Three fixture corrections on the same four tests across three rounds showed the *model* was incomplete, not the predicates. `npm test` 2432/2431/0 fail. |
| 2026-09-14 | **Closed `review` → `done` on verified deliverables.** Re-executed at close, not inferred from task counts: `output_folder` carries `{project-root}/`, colliding workflow names yield `RC,RB`, `title` resolves to the spec title, the manifest lists README/guides/output-dir and no `SKILL.md`, `ensureOutputDirectory` is present and guarded, and v6.3 persona extraction returns 787/919 chars from the real Mila file. **Both R3 safety fixes re-verified at close:** a no-op registry write no longer claims the file (so no destructive `git checkout`), and `buildConfigData` refuses a traversal path. `npm test` 2432/2431/0 fail. Closes `T131` and `T133`; Round 3 residue already filed as `T163`-`T166`. |
