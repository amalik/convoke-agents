# Convoke Codebase Audit — Conception, Execution & Muda

> **Status:** findings only — no remediation applied. **Reconciled** against in-flight work, **intent-reconstructed** for the stalled cluster, and **confidence-checked** (bugs reproduced · reconciliation sampled · intent verified). 2026-06-27 / 2026-06-28.

| | |
|---|---|
| **Date** | 2026-06-27 (audit) · 2026-06-28 (reconstruction + confidence checks) |
| **Package** | `convoke-agents@4.0.0-rc.1` |
| **Auditor** | Winston (System Architect) + multi-agent review workflow |
| **Method** | 8 parallel reviewers → adversarial verification → reconciliation (branches + backlog + WIP markers) → intent reconstruction (v6.3/v4.1 epic+arch trail) → confidence checks (3 pillars) |
| **Scope** | `scripts/`, `index.js`, `tests/` (excludes generated `exported-skills/` and the `_bmad/` framework) |
| **Muda scan** | `knip` (17 unused files / 71 unused exports, triaged) — config at [knip.json](../knip.json) |
| **Confirmed findings** | 45 (high 2 / medium 19 / low 24) |

## Summary

| Severity | Count | | Type | Count |
|---|---|---|---|---|
| 🔴 High | 2 | | Conception | 11 |
| 🟠 Medium | 19 | | Execution | 19 |
| 🟡 Low | 24 | | Muda (dead code) | 15 |

## Reconciliation + intent reconstruction

Technical accuracy ≠ actionability. Each finding was cross-referenced against the two non-main branches (`deprecation` — 2,627 commits behind, touches only `package.json`/`postinstall.js`; `test/governance-migration-v2` — 1,312 behind, only `_bmad-output/` artifacts — **neither touches any flagged code**), the lifecycle backlog, the v6.3 retros, and in-code WIP markers. For the stalled "muda" cluster, a second pass reconstructed original **intent** from the v6.3 / v4.1 epic + architecture trail — which **overturned the dead-code verdict on the two biggest items**.

| Status | Count | Meaning |
|---|---|---|
| 🔴 Live — untracked | 36 | Real defect / accrued debt, no coverage. Actionable now. |
| 🟢 Reserved — pre-positioned (KEEP) | 1 | `config-loader.js` — read primitives reserved for v4.1 E4 (AD1/AD8). **Not muda.** Keep; annotate. |
| 🟣 Blocked — keep (PF1 gate) | 4 | PF1 v4.0 behavioral-equivalence gate (FR36-40); evidence genuinely unobtained → **finish or formally waive**, don't archive. |
| 🟡 Salvage — promote to CI | 2 | `install-scope-check.js` — independent, cheap scope-leak guard. Promote to CI. |
| 🔵 Known — re-prioritize | 1 | Backlog item U11; the audit escalates priority, not discovers. |
| 🟢 In progress — expected | 1 | `frontmatter.js` consolidation, newest code in repo. Don't ticket. |

**Bottom line:** of 45 confirmed findings, **36 are genuinely live and untracked**. The rest are accounted-for: 1 reserved foundation (v4.1), 4 a blocked release gate (PF1), 2 salvageable to CI, 1 known, 1 mid-rollout. **The "muda" signal was almost entirely mislabelled pre-positioned or blocked initiative work** — the dead-code count was an illusion created by reading plan-locked future investments as orphans. The real gap is *provenance*: the reserved/blocked files carry no in-code pointer to the initiative that owns them, so every audit re-discovers them as orphans.

## Confidence checks (2026-06-28)

The decisions above were stress-tested across three pillars; all held, two were sharpened.

- **Pillar A — bugs are real: ✅ both reproduced.** HIGH-1 by execution (`compareVersions("4.0.0-rc.1","4.0.0") === 1`); HIGH-2 by construction (migration write-set = 19 `SKILL.md` targets; backup set = 4 `_vortex/*` paths; zero overlap).
- **Pillar B — reconciliation is accurate: ✅ sampled clean.** 5 of the 36 "live" findings spot-checked against `deferred-work.md` + backlog + open PRs — 0/5 had hidden tracking. The "36 live" holds.
- **Pillar C — intent reflects live commitments: ✅ holds, 2 refinements.** (1) v4.1 plan staleness = 🟢 GREEN (I113 ready-for-sprint, E4 in ratified MVP, nothing changed since 2026-06-21). (2) **PF1 coverage resolved → FINISH, not archive** (FR36-40 ≠ FR21-23; I97 covers ≤2 of 4 target agents). (3) **config-loader fit corrected** → not a drop-in reader; v4.1 reuses its read internals + modifies the frozen API; surfaced an AD1-vs-AD8 arch contradiction now flagged for sprint-planning.

## Architect's diagnosis

1. **Patch-at-the-call-site, not at-the-primitive.** The two HIGH bugs both stem from a core utility being wrong while callers worked around it locally (`compareVersions` + the `compat-preflight.js` suffix-strip workaround; the same config path hardcoded in 10+ places). *The compareVersions bug was already logged as U11 — a known primitive defect left deferred while call-sites accrete workarounds.*
2. **No consolidated base/kernel layer.** Multiple sources of truth that must agree: agent roster (3 copies), install paths (10+ copies), frontmatter parsing (canonical parser now landing, ~15 files still bypass it). One deliberate consolidation initiative.
3. **"Muda" was a mislabel, not waste.** Intent reconstruction showed the dead-code cluster is *pre-positioned* (config-loader → v4.1 cadence reader) or *blocked* (PF1 → v4.0 behavioral-equivalence gate, mid-resumption) — investments waiting on their owning initiative, not `rm` targets. Fix = annotate the provenance, not delete.

## Recommended sequence

1. **Block `4.0.0` on the two HIGH items (both now reproduced).** HIGH-1 = **escalate existing backlog item U11** into the release gate (live rc→final path), with the semver-aware test. HIGH-2 = **new** bug-fix story — extend the backup write-set to the migration's actual write surface (the 19 inventory targets).
2. **One "base-layer consolidation" initiative** for the four single-source-of-truth conception findings (paths module, finish the in-flight `frontmatter.js` adoption, derive rosters from `agent-registry`, relocate kernel utils). After 4.0 ship.
3. **Annotate, don't delete, the reserved/blocked cluster.** Add an in-code header to `config-loader.js` ("read primitives reserved for v4.1 E4 — cadence-state.js, AD1/AD8") and to the `pf1-*.js` files ("v4.0 behavioral-equivalence gate FR36-40; finish or waive before 4.0 ship"). Closes the provenance gap so future audits stop re-flagging them.
4. **Salvage `install-scope-check.js`** into CI (fixing finding #31 as part of the salvage).
5. **PF1 is a v4.0 release-gate decision, not cleanup.** Its evidence is unobtained (coverage-check 2026-06-28) — finish the battery or formally waive/re-scope the gate at the release decision.
6. **Carry the config-loader AD1-vs-AD8 contradiction into v4.1 sprint-planning** (see Confidence checks). Resolve whether cadence.yaml is read directly via config-loader or via a delegating `cadence-state.js`, and amend the frozen-API spec accordingly.
7. **Remaining 🔴 Live lows → backlog**, triaged normally.

---

## Findings

Each finding carries a **Status** from reconciliation + intent reconstruction. 🔴 Live = act now; 🟢 Reserved / 🟣 Blocked / 🟡 Salvage / 🔵 Known / 🟢 In-progress = accounted for.

### 🔴 High severity (2)

#### 1. compareVersions mishandles prerelease tags — rc→final upgrade misreads as a downgrade and is blocked

- **Status:** 🔵 Known — re-prioritize · ✅ reproduced 2026-06-28
- **Type:** execution  •  **Location:** [scripts/update/lib/utils.js:27](scripts/update/lib/utils.js#L27)  •  **Reviewer:** `update-engine`
- **Evidence:** compareVersions splits on '.' and maps Number: '4.0.0-rc.1'.split('.').map(Number) === [4,0,NaN,1]. compareVersions('4.0.0-rc.1','4.0.0') returns 1 (rc treated as GREATER than final); compareVersions('4.0.0','4.0.0-rc.1') returns -1. version-detector.getMigrationPath does `if (compareVersions(currentVersion, targetVersion) > 0) return {type:'downgrade'}`, and convoke-update.js:285 hard-exits 1 with 'DOWNGRADE DETECTED' on that action.
- **Impact:** The package is literally at convoke-agents@4.0.0-rc.1 shipping toward 4.0.0. Any operator who installed an rc and then runs `convoke-update` against the final 4.0.0 (or any later rc) will be told they are downgrading and the update will refuse to proceed (exit 1). This is a release-blocking correctness bug in the core version-ordering primitive used by getMigrationPath, convoke-version status, and changelog-reader range filtering. Note compat-preflight.js:113 already had to special-case this exact NaN trap by stripping the suffix before calling compareVersions — the core function was never fixed.
- **Suggested fix:** Make compareVersions SemVer-aware: split off the prerelease suffix on '-', compare the numeric core first, and only if cores are equal apply SemVer precedence (a version WITH a prerelease is LOWER than the same version without; compare prerelease identifiers dot-by-dot numerically/lexically). At minimum strip and compare the numeric core, then treat presence-of-prerelease as lower. Add a unit test pinning compareVersions('4.0.0-rc.1','4.0.0') === -1.
- **Verifier note:** Every claim in the finding holds against the real code. package.json is literally at convoke-agents@4.0.0-rc.1 (verified by running node). scripts/update/lib/utils.js:27-39 compareVersions does `v1.split('.').map(Number)`, so '4.0.0-rc.1' parses to [4,0,NaN,1]; I ran the real function and confirmed compareVersions('4.0.0-rc.1','4.0.0') === 1 and compareVersions('4.0.0','4.0.0-rc.1') === -1 (rc misordered as GREATER than final). version-detector.js:119 returns {type:'downgrade'} when compareVersions(current,target) > 0, and convoke-update.js:285-302 prints 'DOWNGRADE DETECTED' and process.exit(1) for that case — so an operator who installed the rc and runs convoke-update toward 4.0.0 is told they are downgrading and the update refuses (exit 1). convoke-version.js:80-86 likewise falls into the else and prints 'Package version (4.0.0) is older than installed (4.0.0-rc.1)' — backwards. The finding's strongest corroboration is real: compat-preflight.js:105-119 carries an explicit comment describing this exact NaN trap ('utils.js's compareVersions uses naive Number(part) which yields NaN... fall through to return 0') and strips the suffix as a local workaround rather than fixing the core primitive. Existing tests (tests/unit/utils.test.js) only cover purely numeric versions; no test pins prerelease ordering. This is a reachable, release-blocking ordering bug in the core version primitive used by getMigrationPath, version status, and changelog range filtering, with the rc-to-final upgrade path being exactly the path the project is currently on. Maintainers should make compareVersions SemVer-aware (prerelease < release) and add the suggested unit test. High severity is correct.
- **Reconciliation:** Already tracked as backlog item **U11** ("compareVersions semver-aware pre-release handling", score 0.9, logged 2026-04-18, deferred-from BUG-1+U7). The audit does not discover it — it **escalates** it: U11 was a deferrable edge case until the package reached `4.0.0-rc.1`; the rc→final upgrade path now makes it release-blocking. **Reproduced live 2026-06-28:** `compareVersions("4.0.0-rc.1","4.0.0") === 1` (rc ranked above final → update sees a downgrade). Action = raise U11 priority + add the failing test, not open a new story.

#### 2. Backup set excludes every file the 3.x→4.0 migration rewrites — rollback cannot restore them

- **Status:** 🔴 Live — untracked · ✅ reproduced 2026-06-28
- **Type:** execution  •  **Location:** [scripts/update/lib/backup-manager.js:212](scripts/update/lib/backup-manager.js#L212)  •  **Reviewer:** `update-engine`
- **Evidence:** getFilesToBackup() returns only _bmad/bme/_vortex/config.yaml, _bmad/bme/_vortex/agents, _bmad/bme/_vortex/workflows, and _bmad/_config/agent-manifest.csv. The 3.3.x-to-4.0.0 migration (apply→_phase3_sweepSkillMd, _phase4_deprecateBmadInit) rewrites 18 upstream-BMAD SKILL.md files across arbitrary _bmad/<module>/ paths (from v6.3-migration-inventory.csv) plus _bmad/core/bmad-init/SKILL.md. None of those paths are under the backed-up directories. migration-runner.js:193 calls backupManager.restoreBackup on failure, which only restores the four items in getFilesToBackup().
- **Impact:** If runMigrations fails after Phase 3/4 (e.g. validation fails at step [4/5], or a later module copy throws), the advertised automatic rollback silently leaves all rewritten SKILL.md activation blocks and the bmad-init deprecation banner in place while only reverting the Vortex subset. The operator is told 'Installation restored from backup' but is actually left in a half-migrated state for the upstream skills — the exact files most likely to break activation. This breaks the migration's central safety guarantee.
- **Suggested fix:** Before the SKILL.md sweep, snapshot every file the migration will touch (the canonical inventory targets + bmad-init/SKILL.md) into the backup dir, and have restoreBackup restore them. Either extend getFilesToBackup to include the migration's write set, or give each migration a backup-manifest hook the runner consumes so the backup scope tracks the migration's actual write surface rather than a fixed Vortex-only list.
- **Verifier note:** Verified directly against the code. backup-manager.js getFilesToBackup() (lines 212-235) returns only four Vortex-scoped items: _bmad/bme/_vortex/config.yaml, .../agents, .../workflows, and _bmad/_config/agent-manifest.csv. restoreBackup() (line 95) iterates that same list, so restore scope equals backup scope. The 3.3.x-to-4.0.0 migration's _phase3_sweepSkillMd writes to every canonical target in _bmad/_config/v6.3-migration-inventory.csv — which I inspected and confirmed live under _bmad/bmm/, _bmad/cis/, _bmad/tea/, _bmad/wds/ — and _phase4_deprecateBmadInit writes the deprecation banner to _bmad/core/bmad-init/SKILL.md. None of these paths fall under the four backed-up directories. migration-runner.js:193 calls backupManager.restoreBackup on any failure (including validation failure at step [4/5] line 144-145, or a refreshInstallation throw at step [3/5]) and then prints 'Installation restored from backup' (line 194), so the operator is told they are back to a clean pre-migration state when in fact every rewritten SKILL.md activation block and the bmad-init banner remain on disk. The migration's own header comment (line 26) even asserts 'Backup: handled upstream (backupManager)', codifying the false safety assumption. No project-context.md exception documents this as intentional. The finding is accurate, the rollback safety guarantee is genuinely broken for the upstream-BMAD skills (the files most likely to break activation), and a maintainer would act on it. High severity is justified: it defeats the migration's central advertised safety net during a BREAKING migration. The one caveat — the migration is resume-safe/idempotent via sentinels and phase3_files_done state — mitigates re-run corruption but does nothing for the false 'restored' claim, so it does not lower the severity.
- **Reconciliation:** **Untracked** (the only backup item in the backlog is I81, overwrite-clobbers — a different bug). **Proven by construction 2026-06-28:** the migration's write-set is the 19 `SKILL.md` targets in `_bmad/_config/v6.3-migration-inventory.csv` (under `_bmad/bmm/`, `_bmad/cis/`, …); the backup set is the 4 `_bmad/bme/_vortex/*` paths — **zero overlap**, so `restoreBackup` cannot restore any rewritten skill. Genuinely live; needs a new bug-fix story.

### 🟠 Medium severity (19)

#### 3. convoke-migrate applies a delta but never records migration_history

- **Status:** 🔴 Live — untracked
- **Type:** conception  •  **Location:** [scripts/update/convoke-migrate.js:84](scripts/update/convoke-migrate.js#L84)  •  **Reviewer:** `update-engine`
- **Evidence:** convoke-migrate main() does createBackup → migration.module.apply(projectRoot) → refreshInstallation, with no call to configMerger.addMigrationHistory / migration-runner.updateMigrationHistory. By contrast migration-runner.js:155 writes history after a successful run. convoke-migrate.js:49 itself gates on registry.hasMigrationBeenApplied (which reads migration_history).
- **Impact:** Two sources of truth diverge: convoke-update records applied migrations in config.yaml migration_history; convoke-migrate does not. After a manual convoke-migrate, `convoke-version` shows no record of it, and convoke-migrate's own already-applied guard (line 49) will never fire on re-run because history was never written. For older deltas with no internal idempotency marker (e.g. the taxonomy migrations, or the 1.x no-op deltas), re-running convoke-migrate re-executes the delta. The CLI's own guard contradicts its own write behavior.
- **Suggested fix:** After a successful delta+refresh in convoke-migrate, call the same updateMigrationHistory path migration-runner uses (read Vortex config.yaml, addMigrationHistory(config, fromVersion, toVersion, [migration.name]), writeConfig). Reuse migration-runner's helper rather than duplicating it so the two CLIs stay consistent.
- **Verifier note:** Verified directly in all three cited files. convoke-migrate.js main() does createBackup (line 79) → migration.module.apply(projectRoot) (line 84) → refreshInstallation (line 97), and the success path ends there — there is no call to configMerger.addMigrationHistory/writeConfig anywhere in the file, while migration-runner.js writes history via updateMigrationHistory() (lines 155, 274-293) after a successful run. Yet convoke-migrate.js:49 gates on registry.hasMigrationBeenApplied(migrationName, configPath), and registry.js:194-214 confirms that function reads config.migration_history[].migrations_applied — exactly the field convoke-migrate never writes. So the contradiction the finding describes is real: convoke-migrate's own already-applied guard can never fire as a result of a prior convoke-migrate run, and a manual migration leaves no record in config.yaml (the source of truth convoke-version/convoke-update consult). The re-execution risk is genuine for non-idempotent deltas, though somewhat softened in practice — the 1.x entries are explicit no-ops, the taxonomy deltas are "create or merge" (likely idempotent), and the 4.0.0 SKILL.md rewrites re-read fresh source on refresh. convoke-migrate is also explicitly the advanced/bypass path ("Manual migrations bypass safety checks"). That keeps it at medium, not high: it is a real conception/consistency defect a maintainer would fix (reuse migration-runner's updateMigrationHistory so the two CLIs share one history-write path), but it is not a data-loss or correctness-critical bug on the primary update flow.
- **Reconciliation:** Swept against the lifecycle backlog and the two non-main branches (`deprecation`, `test/governance-migration-v2`): no coverage found. Genuinely live and unowned — a real defect or accumulated debt, actionable now.

#### 4. Governed (frontmatter-bearing) files lose hcPrefix and date, silently disabling HC-chain (FR34) and date inference for the best-governed artifacts

- **Status:** 🔴 Live — untracked
- **Type:** conception  •  **Location:** [scripts/lib/portfolio/portfolio-engine.js:301](scripts/lib/portfolio/portfolio-engine.js#L301)  •  **Reviewer:** `lib-portfolio`
- **Evidence:** For files with frontmatter.initiative + artifact_type, the engine hardcodes typeResult = { type: artifactType, hcPrefix: null, ..., date: null, typeSource: 'frontmatter' } and passes hcPrefix:null/date:null into the enriched record (lines 351-352). Only the ungoverned branch calls inferArtifactType (line 304), which is what populates hcPrefix and date. Downstream, applyArtifactChainRule builds its hcPrefixes Set from a.hcPrefix (artifact-chain-rule.js:36) and detectHCChain depends on it; latestDate tracking (lines 41-49) depends on a.date.
- **Impact:** This inverts the design intent: the more complete a file's governance metadata, the less the portfolio engine can infer from it. A fully-governed Vortex HC artifact contributes nothing to HC-chain completeness detection (FR34) and reports date null, so last-activity/lastArtifact fall back entirely to git-recency. Frontmatter does not carry hcPrefix, so there is no compensating source.
- **Suggested fix:** Even for governed files, still run inferArtifactType(file.filename, taxonomy) (or extract hcPrefix/date from the filename) to populate hcPrefix and date, while keeping frontmatter as the authority for type/initiative. Merge: { ...inferArtifactType(...), type: frontmatter.artifact_type, typeSource: 'frontmatter' }.
- **Verifier note:** Verified directly against the code. In portfolio-engine.js the governed branch (line 295-301) hardcodes typeResult = { type, hcPrefix: null, ..., date: null, typeSource: 'frontmatter' } and never calls inferArtifactType, while the ungoverned branch (line 304) does. Both feed the enriched record at lines 351-352 (hcPrefix: typeResult.hcPrefix, date: typeResult.date). In rules/artifact-chain-rule.js, line 36 builds the hcPrefixes Set as artifacts.map(a => a.hcPrefix).filter(Boolean), so governed artifacts (hcPrefix null) are dropped and detectHCChain — the FR34 Vortex HC-chain completeness check (lines 81-85, 143-154) — never sees them; latestDate tracking (lines 41-49) likewise reads a.date and gets null. I confirmed in artifact-utils.js that hcPrefix and date are extracted ONLY from the filename (HC_PREFIX_PATTERN at line 356, DATED_PATTERN at line 349), and real governed artifacts in _bmad-output carry frontmatter (initiative, artifact_type, created, schema_version) with NO hcPrefix field, so there is no compensating source — frontmatter-rule.js reads only status/phase, not hcPrefix or the created date. The design inversion the finding describes is real: a fully-governed Vortex HC artifact contributes nothing to HC-chain detection. No test covers the governed+HC path (fixtures at portfolio-rules.test.js:180-181 hand-supply hcPrefix), and nothing documents the null as intentional. Severity medium is right: it silently degrades FR34 for the best-governed artifacts and is a real bug a maintainer would fix, but it is not a crash/data-corruption issue and date loss is partly cushioned by the git-recency rule. The suggested fix (still run inferArtifactType on the filename to recover hcPrefix/date while keeping frontmatter authoritative for type/initiative) is sound.
- **Reconciliation:** Swept against the lifecycle backlog and the two non-main branches (`deprecation`, `test/governance-migration-v2`): no coverage found. Genuinely live and unowned — a real defect or accumulated debt, actionable now.

#### 5. Vortex/Gyre agent roster + name mapping duplicated across agent-registry, export-engine, and classify-skills

- **Status:** 🔴 Live — untracked
- **Type:** conception  •  **Location:** [scripts/portability/export-engine.js:175](scripts/portability/export-engine.js#L175)  •  **Reviewer:** `invariant-sweep`
- **Evidence:** export-engine.js:175 BME_SKILL_TO_AGENT = { 'bmad-agent-bme-contextualization-expert':'Emma','bmad-agent-bme-discovery-empathy-expert':'Isla',... ,'bmad-agent-bme-team-factory':'Loom Master' }; and classify-skills.js:97 VORTEX_AGENTS = new Set(['bmad-agent-bme-contextualization-expert',...]) + GYRE_AGENTS Set. Both reproduce the roster that scripts/update/lib/agent-registry.js already defines canonically: AGENTS = [{ id:'contextualization-expert', name:'Emma' }, ... { id:'team-factory', name:'Loom Master' }] with the documented derivation `bmad-agent-bme-{id}` (agent-registry.js:219, 132-139). Neither portability file requires agent-registry.
- **Impact:** Multiple sources of truth for the agent set/name mapping. agent-registry.js is explicitly designed as the single source ("To add a new agent: push one entry to AGENTS") and already exposes id+name+pattern sufficient to derive both BME_SKILL_TO_AGENT (`bmad-agent-bme-${id}` -> name) and the VORTEX/GYRE partitions. Adding or renaming an agent today requires editing 3+ files in lockstep; a missed edit silently mis-classifies or mis-labels exported skills.
- **Suggested fix:** Import AGENTS from scripts/update/lib/agent-registry.js into both portability modules. Derive BME_SKILL_TO_AGENT as Object.fromEntries(AGENTS.map(a => [`bmad-agent-bme-${a.id}`, a.name])). Tag each agent-registry entry with its stream (vortex/gyre) and derive VORTEX_AGENTS/GYRE_AGENTS by filtering, eliminating the hand-maintained Sets.
- **Verifier note:** Verified against the real code. scripts/portability/export-engine.js:176-189 hand-maintains BME_SKILL_TO_AGENT (12 entries: bmad-agent-bme-{id} -> displayName), and scripts/portability/classify-skills.js:97-113 hand-maintains VORTEX_AGENTS (7) + GYRE_AGENTS (4) Sets keyed on the same bmad-agent-bme-{id} strings. scripts/update/lib/agent-registry.js is explicitly documented as the canonical single source ("Single source of truth..."; "To add a new agent: push one entry to AGENTS") and already exports AGENTS (7 Vortex, name Emma..Max), GYRE_AGENTS (4, Scout/Atlas/Lens/Coach), and EXTRA_BME_AGENTS (team-factory -> 'Loom Master'), with the bmad-agent-bme-{id} derivation documented at line 219. Every value in both portability maps is exactly reproducible: BME_SKILL_TO_AGENT = Object.fromEntries([...AGENTS, ...GYRE_AGENTS, ...EXTRA_BME_AGENTS].map(a => [`bmad-agent-bme-${a.id}`, a.name])); VORTEX_AGENTS/GYRE_AGENTS partitions are simply the AGENTS vs GYRE_AGENTS arrays (no stream-tag needed — they are already separate exported arrays, so the suggested fix's "tag each entry" framing is slightly off but the partition is trivially derivable). I confirmed neither portability file requires agent-registry (their requires are fs/path/utils/manifest-csv only). So this is a genuine multiple-source-of-truth defect: adding or renaming an agent requires lockstep edits across 3 files, and a missed edit silently mis-classifies (omitted from a Set -> wrong intent bucket) or mis-labels (missing map entry -> wrong dep attribution) exported skills. Severity medium is right: real maintainability/correctness hazard but not a runtime bug today, and the registry already exists making the fix mechanical. The registry's own EXTRA_BME_AGENTS comment (line 219) and the existing disjoint-id assertion show the project already treats this registry as authoritative, reinforcing that the portability files should consume it.
- **Reconciliation:** Swept against the lifecycle backlog and the two non-main branches (`deprecation`, `test/governance-migration-v2`): no coverage found. Genuinely live and unowned — a real defect or accumulated debt, actionable now.

#### 6. Canonical frontmatter parser exists but ~15 files re-roll their own YAML/frontmatter extraction (two YAML libs in parallel)

- **Status:** 🟢 In progress — expected
- **Type:** conception  •  **Location:** [scripts/update/lib/validator.js:609](scripts/update/lib/validator.js#L609)  •  **Reviewer:** `cross-cutting-conception`
- **Evidence:** scripts/lib/frontmatter.js was created specifically to be the single frontmatter parser (its docstring: 'Drop-in replacement for the subset of gray-matter... so we no longer pull in gray-matter's vulnerable js-yaml@3.x'). Yet only 3 files import it (artifact-utils.js:12, audit-bmad-init-refs.js:6, audit-bmm-dependencies.js:7). validator.js:609-618 re-extracts frontmatter with its own regex `/^---\n([\s\S]*?)\n---/` + `yaml.load`; pf1-validation-battery.js and export-engine.js/validate-exports.js hand-roll `^---` splitting too. Meanwhile two YAML libraries run in parallel: `yaml` (frontmatter.js, config-loader.js, generate-badges-json.js, refresh-installation write path) and `js-yaml` (~14 files incl. validator.js, migrate-artifacts.js, portfolio-engine.js, audit/*). artifact-utils.js even imports BOTH the canonical frontmatter parser (line 12) and js-yaml directly (line 11).
- **Impact:** Multiple sources of truth for the same parse concern. The ad-hoc regex parsers don't strip the BOM or tolerate CRLF the way frontmatter.js does (it explicitly handles 0xFEFF and \r?\n), so identical files can parse differently across subsystems. Running both js-yaml and yaml defeats the security rationale that motivated frontmatter.js and doubles the surface area for behavioral skew.
- **Suggested fix:** Route all frontmatter extraction through scripts/lib/frontmatter.js (move it to the base layer), replace the inline `^---` regexes in validator.js/pf1/export scripts with frontmatter.parse(), and standardize on a single YAML library, retiring whichever of js-yaml / yaml is redundant.
- **Verifier note:** Verified against the real code. The cited validator.js:609-618 is exact: it hand-rolls `content.match(/^---\n([\s\S]*?)\n---/)` then `yaml.load()` (js-yaml, required at validator.js:5) instead of using the canonical scripts/lib/frontmatter.js (which explicitly strips BOM at 0xFEFF and tolerates CRLF via `/^---\r?\n([\s\S]*?)\r?\n---\r?\n?/`). The single-parser intent in frontmatter.js's docstring is real, yet only artifact-utils.js, audit-bmad-init-refs.js, and audit-bmm-dependencies.js import it — and artifact-utils.js indeed imports BOTH js-yaml (line 11) and frontmatter (line 12), exactly as claimed. I found MORE hand-rolled `^---` parsers than the finding cited: validator.js:610, export-engine.js:407 & 963, validate-exports.js:83, audit-skill-dirs.js:130, validate-marketplace.js:225, pf1-judge-calibration.js:47 — none strip BOM or tolerate CRLF, so identical files can parse differently across subsystems (a genuine, actionable behavioral-skew concern). package.json confirms both js-yaml@^4.2.0 and yaml@^2.8.3 are real parallel dependencies (16 files require js-yaml, ~5 require yaml). Two minor inaccuracies that do not undermine the finding: (1) pf1-validation-battery.js does not parse frontmatter via `^---` splitting — it manually *serializes* it (lines 263-267), so it's a stringify-duplication example, not a parse one; (2) the security rationale is overstated — the vulnerability was js-yaml@3.x pulled transitively by gray-matter, and the project now uses js-yaml@4.x directly (not the vulnerable line), so coexisting js-yaml@4/yaml@2 does not 'defeat the security rationale,' though it does keep two parser surfaces alive. Net: a maintainer would genuinely act on consolidating frontmatter extraction through scripts/lib/frontmatter.js. Medium severity is appropriate — it's a maintainability/correctness-consistency issue, not a live security hole.
- **Reconciliation:** Active rollout, not settled debt. `scripts/lib/frontmatter.js` is the **newest code in the repository** (top of the commit log). The 3-adopt / ~15-bypass state is a migration mid-flight, days old. Do **not** action as debt — track it as the natural tail of an in-progress consolidation.

#### 7. Install-layout paths are a dispersed source of truth — no shared paths module

- **Status:** 🔴 Live — untracked
- **Type:** conception  •  **Location:** [scripts/update/lib/migration-runner.js:52](scripts/update/lib/migration-runner.js#L52)  •  **Reviewer:** `cross-cutting-conception`
- **Evidence:** The canonical install config path string `_bmad/bme/_vortex/config.yaml` is hardcoded in 10+ files across subsystems: version-detector.js:27, validator.js:76 & :120, migration-runner.js:52 & :275, convoke-version.js:114, convoke-migrate.js:48, backup-manager.js:216, 1.5.x-to-1.6.0.js:52, install-vortex-agents.js:113, isolated-install.js:46. The `_bmad-output` root literal appears 15x in artifact-utils.js alone and across utils.js, backup-manager.js (`_bmad-output/.backups` repeated at :22/:131/:195), migration-runner.js (`_bmad-output/.logs` at :322/:347). grep confirms NO scripts/lib/paths.js or constants.js exists.
- **Impact:** agent-registry.js advertises itself as the 'single source of truth' for agents/workflows, but the equally-load-bearing on-disk layout (module dir, config path, output/backup/log dirs) has no such anchor. Relocating the module or renaming the output tree requires a fragile multi-file find-and-replace, and any miss yields a silently-wrong path. This is precisely the multiple-sources-of-truth smell the registry pattern was meant to eliminate, applied inconsistently.
- **Suggested fix:** Add a base-layer scripts/lib/paths.js exporting derivations like vortexConfigPath(root), backupsDir(root), logsDir(root), outputDir(root), and have every subsystem build paths from it instead of inlining literals.
- **Verifier note:** Verified directly against the code. The literal `_bmad/bme/_vortex/config.yaml` is independently inlined (via path.join with projectRoot) in version-detector.js:27, validator.js:76 & :120, migration-runner.js:52 & :275, convoke-version.js:114, convoke-migrate.js:48, backup-manager.js:216, install-vortex-agents.js:113, and the fixture isolated-install.js:46 — none imports a shared constant; the migration file (1.5.x-to-1.6.0.js:52) uses the multi-segment form `path.join(projectRoot,'_bmad','bme','_vortex','config.yaml')`, which is the same canonical path expressed differently (reinforcing the dispersion point). The `_bmad-output` root literal recurs ~15x in artifact-utils.js and the `_bmad-output/.backups` (backup-manager.js:22/131/195) and `_bmad-output/.logs` (migration-runner.js:322/347) subtrees are repeated verbatim. `find` confirms no scripts/lib/paths.js or constants.js exists. These are semantically identical load-bearing paths, not coincidental constants or test fixtures. Crucially, the project documents its own single-source-of-truth discipline elsewhere (project-context.md rules no-hardcoded-versions, shared-test-constants; agent-registry.js header literally declares itself the 'single source of truth' for agents/workflows; portability/test-constants.js's FORBIDDEN_STRINGS), so the install-layout being dispersed is a genuine and self-acknowledged inconsistency a maintainer would act on. I rate it medium rather than high because the paths are static and currently correct — there is no active bug; the risk is maintenance fragility on a future relocation/rename, exactly the smell the finding names. The suggested fix (a base scripts/lib/paths.js with vortexConfigPath/backupsDir/logsDir/outputDir derivations) is the right shape and consistent with existing project conventions.
- **Reconciliation:** Swept against the lifecycle backlog and the two non-main branches (`deprecation`, `test/governance-migration-v2`): no coverage found. Genuinely live and unowned — a real defect or accumulated debt, actionable now.

#### 8. Migration state file (migration-state-4.0.yaml) survives rollback, leaving phase5_complete=true after files are reverted

- **Status:** 🔴 Live — untracked
- **Type:** execution  •  **Location:** [scripts/update/migrations/3.3.x-to-4.0.0.js:380](scripts/update/migrations/3.3.x-to-4.0.0.js#L380)  •  **Reviewer:** `update-engine`
- **Evidence:** _phase5_doctorDiff sets state.phase5_complete = true and persists it via _writeState to _bmad/_memory/migration-state-4.0.yaml. This path is NOT in backup-manager.getFilesToBackup(), and migration-runner rollback (restoreBackup) does not delete it. _phase1_detect returns {isPreV4:false} whenever state.phase5_complete === true.
- **Impact:** apply() completes all five phases (writing phase5_complete=true) before control returns to the runner, which THEN runs refreshInstallation + validation. If validation fails, the runner rolls back the Vortex files but the state file still says phase5_complete. A subsequent convoke-update re-run hits _phase1_detect → 'already complete' and SKIPS the migration entirely, even though the Vortex config/agents were just reverted to the pre-migration version. The state file and the on-disk reality disagree, and the operator cannot re-drive the migration without manually deleting the state file.
- **Suggested fix:** Either include _bmad/_memory/migration-state-4.0.yaml in the backup/restore set so rollback reverts it too, or only mark phase5_complete after the runner confirms validation passed (move the completion stamp out of apply() into a post-validation callback). Document the 'delete state file to retry' escape hatch in the rollback error message at minimum.
- **Verifier note:** Verified against the real code. In scripts/update/migrations/3.3.x-to-4.0.0.js, _phase5_doctorDiff (line 380) sets state.phase5_complete = true and persists it via _writeState (line 383) to _bmad/_memory/migration-state-4.0.yaml (path constant line 31). _phase1_detect (lines 158-159) returns {isPreV4:false} whenever phase5_complete === true, and apply() (lines 92-95) short-circuits with "Migration skipped: already at v4" on that. The runner (migration-runner.js) runs all five phases inside apply() (line 97) BEFORE refreshInstallation (line 116) and validation (line 127); on validation failure it throws and rolls back via backupManager.restoreBackup (line 193). backup-manager.getFilesToBackup() (lines 212-235) backs up only four paths — _bmad/bme/_vortex/config.yaml, agents, workflows, and _bmad/_config/agent-manifest.csv — none under _bmad/_memory/, and restoreBackup iterates that same list, so the state file is neither restored nor deleted. The other skip-guard, registry.hasMigrationBeenApplied (runner line 53), reads migration_history which is written only AFTER validation passes (lines 151-155), so on a failed run history is NOT recorded and the migration IS re-selected — meaning the surviving state file is indeed what silently skips the re-run, exactly as the finding claims. No documented exception exists in project-context.md, and no other code touches the state file or offers a rollback hook. Two refinements: (1) the finding slightly overstates the disagreement — the migration's primary outputs (SKILL.md rewrites + deprecation banner) also live outside the backup set and are NOT reverted, so it is really the Vortex config/agents that get reverted while the state says done; (2) triggering requires a validation failure and there is a manual workaround (delete the state file), which keeps it at medium rather than high. Still a genuine, actionable robustness gap contradicting the module's stated 'basic resume safety,' and a maintainer would act on the suggested fix (back up/restore the state file, or move the phase5 stamp into a post-validation callback).
- **Reconciliation:** Swept against the lifecycle backlog and the two non-main branches (`deprecation`, `test/governance-migration-v2`): no coverage found. Genuinely live and unowned — a real defect or accumulated debt, actionable now.

#### 9. Phase 6 config-var catch-all corrupts regex quantifiers and non-config braces

- **Status:** 🔴 Live — untracked
- **Type:** execution  •  **Location:** [scripts/portability/export-engine.js:509](scripts/portability/export-engine.js#L509)  •  **Reviewer:** `portability`
- **Evidence:** result = result.replace(/\{\{?([\w_-]+)\}?\}/g, (_match, varName) => { ... return 'your-project-context'; });  — applied to every section. Verified: input "Match names with /[a-z]{2}/ pattern ... key {sessionId}" becomes "/[a-z]your-project-context/ ... key your-project-context".
- **Impact:** The catch-all matches ANY {token} of word characters, not just config vars. A regex quantifier like {2}, JSON/template tokens, or legitimate identifier placeholders ({sessionId}) in step instructions get rewritten to 'your-project-context', producing broken regexes and nonsense in the exported instructions.md. Only a warning is emitted, so it ships silently. Violates catch-all-phase-review: the fallback matcher does not guard against false positives.
- **Suggested fix:** Restrict the catch-all to the snake_case config-var shape actually used by BMAD config (e.g. /\{\{?[a-z][a-z0-9_]*\}?\}/ with a minimum length, or only strip tokens containing an underscore / matching a known config-var prefix). Exclude purely-numeric tokens and tokens that are part of /.../  regex spans. Better: only substitute keys present in configVarMap and leave unknown braces untouched (still warn).
- **Verifier note:** Verified against the real code: scripts/portability/export-engine.js:509 is `result = result.replace(/\{\{?([\w_-]+)\}?\}/g, (_match, varName) => { ... return 'your-project-context'; })`, quoted verbatim. This catch-all runs inside applyTransformations on every extracted instruction section (howToProceed, qualityChecks, etc., line 1049), reachable via the published `convoke-export` bin. Running the actual exported applyTransformations on representative BMAD content confirms the corruption: `{outputFile}`, `{date}`, `{count}`, `{nextStepFile}`, `{targetWorkflowPath}` all collapse to the identical string `your-project-context`, and grep of _bmad/*.md shows hundreds of such non-config brace tokens ({date}=149, {count}=99, {outputFile}=61, {name}=40, {status}=36, etc.) that are not stripped by earlier phases. The defect is real, certain, and ships into exported instructions.md with only a warning — the comment at line 506 even rationalizes it as "avoid leakage," but rewriting every distinct placeholder to one wrong token is worse than leaving them. The suggested fix (substitute only configVarMap keys, leave unknown braces untouched) is sound. I downgraded the reviewer's "high" to "medium" because the most alarming sub-claim — broken regex quantifiers — is partly overstated: the catch-all's `[\w_-]+` does not match commas, so ranged quantifiers `{2,}`/`{2,5}` survive intact, and the corpus contains no exact-count `{2}` quantifiers at all (only the rare digit-only form is at risk). The actionable, real-world harm is the mass placeholder collapse, not regex breakage; it is non-crashing and warning-emitted, so medium is appropriate.
- **Reconciliation:** Swept against the lifecycle backlog and the two non-main branches (`deprecation`, `test/governance-migration-v2`): no coverage found. Genuinely live and unowned — a real defect or accumulated debt, actionable now.

#### 10. no-hardcoded-versions: target version '4.0.0-rc.1' hardcoded in PF1 recording provenance

- **Status:** 🟣 Blocked — keep (FINISH or waive)
- **Type:** execution  •  **Location:** [scripts/audit/pf1-record-agent.js:61](scripts/audit/pf1-record-agent.js#L61)  •  **Reviewer:** `audit-scripts`
- **Evidence:** const PHASE_TO_VERSION = {   baseline: '3.x',   'post-migration': '4.0.0-rc.1', }; ... used at line 156: `from convoke ${version} (commit ${commitSha})` in buildRecordingFile()
- **Impact:** '4.0.0-rc.1' is a semver literal that exactly matches package.json's current version. It is stamped verbatim into the provenance header of every post-migration recording file. The project invariant no-hardcoded-versions requires reading the version via getPackageVersion() (which exists in scripts/update/lib/utils.js). When the package ships 4.0.0 / 4.0.1 / etc., this script keeps stamping '4.0.0-rc.1', producing recordings whose provenance lies about which build they were captured from — defeating the whole point of the commit+version provenance stamp.
- **Suggested fix:** Import getPackageVersion from scripts/update/lib/utils and derive the post-migration version at runtime: `const { getPackageVersion } = require('../update/lib/utils'); ... 'post-migration': getPackageVersion()`. Keep 'baseline' literal only if it intentionally names a frozen pre-migration line; otherwise document why it is exempt.
- **Verifier note:** Verified against the real file: lines 61-64 contain `PHASE_TO_VERSION = { baseline: '3.x', 'post-migration': '4.0.0-rc.1' }`, consumed at line 155-156 to stamp provenance into every recording file — the quote is verbatim-accurate. The literal `4.0.0-rc.1` exactly matches package.json's current `version`, and the project documents a real `no-hardcoded-versions` invariant in project-context.md (line 29-35: "Always read via getPackageVersion()") with no exception for this script; `getPackageVersion()` exists in scripts/update/lib/utils.js and findProjectRoot is already imported here, so the suggested one-line fix is sound. The literal will rot: once the package bumps to 4.0.0/4.0.1, re-running the script stamps a stale version. However, severity is overstated as high for three reasons I confirmed in the code: (1) the sibling `baseline: '3.x'` is correctly a frozen, non-derivable experiment-arm label, so this map is semantically a phase->reference-line label, not purely a current-version read — the `post-migration` literal reflects defensible intent to name the v4.0 line; (2) the same provenance header also stamps the git commit SHA derived at runtime (line 119), which is the precise, non-rotting provenance identifier, so the version string is secondary/decorative and the "defeats the whole point of provenance" claim is exaggerated; (3) this is a Story-4.3 audit/experiment helper, not the update/migration core the invariant was written to protect — the rot mislabels experiment recordings rather than corrupting comparison logic. No test freezes this literal as a fixture. A maintainer would reasonably fix it to satisfy the documented invariant, but it is medium, not high.
- **Reconciliation:** **Intent reconstruction overturned the "archive" lean; a coverage-check (2026-06-28) resolved the open question.** PF1 is the **v4.0 behavioral-equivalence release gate (FR36-40, LLM-judge, drift T=4.0, FAIL blocks *release*)**. The earlier open question — "can I97's per-agent personality scoring (FR21-23) satisfy it?" — **resolves to NO**: FR36-40 and FR21-23 are from *different PRDs* measuring *different axes* (PF1 = LLM-judged structural+capability+persona drift gating release; I97 = operator-ranked persona-only gating *merge*), and I97 covers only **2 of PF1's 4 target agents** (Emma, Wade — not Liam, not the Gyre control). **PF1's gate evidence is genuinely unobtained.** `pf1-record-agent.js` was committed 2026-05-31 (after the snapshot called it "not yet created"), so it is mid-resumption. **Action: FINISH the battery (or formally waive/re-scope the gate at the v4.0 release decision) — do NOT archive.** The per-finding fix here still applies when work resumes.

#### 11. Module-load-time findProjectRoot() with no null guard crashes require() outside a project

- **Status:** 🟣 Blocked — keep (FINISH or waive)
- **Type:** execution  •  **Location:** [scripts/audit/pf1-judge-calibration.js:29](scripts/audit/pf1-judge-calibration.js#L29)  •  **Reviewer:** `audit-scripts`
- **Evidence:** const PROJECT_ROOT = findProjectRoot(); const PROMPT_PATH = path.join(PROJECT_ROOT, 'scripts/audit/pf1-judge-prompt.md'); // findProjectRoot() returns null when no _bmad/ ancestor exists (utils.js:88). Same top-level pattern in pf1-validation-battery.js:46, pf1-record-agent.js:46, install-scope-check.js:48.
- **Impact:** findProjectRoot() returns null when run outside a Convoke project. These four scripts call it at module top level and immediately pass the result to path.join(PROJECT_ROOT, ...). path.join(null, ...) throws a TypeError at REQUIRE time — before main()/its try-catch and exit-code mapping can run. pf1-validation-battery.js imports pf1-judge-calibration.js at the top, so requiring the battery (which tests/lib/pf1-validation-battery.test.js does) executes findProjectRoot() at import; in a context without _bmad/ the import itself throws. The sibling CLIs validate-marketplace.js and audit-skill-dirs.js correctly call findProjectRoot() inside main() and emit a clean 'Not inside a Convoke project' message — this group does not.
- **Suggested fix:** Move findProjectRoot() into main()/the CLI entry (or lazily compute PROJECT_ROOT inside functions), and emit the same clean 'no _bmad/ found' diagnostic + nonzero exit the other audit CLIs use. At minimum, null-check PROJECT_ROOT before any path.join and throw an Error with a tagged exitCode instead of letting path.join blow up at load time.
- **Verifier note:** Every claim verified against the real code. pf1-judge-calibration.js:29 does `const PROJECT_ROOT = findProjectRoot();` at module top level and immediately uses `path.join(PROJECT_ROOT, ...)` at line 32; utils.js:76-88 confirms findProjectRoot() returns null when no `_bmad/` ancestor exists. The identical top-level pattern (no null guard) is present in pf1-validation-battery.js:46, pf1-record-agent.js:46, and install-scope-check.js:48. pf1-validation-battery.js:44 imports pf1-judge-calibration.js at the top, so requiring the battery executes findProjectRoot() at import — and tests/lib/pf1-validation-battery.test.js:35 does `require(BATTERY_PATH)`, so outside a project the import itself would throw a TypeError before any main()/try-catch. The contrast is real: validate-marketplace.js:436-438 and audit-skill-dirs.js:375-377 call findProjectRoot() inside main() with an `if (!projectRoot)` guard emitting a clean 'Not inside a Convoke project (no _bmad/ directory found)' message and nonzero exit — this group does not. The suggested fix is sound and matches the existing in-project convention. I downgrade from high to medium because these are internal PF1 validation/audit dev-tooling scripts that always run inside the Convoke repo (where `_bmad/` is present — confirmed at repo root), so the crash path is never hit in normal dev/CI workflows; the real-world impact is a confusing TypeError instead of a clean diagnostic only in the edge case of requiring these scripts from a directory with no `_bmad/` ancestor. Also note the test file itself computes findProjectRoot() at its own top level (line 8), so it shares the same precondition and is not made strictly more fragile by the import — slightly tempering the 'import itself throws' framing, though the underlying inconsistency the finding flags is genuine.
- **Reconciliation:** **Intent reconstruction overturned the "archive" lean; a coverage-check (2026-06-28) resolved the open question.** PF1 is the **v4.0 behavioral-equivalence release gate (FR36-40, LLM-judge, drift T=4.0, FAIL blocks *release*)**. The earlier open question — "can I97's per-agent personality scoring (FR21-23) satisfy it?" — **resolves to NO**: FR36-40 and FR21-23 are from *different PRDs* measuring *different axes* (PF1 = LLM-judged structural+capability+persona drift gating release; I97 = operator-ranked persona-only gating *merge*), and I97 covers only **2 of PF1's 4 target agents** (Emma, Wade — not Liam, not the Gyre control). **PF1's gate evidence is genuinely unobtained.** `pf1-record-agent.js` was committed 2026-05-31 (after the snapshot called it "not yet created"), so it is mid-resumption. **Action: FINISH the battery (or formally waive/re-scope the gate at the v4.0 release decision) — do NOT archive.** The per-finding fix here still applies when work resumes.

#### 12. Epic completion markers fire on a single sub-task checkbox, falsely marking initiatives 'complete'

- **Status:** 🔴 Live — untracked
- **Type:** execution  •  **Location:** [scripts/lib/portfolio/rules/artifact-chain-rule.js:22](scripts/lib/portfolio/rules/artifact-chain-rule.js#L22)  •  **Reviewer:** `lib-portfolio`
- **Evidence:** DONE_PATTERNS = [ /(?:status|epic)[^:]*:\s*done\b/i, /(?:status|epic)[^:]*:\s*complete\b/i, /\*\*\s*done\b/i, /✅/, /\[x\]/i, /~~[^~]{3,}~~/ ]; isEpicDone() returns true if ANY pattern matches. The header comment claims patterns 'require status context to avoid false positives' and the rule's Priority-1 docstring says 'Epic with all stories done/complete/✅/[x]/strikethrough → complete'. But /\[x\]/i, /✅/, and the strikethrough pattern have NO status context — a single checked checkbox or one ✅ anywhere in an in-progress epic marks the WHOLE initiative complete. tests/lib/portfolio-rules.test.js:125 confirms content '- [x] All stories' → complete, and an epic with one checked subtask among many would match identically.
- **Impact:** This is the catch-all-phase-review invariant: a fallback/heuristic matcher must guard against false positives. A real in-progress epic with a single completed subtask (extremely common in story checklists) is misreported as 'complete', which then drives deriveNextAction to suggest a retrospective and removes the initiative from WIP radar — an actively misleading portfolio signal.
- **Suggested fix:** Require status context for the bare markers too: scope /\[x\]/, /✅/, and strikethrough to a line that also contains an epic/status/all-stories token (e.g. match within a 'Status:' or 'Epic ... :' line), or only treat the epic as done when EVERY story/checkbox in the file is checked (count [x] vs [ ]) rather than any single one.
- **Verifier note:** The finding is accurate against the real code. scripts/lib/portfolio/rules/artifact-chain-rule.js:22 defines DONE_PATTERNS exactly as quoted, isEpicDone (line 133) returns true if ANY pattern matches via .some(), and three of the patterns — /✅/, /\[x\]/i, and the strikethrough /~~[^~]{3,}~~/ — carry NO status context, contradicting both the file comment (line 20: "require status context to avoid false positives") and the Priority-1 docstring (line 5). The test at tests/lib/portfolio-rules.test.js:122-129 bakes the false-positive behavior in: content '- [x] All stories' → complete, and structurally a single checked subtask or one stray ✅ in an in-progress epic matches identically. The downstream impact is confirmed: a 'complete' phase drives deriveNextAction (conflict-resolver.js:81) to "Initiative complete — consider retrospective", an actively misleading portfolio signal. This squarely violates a documented project invariant (project-context.md:206-212, the catch-all/fallback-matcher false-positive-review rule), so the finding is not an undocumented exception. I found a concrete real-world trigger: convoke-epic-artifact-governance-portfolio.md:431 contains backtick-quoted `✅` and `[x]` in prose describing the markers — that line alone would mark the epic complete. I downgrade severity from high to medium because in this repo's actual epic-authoring convention, per-story `- [x]`/`- [ ]` mixed checklists are essentially absent (epics track status in the backlog/memory, not inline checkboxes), so the "common in-progress epic with one checked subtask" scenario is rarer here than the finding assumes; the realized harm is bounded to documentation-style files and any future epic that adopts inline checklists. The bug is real and worth fixing (scope the bare markers to a status/all-stories line, or require all checkboxes checked), but it is not a high-severity active data-corruption issue.
- **Reconciliation:** Swept against the lifecycle backlog and the two non-main branches (`deprecation`, `test/governance-migration-v2`): no coverage found. Genuinely live and unowned — a real defect or accumulated debt, actionable now.

#### 13. Tree-wide 'git reset --hard HEAD' rollback can destroy uncommitted work outside the migration scope that ensureCleanTree intentionally ignores

- **Status:** 🔴 Live — untracked
- **Type:** execution  •  **Location:** [scripts/lib/artifact-utils.js:1406](scripts/lib/artifact-utils.js#L1406)  •  **Reviewer:** `lib-portfolio`
- **Evidence:** executeRenames() and executeInjections() roll back with execFileSync('git', ['reset', '--hard', 'HEAD'], { cwd: projectRoot }) (lines 1406, 1427, 1616, 1645). The only pre-flight guard is ensureCleanTree(scopeDirs, projectRoot) (line 270), which scopes its diff/cached/ls-files checks to `_bmad-output/${dir}` for the scope dirs ONLY (line 272) and is invoked in scripts/migrate-artifacts.js:346 with filteredIncludeDirs. A user can therefore have legitimate uncommitted changes anywhere else in the repo (source, docs, other _bmad-output subdirs) that pass the clean-tree gate, and a mid-migration git mv / commit failure then runs a tree-wide hard reset that discards ALL of them.
- **Impact:** path-safety-for-destructive-ops: a destructive operation must be bounded to the project scope it operates on. The clean-tree precondition and the rollback blast radius are mismatched — the guard is scope-narrow, the destruction is tree-wide, so the guard does not actually protect the data the rollback can destroy.
- **Suggested fix:** Make rollback scope-bounded: reset only the migrated paths (git checkout -- <scopePaths> / git reset HEAD <scopePaths> then restore), or stash-and-restore unrelated changes, or have ensureCleanTree assert a fully clean tree (no path scoping) before any phase that can hard-reset. At minimum document and enforce that the whole tree must be clean before executeRenames.
- **Verifier note:** Verified against the real code. ensureCleanTree (scripts/lib/artifact-utils.js:270) scopes every precondition check — tracked diff (line 276), staged diff (line 289), and untracked-file scan (lines 302-304) — to `_bmad-output/${dir}` for the scope dirs only, and it is invoked at migrate-artifacts.js:346 with filteredIncludeDirs. The four rollback sites (lines 1406, 1427, 1616, 1645) all run execFileSync('git', ['reset', '--hard', 'HEAD'], { cwd: projectRoot }) with no pathspec. Technically, `git reset --hard HEAD` with no path argument resets the index and working tree to HEAD across the ENTIRE repo, silently discarding all tracked modifications (staged and unstaged) anywhere — source, docs, other _bmad-output subdirs — while only untracked files survive. So a user with legitimate uncommitted tracked edits outside the migration scope passes the scope-narrow clean-tree gate, and any mid-migration git mv/commit failure then destroys that out-of-scope work. The guard's scope and the rollback's blast radius are genuinely mismatched, which directly violates the project's documented path-safety-for-destructive-ops invariant (project-context.md:49-51). This is a real, actionable bug; the suggested fixes (scope the reset to migrated paths, stash unrelated changes, or assert a fully-clean tree before any hard-reset phase) are appropriate. I rate it medium rather than high because realizing the data loss requires the conjunction of (a) a git mv or commit actually failing mid-migration, (b) the user having uncommitted out-of-scope tracked changes, and (c) running the migration despite that — it is not on the happy path and untracked files are unaffected — but the silent, irreversible nature of the loss keeps it above low.
- **Reconciliation:** Swept against the lifecycle backlog and the two non-main branches (`deprecation`, `test/governance-migration-v2`): no coverage found. Genuinely live and unowned — a real defect or accumulated debt, actionable now.

#### 14. executeInjections stages the entire _bmad-output/ directory, absorbing unrelated changes into the injection commit

- **Status:** 🔴 Live — untracked
- **Type:** execution  •  **Location:** [scripts/lib/artifact-utils.js:1637](scripts/lib/artifact-utils.js#L1637)  •  **Reviewer:** `lib-portfolio`
- **Evidence:** execFileSync('git', ['add', '_bmad-output/'], { cwd: projectRoot, stdio: 'pipe' }) stages everything under _bmad-output/ before committing 'chore: inject frontmatter metadata and update links'. This contradicts the sibling ADR-phase logic in scripts/migrate-artifacts.js:398-404 which deliberately stages 'only the paths this phase writes — not the entire planning-artifacts/ dir — so unrelated staged/modified files in the directory don't get absorbed'.
- **Impact:** Any incidental file under _bmad-output/ (outside the scope dirs, e.g. _archive edits, drafts, brainstorming) gets swept into the migration's injection commit, polluting the atomic migration history and making the rollback hard-reset (which targets HEAD) even more dangerous. Inconsistent staging discipline within the same codebase is a coupling/correctness defect.
- **Suggested fix:** Stage only the files this phase actually writes: the renamed-file paths it injected into, the updated-link files returned by updateLinks, and the rename-map path. Build an explicit path list and `git add -- <paths>` exactly like the ADR phase does.
- **Verifier note:** Verified directly against the code. scripts/lib/artifact-utils.js:1637 does `execFileSync('git', ['add', '_bmad-output/'], ...)` then commits "chore: inject frontmatter metadata and update links" — staging the entire _bmad-output/ tree. The sibling ADR phase in scripts/migrate-artifacts.js:398-404 deliberately builds an explicit pathsToStage list and runs `git add -- <paths>`, with a comment explicitly warning against absorbing "unrelated staged/modified files." So the inconsistency the finding describes is real and the codebase documents the correct pattern that the injection phase violates. Crucially, the pre-flight does NOT neutralize this: ensureCleanTree (artifact-utils.js:270) checks for dirty/staged/untracked files scoped to `_bmad-output/<scopeDir>` paths ONLY (line 272), not the whole _bmad-output/ directory. Therefore any uncommitted, staged, or untracked file living under _bmad-output/ but outside the configured scope dirs (e.g. _archive edits, vortex-artifacts, drafts) passes the clean-tree gate and then gets swept into the injection commit by the broad `git add _bmad-output/`. This pollutes the atomic migration history and makes the subsequent `git reset --hard HEAD` rollback (lines 1616, 1645) more destructive, since it now also reverts absorbed unrelated work. It is conditional (requires out-of-scope dirty files under _bmad-output/), which is why it is medium rather than high; the suggested fix — stage only renamed paths, updateLinks-touched files, and the rename-map path — matches the established sibling pattern and is a reasonable action a maintainer would take.
- **Reconciliation:** Swept against the lifecycle backlog and the two non-main branches (`deprecation`, `test/governance-migration-v2`): no coverage found. Genuinely live and unowned — a real defect or accumulated debt, actionable now.

#### 15. convoke-check 'CI mirror' runs a Jest step that cannot succeed — jest is not a dependency and the lib tests are node:test

- **Status:** 🔴 Live — untracked
- **Type:** execution  •  **Location:** [scripts/convoke-check.js:26](scripts/convoke-check.js#L26)  •  **Reviewer:** `entrypoints`
- **Evidence:** STEPS includes `{ name: 'Jest lib tests', cmd: 'npx jest tests/lib/ --no-coverage' }`. But package.json has NO jest in dependencies or devDependencies (`node -e ... jest dep: undefined`), and every tests/lib/*.test.js imports `node:test` (grep confirms node:test in artifact-utils/config-loader/manifest/... ; project MEMORY records the C1 conversion from Jest to node:test and removal of Jest globals). The file header asserts 'Runs the same checks as .github/workflows/ci.yml', yet ci.yml runs only `npm test`, `npm run test:integration`, `npm run test:coverage` (grep of ci.yml shows no jest job at all). `npm test` already covers tests/lib via `node scripts/test-runner.js tests/unit tests/team-factory tests/lib`.
- **Impact:** `npm run check` (the documented pre-push gate, used in dev-story step 9 per the header) will hang on `npx jest` attempting to download a non-installed package, or fail outright — and the failure is in a step CI does not even have, so it is a false negative that blocks pushes. It also double-runs tests/lib (already inside `npm test`).
- **Suggested fix:** Delete the Jest step from STEPS. The remaining Lint / Unit / Integration / Coverage steps already mirror the three CI test jobs. If a separate lib pass is wanted, use `node scripts/test-runner.js tests/lib` instead of npx jest.
- **Verifier note:** Verified directly against the real code. scripts/convoke-check.js:26 contains the step { name: 'Jest lib tests', cmd: 'npx jest tests/lib/ --no-coverage' }. package.json has no jest in dependencies or devDependencies (both resolve to undefined) and the jest binary is absent from node_modules/.bin, so npx jest would try to fetch an uninstalled package (prompt/hang or fail) — and even if it ran, all 30 tests/lib/*.test.js files import node:test, not jest, so jest would discover no compatible tests. The file header's claim that it 'Runs the same checks as .github/workflows/ci.yml' is false: ci.yml has zero jest references and runs only npm test, npm run test:integration, and npm run test:coverage. The step is also redundant because npm test is 'node scripts/test-runner.js tests/unit tests/team-factory tests/lib', which already runs tests/lib via the native runner. This is a genuine bug in the documented pre-push gate (npm run check), and the suggested fix (delete the Jest step) is correct. I downgrade severity from high to medium because this is a developer-tooling script that is opt-in and not part of CI itself; it breaks/blocks the local pre-push gate rather than shipping any runtime or user-facing defect.
- **Reconciliation:** Swept against the lifecycle backlog and the two non-main branches (`deprecation`, `test/governance-migration-v2`): no coverage found. Genuinely live and unowned — a real defect or accumulated debt, actionable now.

#### 16. Fresh-install manifest row counts (12 / 14) hardcoded instead of derived from the agent registry

- **Status:** 🔴 Live — untracked
- **Type:** execution  •  **Location:** [tests/integration/fresh-install.test.js:251](tests/integration/fresh-install.test.js#L251)  •  **Reviewer:** `test-architecture`
- **Evidence:** Line 251: `assert.equal(dataRows.length, 12, 'should have 12 agent rows (7 vortex + 4 gyre + 1 standalone bme: team-factory)');` and line 287: `assert.equal(lines.length, 14, 'should have header + 1 bmm + 12 bme rows (7 vortex + 4 gyre + 1 standalone)');`. The numbers are asserted against the live output of refreshInstallation(tmpDir). The component counts are all exported from scripts/update/lib/agent-registry.js (AGENTS, GYRE_AGENTS, EXTRA_BME_AGENTS — confirmed exported at lines 285-289 and referenced by refresh-installation.js line 12), so the expected total is computable as AGENTS.length + GYRE_AGENTS.length + EXTRA_BME_AGENTS.length.
- **Impact:** Violates derive-counts-from-source: when an agent is added to any of the three registry arrays, refreshInstallation correctly writes the extra row but this test fails with an opaque '13 !== 12' instead of tracking the source of truth. The literal and its prose comment are a second source of truth for the agent count that must be hand-edited in lockstep with the registry.
- **Suggested fix:** Import the registry arrays and compute the expected count: `const { AGENTS, GYRE_AGENTS, EXTRA_BME_AGENTS } = require('../../scripts/update/lib/agent-registry'); const bmeRows = AGENTS.length + GYRE_AGENTS.length + EXTRA_BME_AGENTS.length;` then assert `dataRows.length === bmeRows` and `lines.length === bmeRows + 2` (header + 1 preserved bmm row). Keep the column-count assertion (12) since that is the structural CSV schema, not an agent count.
- **Verifier note:** Verified against real code. tests/integration/fresh-install.test.js:251 asserts dataRows.length === 12 and :287 asserts lines.length === 14, both against the live output of refreshInstallation(tmpDir). refresh-installation.js:12 derives those manifest rows from AGENTS, GYRE_AGENTS, and EXTRA_BME_AGENTS imported from agent-registry.js, and those three arrays are exported (registry lines 277/285/290-291) and sum to exactly 12 (7+4+1=12, confirmed by running node). So 12 and 14 (=12+header+1 preserved bmm row) are a hand-maintained second source of truth for the agent count, and adding an agent to any registry array would fail these with an opaque '13 !== 12'. This matches the project's derive-counts-from-source culture; tellingly, the SAME test file already imports AGENTS from the registry and iterates it dynamically at line 307 in the next describe block, so these two literals are an inconsistency rather than a documented exception. The finding's quotes, line numbers, export claims, and computed total all hold up exactly. It is a genuine, actionable test-brittleness fix a maintainer would accept; severity medium is reasonable (slightly generous since the assertions are currently correct and this is maintainability not a live bug), and the suggested fix should add a brief comment explaining the +2 = header + preserved bmm row.
- **Reconciliation:** Swept against the lifecycle backlog and the two non-main branches (`deprecation`, `test/governance-migration-v2`): no coverage found. Genuinely live and unowned — a real defect or accumulated debt, actionable now.

#### 17. FORBIDDEN_IN_EXAMPLE duplicates the shared FORBIDDEN_STRINGS list instead of importing it

- **Status:** 🔴 Live — untracked
- **Type:** execution  •  **Location:** [tests/lib/portability-canonical-format.test.js:28](tests/lib/portability-canonical-format.test.js#L28)  •  **Reviewer:** `invariant-sweep`
- **Evidence:** const FORBIDDEN_IN_EXAMPLE = [ 'Read tool', 'Edit tool', 'Write tool', 'Bash tool', 'Glob tool', 'Grep tool', 'Skill tool', 'bmad-init', 'bmad-help', 'bmad-speak', '_bmad/', '.claude/hooks', '{project-root}', 'Load step:', 'read fully and follow', 'Read fully and execute:', 'Load fully and follow:' ]; — verified byte-for-byte identical (17/17 entries, 0 diff) to FORBIDDEN_STRINGS exported from scripts/portability/test-constants.js. Sibling tests (portability-export-engine.test.js, portability-export-all.test.js, portability-catalog-generator.test.js) all `require` the shared constant; this file (and only this file) inlines a copy.
- **Impact:** Violates shared-test-constants invariant: a forbidden-string list used by 2+ test files must be imported from the shared constants file, not duplicated. test-constants.js's own header explicitly states it was extracted to close this exact debt ("flagged in Epic 2 retro A4, Epic 3 retro A1, Epic 4 retro A1 as the #1 test-reliability debt item"). The inline copy will silently drift when the exporter's strip rules change, producing a false-green here while siblings catch a regression — or vice versa.
- **Suggested fix:** Delete the inline FORBIDDEN_IN_EXAMPLE array and replace its uses with `const { FORBIDDEN_STRINGS } = require('../../scripts/portability/test-constants');` then iterate over FORBIDDEN_STRINGS in Test 3, matching the pattern already used by the three sibling portability tests.
- **Verifier note:** Confirmed against the real code. tests/lib/portability-canonical-format.test.js lines 28-50 define FORBIDDEN_IN_EXAMPLE with exactly the same 17 entries, same ordering, and same section comments as FORBIDDEN_STRINGS exported from scripts/portability/test-constants.js (lines 18-40) — byte-for-byte identical. The shared file's own header documents that it was extracted specifically to close this debt ("flagged in Epic 2 retro A4, Epic 3 retro A1, Epic 4 retro A1 as the #1 test-reliability debt item"). I verified via grep that three sibling tests (portability-export-engine.test.js, portability-catalog-generator.test.js, portability-export-all.test.js) plus two scripts all `require` the shared constant, while this one file inlines a copy — the finding's sibling claim is accurate, and the import pattern in the suggested fix matches the siblings exactly (line 16 of portability-export-engine.test.js). The duplication is real and will silently drift when the exporter's strip rules change, producing a false-green or false-red divergence between this test and its siblings. Note the file also derives REQUIRED_TOOL_NAMES_IN_FORMAT (the first 7 tool-name entries) for a different purpose, which legitimately remains inline; only the forbidden-string portion is the actionable duplication. Severity medium is correct: this is documented test-reliability debt a maintainer would act on, not a runtime defect.
- **Reconciliation:** Swept against the lifecycle backlog and the two non-main branches (`deprecation`, `test/governance-migration-v2`): no coverage found. Genuinely live and unowned — a real defect or accumulated debt, actionable now.

#### 18. ~110 lines of installer scaffolding duplicated verbatim between install-vortex and install-gyre with no shared module

- **Status:** 🔴 Live — untracked
- **Type:** muda  •  **Location:** [scripts/install-gyre-agents.js:18](scripts/install-gyre-agents.js#L18)  •  **Reviewer:** `entrypoints`
- **Evidence:** printBanner (the entire CONVOKE ASCII block), checkPrerequisites (identical _bmad dir + bmad.yaml probe), createOutputDirectory, verifyInstallation (identical loop + exit logic), printSuccess (identical box + next-steps loop), and the 7 ANSI color constants are byte-for-byte duplicated between install-vortex-agents.js and install-gyre-agents.js. grep confirms `printBanner`/`checkPrerequisites`/`verifyInstallation` exist ONLY in these two files — no shared helper. The only real deltas are the registry array (AGENTS vs GYRE_AGENTS), step counts ([n/5] vs [n/4]), output dir name, agent-file leaf shape (`<id>/SKILL.md` vs `<id>.md`), and the banner subtitle.
- **Impact:** Divergence risk: the Gyre `verifyInstallation` already had to be kept in lock-step manually (note the SKILL.md vs .md leaf difference between the two), and the R2-H1 comment in install-gyre main() documents a prior bug where the two scripts drifted on null-handling. Every future installer change must be applied twice. Adding a third team multiplies the copy.
- **Suggested fix:** Extract a shared `scripts/update/lib/installer-ui.js` (or similar) exposing `printBanner(subtitle)`, `checkPrerequisites(projectRoot)`, `createOutputDirectory(projectRoot, artifactsDirName)`, `verifyInstallation(projectRoot, checks)`, `printSuccess(agents, opts)` parameterized by the per-team specifics. Both installers (and any future one) then become ~30 lines of wiring.
- **Verifier note:** Verified against both files. The duplication is real and substantial: the 7 ANSI color constants (lines 10-16), printBanner with the full CONVOKE ASCII block (18-30), checkPrerequisites with identical _bmad dir + bmad.yaml probe (32-52), createOutputDirectory, verifyInstallation with identical loop + process.exit(1) logic, and printSuccess with identical box + next-steps loops are duplicated near byte-for-byte between scripts/install-vortex-agents.js and scripts/install-gyre-agents.js. grep confirms printBanner/checkPrerequisites/verifyInstallation are defined ONLY in these two files, and there is no shared installer-ui module under scripts/update/lib/ (only refresh-installation.js handles the actual file copying). The deltas the finding lists are accurate: registry (AGENTS vs GYRE_AGENTS), step counts ([n/5] vs [n/4]), output dir (vortex-artifacts vs gyre-artifacts), banner subtitle, and the verifyInstallation agent-file leaf (Vortex uses `${a.id}/SKILL.md`, Gyre uses `${a.id}.md`) — a manually-maintained divergence point. The R2-H1 comment in install-gyre main() (lines 123-130) is genuine and documents a prior real bug where the two scripts drifted on null-handling of projectRoot, which substantiates the divergence-risk argument. This is a legitimate muda/maintainability finding a maintainer would act on, especially before adding a third team installer. Severity is medium-leaning-low: the code currently works correctly, only two installers exist today, and Vortex carries genuinely unique logic (archiveDeprecatedWorkflows/cleanupLegacyFiles) that the suggested extraction must accommodate, so it is cleanup rather than a correctness defect.
- **Reconciliation:** Swept against the lifecycle backlog and the two non-main branches (`deprecation`, `test/governance-migration-v2`): no coverage found. Genuinely live and unowned — a real defect or accumulated debt, actionable now.

#### 19. FORBIDDEN_IN_EXAMPLE duplicates the shared FORBIDDEN_STRINGS list byte-for-byte

- **Status:** 🔴 Live — untracked
- **Type:** muda  •  **Location:** [tests/lib/portability-canonical-format.test.js:28](tests/lib/portability-canonical-format.test.js#L28)  •  **Reviewer:** `test-architecture`
- **Evidence:** Lines 28-50 define a local `const FORBIDDEN_IN_EXAMPLE = [...]` containing exactly the same entries as `FORBIDDEN_STRINGS` exported from scripts/portability/test-constants.js (the same 7 Claude tool names 'Read tool'..'Skill tool', the same framework calls 'bmad-init'/'bmad-help'/'bmad-speak', the same framework paths '_bmad/'/'.claude/hooks'/'{project-root}', and the same 4 micro-file directives). Sibling tests portability-export-engine.test.js, portability-catalog-generator.test.js, and portability-export-all.test.js all `require('../../scripts/portability/test-constants')` for this exact list. The shared file's own header documents this list was 'flagged in Epic 2 retro A4, Epic 3 retro A1, Epic 4 retro A1 as the #1 test-reliability debt item' and 'Import this instead of duplicating the list.'
- **Impact:** Direct violation of the shared-test-constants invariant: a forbidden-string list used by 2+ test files is duplicated rather than imported. The two lists will drift — a forbidden token added to test-constants.js (e.g. a new Claude tool name) will silently NOT be enforced against canonical-example.md, creating a false-negative gap exactly where the project already recorded this as its top test-reliability debt.
- **Suggested fix:** Delete the local FORBIDDEN_IN_EXAMPLE array and replace its uses (lines 142-144) with `const { FORBIDDEN_STRINGS } = require('../../scripts/portability/test-constants');` then iterate `for (const forbidden of FORBIDDEN_STRINGS)`, matching the other three portability test files.
- **Verifier note:** Verified directly against the code. tests/lib/portability-canonical-format.test.js lines 28-50 define a local FORBIDDEN_IN_EXAMPLE array whose 17 entries (7 Claude tool names, bmad-init/bmad-help/bmad-speak, _bmad/ + .claude/hooks + {project-root}, and 4 micro-file directives) are identical entry-for-entry and order-for-order to FORBIDDEN_STRINGS exported from scripts/portability/test-constants.js lines 18-40. The three sibling portability tests (portability-export-engine.test.js:16, portability-catalog-generator.test.js:63, portability-export-all.test.js:22) all require that shared module and iterate FORBIDDEN_STRINGS, so the canonical-format test is the sole file that hand-rolls a duplicate. The shared file's header (lines 5-8) explicitly documents this exact list as the '#1 test-reliability debt item' flagged across Epic 2/3/4 retros and instructs 'Import this instead of duplicating the list,' making this a clear, project-acknowledged invariant violation. The suggested fix is correct and matches the established sibling pattern; note the separate REQUIRED_TOOL_NAMES_IN_FORMAT subset (lines 54-62) is correctly out of scope. Severity is medium rather than high because the two lists are currently in sync (no live incorrectness), it is isolated test-code muda affecting a single file, and the failure mode is a future false-negative only if a maintainer edits one list without the other — a genuine drift risk a maintainer would act on, but not a production or currently-failing defect.
- **Reconciliation:** Swept against the lifecycle backlog and the two non-main branches (`deprecation`, `test/governance-migration-v2`): no coverage found. Genuinely live and unowned — a real defect or accumulated debt, actionable now.

#### 20. config-loader.js: fully-built, fully-tested v4 config module with zero production consumers (created-but-never-wired)

- **Status:** 🟢 Reserved — pre-positioned (KEEP)
- **Type:** muda  •  **Location:** [scripts/update/lib/config-loader.js:325](scripts/update/lib/config-loader.js#L325)  •  **Reviewer:** `muda-triage`
- **Evidence:** Sole export is `module.exports = { loadModuleConfig };` (line 325). A repo-wide grep for `loadModuleConfig` and `config-loader` across scripts/ + index.js + tests/ returns hits ONLY in: (a) its own dedicated test `tests/lib/config-loader.test.js` (which imports and exercises it ~40x), and (b) a deprecation-message string literal in `scripts/update/migrations/3.3.x-to-4.0.0.js:42` ('via `scripts/update/lib/config-loader.js`...' — a text mention, not a require). NO production module (migration, installer, refresh, validator, doctor) requires it. The module's own header says it 'Replaces the legacy bmad-init skill load path' (Story v63-1a-2), but that replacement was never plumbed into any caller.
- **Impact:** This is the high-signal 'created-but-never-wired' case the v6.3 adoption work introduced (Story 1A.2). The pinning test at tests/lib/config-loader.test.js gives the file a false appearance of liveness (it passes, has coverage), but the v4 direct-YAML load path it provides is dead — nothing in the runtime ever calls loadModuleConfig(). It carries a subprocess-shelling backwards-compat branch (execFileSync of bmad_init.py) and a 30s timeout / 10MB buffer config that will never execute in production. It is maintenance surface and reader-confusion with no payoff.
- **Suggested fix:** Either WIRE IT UP (have the v4 migration / agent-activation path actually call loadModuleConfig instead of the legacy load path it was meant to replace) or DELETE both scripts/update/lib/config-loader.js and tests/lib/config-loader.test.js. Do not leave a tested-but-unconsumed module: pick one. Given Story 4.3 is stalled and 4.0 ships on the v6.3 baseline, confirm with the release owner whether the direct-load path is still on the 4.0 critical path before deleting.
- **Verifier note:** Verified against the real code. config-loader.js exports only { loadModuleConfig } at line 325, and a repo-wide grep for require()/loadModuleConfig confirms ZERO production consumers: the only require() of the module is in tests/lib/config-loader.test.js (which exercises it ~40x), the only other code reference is a deprecation-message string literal in scripts/update/migrations/3.3.x-to-4.0.0.js:42 (text inside a console warning, not a require), and the remaining hits are all planning/epic markdown describing FUTURE v4.1 cadence-state work ("read via config-loader"). No migration, installer, refresh, validator, or doctor requires it. The module's own header (line 11) states it "Replaces the legacy bmad-init skill load path," but that replacement was never wired into any caller, including the legacy execFileSync(bmad_init.py) fallback with its 30s timeout / 10MB buffer that can never run in production. This is a legitimate created-but-never-wired (muda) finding a maintainer would act on. Severity medium is correct, not high: it is not a correctness bug, the dead surface is well-documented, and the planning docs (convoke-epic-bmad-v6.4-v6.8-absorption.md, Story I113/E1) explicitly earmark config-loader as the v4.1 cadence-state reader — which is precisely why the finding's suggested fix correctly hedges "confirm with the release owner before deleting" rather than recommending an outright delete. The finding's quotes and grep claims all reproduce exactly.
- **Reconciliation:** **Intent reconstruction overturns the "muda" verdict; a fit-check (2026-06-28) refines the rationale.** config-loader is a **named, load-bearing component of v4.1 Epic E4 "Managed Currency" (MVP)** — AD1 + Epic 1 Story 1.1 (`convoke-epic-bmad-v6.4-v6.8-absorption.md:147`) say cadence state is "read via config-loader"; the arch file-tree marks it "config-loader.js # **MODIFIED** load cadence.yaml" (`convoke-arch-bmad-v6.4-v6.8-absorption.md:203`). **It is NOT a drop-in frozen reader, though:** the frozen API hardcodes the filename `config.yaml` (`config-loader.js:104`) and v4.1 routes cadence I/O through a *new* `cadence-state.js` module (AD8: "no other module touches the file directly"), with all *writes* outside config-loader's scope. So v4.1 reuses its **read machinery** (YAML parse, `_bmad/` traversal guard, `{project-root}` resolution) — not the entry point, which needs a spec amendment to read `cadence.yaml`. **Action: KEEP (deleting a named v4.1 foundation would be wrong); annotate the header "read primitives reserved for v4.1 E4 — cadence-state.js (AD1/AD8)". ⚠ The fit-check surfaced an internal v4.1 arch contradiction — AD1 ("read via config-loader") vs AD8 ("cadence-state.js owns all cadence I/O") — captured for v4.1 sprint-planning.**

#### 21. install-scope-check.js: orphaned operator CLI with no code, test, or CI references

- **Status:** 🟡 Salvage — promote to CI
- **Type:** muda  •  **Location:** [scripts/audit/install-scope-check.js:1](scripts/audit/install-scope-check.js#L1)  •  **Reviewer:** `muda-triage`
- **Evidence:** A grep for `install-scope-check` across all *.js/*.json/*.yml/*.yaml (excluding node_modules, exported-skills, and self/comment references) returns ZERO hits. It is not in package.json `bin`/`scripts`, not in any .github workflow, not required anywhere, and has no test file. The only references are: (a) two source COMMENTS in pf1-validation-battery.js:51 and pf1-record-agent.js:53 ('replaced by mechanical install-scope-check.js'), and (b) Story-4.3 markdown under _bmad-output/. Its own header documents a snapshot-based write-op-count assertion driven by a `--update` operator-confirmed snapshot file.
- **Impact:** Like pf1-record-agent.js, this is dormant Story-4.3 (Path B+ re-scope) operator tooling that substitutes mechanical scope-checking for 4 BMAD-agent control validation. With Story 4.3 stalled and no programmatic entry point, test, or snapshot fixture committed/exercised, the script provides no executing guard today — a migration could leak write scope into BMAD-owned dirs and nothing runs this check. It is either an unfinished safety net (should be wired into CI) or dead weight.
- **Suggested fix:** Decide its fate explicitly: if the install-scope guard is valuable (it catches migration-scope-leak regressions cheaply), wire `node scripts/audit/install-scope-check.js` into ci.yml and commit the snapshot fixture so it actually runs on every PR. Otherwise delete it. Leaving an unwired guard gives false assurance of scope protection.
- **Verifier note:** Verified directly against the code. scripts/audit/install-scope-check.js (added in a single commit d5b3ef6f, dated 2026-05-29) is an executable Node CLI whose entire purpose is a snapshot-based migration write-scope guard. A repo-wide grep for `install-scope-check`/`scanForbiddenPatterns`/`countWriteOps` (excluding node_modules and exported-skills) returns ONLY: two source COMMENTS in pf1-validation-battery.js:51 and pf1-record-agent.js:53 ("BMAD-agent control validation replaced by mechanical install-scope-check.js"), and self/usage references in its own header. I confirmed it is absent from package.json `bin` (which DOES wire sibling audit scripts like validate-marketplace.js and audit-skill-dirs.js as `convoke-validate-marketplace`/`convoke-audit-skill-dirs`, proving the maintainer's pattern is to expose useful audit scripts) and absent from package.json `scripts`. .github/workflows/ci.yml has no run step invoking it; the only `audit` reference there is `npm audit`. No *.test.js references it or its exported functions. It is NOT a dynamically-loaded migration (those live under scripts/update/migrations/ and load via registry.js; this lives under scripts/audit/ and is loaded by nothing). The lone execution surface it gets is ESLint (scripts/ is linted, not ignored), which is static-only and does not exercise the guard. This corroborates the finding's framing as dormant Story-4.3 (Path B+ re-scope) operator tooling — project memory independently records Story 4.3 as STALLED. The guard therefore runs on zero PRs and provides no executing protection; a migration could leak write scope into BMAD-owned dirs (the very thing FORBIDDEN_PATTERNS targets) and nothing would catch it. A maintainer would genuinely act: either wire `node scripts/audit/install-scope-check.js` into ci.yml (note --update snapshot is operator-manual per lines 184-186, so no fixture file is needed — the TRACKED counts are inlined in the source, lowering the cost of wiring it in) or delete it. Medium is correct: not a runtime defect, but real dead weight that gives false assurance of scope protection.
- **Reconciliation:** Independently useful, orphaned only because it was filed under PF1. Mechanically self-contained ($0, no API key, no operator fill-in) and guards a real regression class — migration/install write-ops leaking outside Convoke-owned scope into BMAD-core dirs (`install-scope-check.js:4-18`). **Action: promote `node scripts/audit/install-scope-check.js` (+ committed snapshot) into `npm test`/CI, independent of PF1's fate** — fixing the finding #31 comment/string false-positive as part of the salvage, not as a reason to drop it.

### 🟡 Low severity (24)

#### 22. exportSkill JSDoc/contract is stale — claims throws that never happen

- **Status:** 🔴 Live — untracked
- **Type:** conception  •  **Location:** [scripts/portability/export-engine.js:1006](scripts/portability/export-engine.js#L1006)  •  **Reviewer:** `portability`
- **Evidence:** JSDoc lines 1006-1010: 'Throws: - if the skill's tier is pipeline (only standalone + light-deps are exportable) - if persona resolution fails (all 5 strategies)'. But line 1014 comment says 'all tiers exportable; pipeline skills get a framework-only notice' and line 1082 emits a notice instead of throwing; synthesizePersonaFromWorkflow (Strategy 5, line 328) is documented 'Always returns a valid persona — never throws'.
- **Impact:** The documented contract contradicts the implementation. Callers (e.g. convoke-export.js runSingle) code defensively against throws that cannot occur, creating dead branches (see related finding). A wrong contract is a maintenance hazard and a single-source-of-truth violation between doc and behavior.
- **Suggested fix:** Update the JSDoc to reflect actual behavior: throws only when the skill is not in the manifest or SKILL.md is missing; pipeline tiers are exported with a framework-only banner; persona resolution always succeeds via Strategy 5 fallback.
- **Verifier note:** Verified against the real code in scripts/portability/export-engine.js. The JSDoc at lines 1006-1010 documents two throw conditions that cannot occur: (1) "throws if tier is 'pipeline'" is contradicted by line 1014's own comment ("all tiers exportable; pipeline skills get a framework-only notice") and lines 1082-1085, which push a framework-only banner instead of throwing; (2) "throws if persona resolution fails (all 5 strategies)" is contradicted by loadPersona (line 243), whose Strategy 5 fallback synthesizePersonaFromWorkflow (line 330) is explicitly documented at line 328 as "Always returns a valid persona — never throws," and which loadPersona unconditionally returns at line 267 when no agent matches. The only real throws are in loadSkillRow (line 88, skill not in manifest) and loadSkillSource (line 110, SKILL.md missing) — exactly what the suggested fix prescribes. The inline comment at line 1020 repeats the same stale persona-throw claim, compounding the drift. This is a genuine doc/behavior single-source-of-truth violation a maintainer would correct, but it is a documentation-accuracy/maintainability issue with no runtime impact, so I corrected severity from medium to low.
- **Reconciliation:** Swept against the lifecycle backlog and the two non-main branches (`deprecation`, `test/governance-migration-v2`): no coverage found. Genuinely live and unowned — a real defect or accumulated debt, actionable now.

#### 23. Module-level README template cache keyed by nothing; ignores projectRoot

- **Status:** 🔴 Live — untracked
- **Type:** conception  •  **Location:** [scripts/portability/convoke-export.js:219](scripts/portability/convoke-export.js#L219)  •  **Reviewer:** `portability`
- **Evidence:** let _templateCache = null; function loadReadmeTemplate(projectRoot) { if (_templateCache !== null) return _templateCache; ... _templateCache = fs.readFileSync(tplPath, 'utf8'); } — the cache stores the first template read and returns it for ALL subsequent calls regardless of the projectRoot argument.
- **Impact:** If loadReadmeTemplate/buildReadme is ever invoked against more than one projectRoot in a single process (test harnesses using a tmpDir, or a future multi-root tool), the second call silently returns the first root's template. The parameter that should key the cache is ignored — a latent correctness hazard and a leaky abstraction (mutable module-global state in an otherwise pure-ish builder).
- **Suggested fix:** Key the cache by resolved template path (a Map<path,string>), or drop the cache entirely (reading one small file per export is negligible).
- **Verifier note:** The code quote is accurate. At scripts/portability/convoke-export.js:219-231, `let _templateCache = null` and `loadReadmeTemplate(projectRoot)` short-circuit on the cached value before ever using the `projectRoot` argument, so a second call with a different root would silently return the first root's template. The parameter genuinely does not key the cache — that is a real leaky abstraction. However, severity is correctly low and the danger is purely latent: this is a CLI entry file (`require.main === module`, main() resolves exactly one `findProjectRoot()` at line 487 per process), so today only one root exists per invocation. `buildReadme` is exported (line 525), but every consuming test (portability-*.test.js) drives the CLI via spawnSync subprocesses — each gets a fresh process with `_templateCache = null` — and none calls `buildReadme` in-process against two different roots. So no current code path triggers the bug. A maintainer could reasonably act on the suggested fix (Map keyed by resolved path, or just drop the cache since it reads one tiny file) as cheap hygiene, but it is not a correctness bug under any present caller — a real-but-minor latent-design nit, not an active defect.
- **Reconciliation:** Swept against the lifecycle backlog and the two non-main branches (`deprecation`, `test/governance-migration-v2`): no coverage found. Genuinely live and unowned — a real defect or accumulated debt, actionable now.

#### 24. docs-audit hardcodes the seven user-guide paths instead of deriving them from the agent registry's USER_GUIDES

- **Status:** 🔴 Live — untracked
- **Type:** conception  •  **Location:** [scripts/docs-audit.js:27](scripts/docs-audit.js#L27)  •  **Reviewer:** `entrypoints`
- **Evidence:** USER_FACING_DOCS lists `_bmad/bme/_vortex/guides/EMMA-USER-GUIDE.md` ... `MAX-USER-GUIDE.md` as seven literal strings. agent-registry.js already exports `USER_GUIDES = AGENTS.map(a => `${a.name.toUpperCase()}-USER-GUIDE.md`)` (verified at runtime: ['EMMA-USER-GUIDE.md',...,'MAX-USER-GUIDE.md']). docs-audit.js imports AGENTS/WORKFLOWS/GYRE_AGENTS from that same registry (line 9-12) but NOT USER_GUIDES, then re-spells the same seven names by hand.
- **Impact:** Multiple sources of truth for the agent roster. If an 8th Vortex agent is added (the registry's documented one-line extension point), AGENTS-derived checks update automatically but the guide-audit list silently stays at 7 — the new agent's guide never gets link/coverage-audited, defeating the audit's purpose. The list is already subtly incomplete (VORTEX-TEAM-GUIDE.md on disk is unaudited).
- **Suggested fix:** Import USER_GUIDES from agent-registry and build the guide paths as `USER_GUIDES.map(g => `_bmad/bme/_vortex/guides/${g}`)`, concatenated into USER_FACING_DOCS. The non-guide docs (README, docs/*) stay as literals.
- **Verifier note:** Verified against the real code. docs-audit.js lines 27-33 hardcode the seven user-guide paths (EMMA...MAX) as literal strings, and line 9-12 imports AGENTS/WORKFLOWS/WORKFLOW_NAMES/GYRE_AGENTS/GYRE_WORKFLOWS from agent-registry.js but does NOT import USER_GUIDES. The registry at line 141 defines `USER_GUIDES = AGENTS.map(a => `${a.name.toUpperCase()}-USER-GUIDE.md`)` and exports it (line 283), producing exactly those seven names. The registry header explicitly documents the single-entry extension point ("To add a new agent: push one entry to AGENTS"), and the file itself already derives AGENT_FILES, VORTEX_SKILL_PATHS, AGENT_IDS, and WORKFLOW_NAMES from AGENTS — so hand-spelling the guide names breaks the file's own single-source-of-truth pattern and would silently leave a hypothetical 8th agent's guide unaudited. This is genuine latent maintenance debt a maintainer would act on, and the suggested fix (import USER_GUIDES, map to paths) is correct and consistent with existing derivations. Severity is correctly low: the list is presently accurate for 7 agents, so there is no current bug — only drift risk on future expansion. Minor caveat: the finding's secondary claim that VORTEX-TEAM-GUIDE.md (which does exist on disk, unaudited) supports the fix is misleading, since the proposed USER_GUIDES-derived fix would not cover that team guide either (it is not agent-name-derived); this does not undermine the core finding.
- **Reconciliation:** Swept against the lifecycle backlog and the two non-main branches (`deprecation`, `test/governance-migration-v2`): no coverage found. Genuinely live and unowned — a real defect or accumulated debt, actionable now.

#### 25. runMigrations has no projectRoot seam, forcing the orchestration test to process.chdir into the temp dir

- **Status:** 🔴 Live — untracked
- **Type:** conception  •  **Location:** [tests/unit/migration-runner-orchestration.test.js:28](tests/unit/migration-runner-orchestration.test.js#L28)  •  **Reviewer:** `test-architecture`
- **Evidence:** The suite must `process.chdir(tmpDir)` (line 28; cwd captured at line 18, restored in `after` at line 22) before every `runMigrations('1.4.1')` call because runMigrations takes only a fromVersion string. scripts/update/lib/migration-runner.js:31 resolves the target via `const projectRoot = findProjectRoot();` (cwd-walking) rather than accepting a projectRoot parameter — unlike its own collaborators backupManager.createBackup(fromVersion, projectRoot), executeMigration(migration, projectRoot), refreshInstallation(projectRoot), and validator.validateInstallation(backupMetadata, projectRoot), which all take projectRoot explicitly.
- **Impact:** Missing test seam: the only way to point runMigrations at an isolated fixture is global process.cwd mutation, which is shared mutable state. The test cannot run in parallel with anything else in-process and depends on after-hook restoration for isolation; a thrown assertion before restore would leak cwd to later files. It is also an inconsistent abstraction — every downstream function in the same module is projectRoot-parameterized except its entry orchestrator.
- **Suggested fix:** Add an optional projectRoot parameter to runMigrations(fromVersion, { projectRoot, verbose }) defaulting to findProjectRoot() for the CLI path, and pass it through. Then change the test to `runMigrations('1.4.1', { projectRoot: tmpDir })` and delete all process.chdir/originalCwd plumbing.
- **Verifier note:** Verified against the real code. tests/unit/migration-runner-orchestration.test.js does call process.chdir(tmpDir) before every runMigrations invocation (line 28 cited exactly, plus lines 86,119,150,171,206,251,299,336,367,401), capturing originalCwd in before and restoring it in after, purely to point the function at an isolated fixture. scripts/update/lib/migration-runner.js:26 confirms runMigrations(fromVersion, options={dryRun,verbose}) has no projectRoot parameter, and line 31 resolves the target via findProjectRoot() (cwd-walking). The inconsistency claim is also true: backupManager.createBackup (line 86), executeMigration (line 97), refreshInstallation (line 116), and validator.validateInstallation (line 127) all receive projectRoot explicitly — only the entry orchestrator does not. The sibling runRefreshOnly (line 388) shares the same shape, and the only production caller (convoke-update.js:398,427) passes just version+options, so the suggested optional-projectRoot fix is backward compatible. This is a genuine, low-risk test-seam improvement a maintainer would plausibly accept. I lowered severity to low: the chdir pattern is the dominant cwd-walking convention across this whole test file and the rest of the update test suite (not an isolated smell), node:test's after hook runs even when an assertion throws (so the 'leak to later files on thrown assertion' concern is overstated — the harness restores cwd regardless), and node:test runs files in separate processes by default so the in-process parallel-safety concern is largely theoretical here. The value is real but modest: consistency of abstraction and cleaner isolation, not a correctness or reliability defect.
- **Reconciliation:** Swept against the lifecycle backlog and the two non-main branches (`deprecation`, `test/governance-migration-v2`): no coverage found. Genuinely live and unowned — a real defect or accumulated debt, actionable now.

#### 26. Shared kernel utilities misplaced inside the update/ subsystem — every subsystem reaches up into update/lib/utils.js

- **Status:** 🔴 Live — untracked
- **Type:** conception  •  **Location:** [scripts/update/lib/utils.js:76](scripts/update/lib/utils.js#L76)  •  **Reviewer:** `cross-cutting-conception`
- **Evidence:** utils.js (under scripts/update/lib/) defines the codebase-wide primitives findProjectRoot(), getPackageVersion(), compareVersions(), assertVersion(), countUserDataFiles(). Its consumers span EVERY subsystem: audit/ (audit-bmm-dependencies.js:9, audit-skill-dirs.js:37, reference-integrity.js:63, validate-marketplace.js:36, drift-snapshot.js:34, install-scope-check.js:46, pf1-*.js), portability/ (catalog-generator.js:22, convoke-export.js:28, classify-skills.js:24, validate-classification.js:33, seed-catalog-repo.js:21), portfolio (scripts/lib/portfolio/portfolio-engine.js:19), and 8 top-level scripts (archive.js:5, convoke-doctor.js:7, migrate-artifacts.js:15, docs-audit.js:8, etc.). 30+ importers, most having nothing to do with the update workflow.
- **Impact:** These are foundational, subsystem-agnostic utilities, but they are scoped as if private to the update/ feature. The module's own docstring even calls itself 'Shared utilities for the Convoke update system' while being the de-facto shared kernel for the whole package. The result is a leaky abstraction with the wrong home: low-level shared code lives inside a high-level feature folder, so the dependency graph fans out from a feature subsystem rather than a neutral base layer.
- **Suggested fix:** Promote the cross-cutting primitives (findProjectRoot, getPackageVersion, compareVersions, assertVersion) to a neutral base module such as scripts/lib/runtime.js (or scripts/lib/utils.js), leave update-specific helpers (countUserDataFiles) in update/lib, and re-point all importers. This makes update/ a consumer of the base layer like every other subsystem.
- **Verifier note:** The finding's facts check out against the real code. scripts/update/lib/utils.js does define the cross-cutting primitives findProjectRoot(), getPackageVersion(), compareVersions(), assertVersion(), and countUserDataFiles(), and its own docstring (line 7) calls it "Shared utilities for the Convoke update system." I confirmed 25 importers via grep: 9 in top-level scripts/ (archive.js, convoke-doctor.js, migrate-artifacts.js, docs-audit.js, etc.), 10 in scripts/audit/, 5 in scripts/portability/, and 1 in scripts/lib/portfolio/portfolio-engine.js — so roughly 16 of 25 consumers have nothing to do with the update workflow, all reaching up into a feature subfolder for foundational utilities. A neutral base layer already exists at scripts/lib/ (artifact-utils.js, frontmatter.js, types.js), so the suggested promotion to scripts/lib/ is natural and consistent with existing structure. The architectural critique (low-level shared code with the wrong home, dependency graph fanning out from a feature folder) is legitimate and not a false positive. However, severity is overstated: this is pure code organization with zero behavioral, correctness, security, or coupling-breakage impact — a move-and-re-point refactor. Crucially, project-context.md line 31 explicitly canonizes "scripts/update/lib/utils.js" as the documented single-source-of-truth home for getPackageVersion(), so any fix must also update that invariant text and the no-hardcoded-version policy, raising the churn-vs-benefit ratio. A maintainer might act on it during a cleanup pass, but it is a low-priority cosmetic/conception nit, not high severity.
- **Reconciliation:** Swept against the lifecycle backlog and the two non-main branches (`deprecation`, `test/governance-migration-v2`): no coverage found. Genuinely live and unowned — a real defect or accumulated debt, actionable now.

#### 27. Package self-location depth duplicated as fragile ../../../ chains with no getPackageRoot() helper

- **Status:** 🔴 Live — untracked
- **Type:** conception  •  **Location:** [scripts/update/lib/refresh-installation.js:34](scripts/update/lib/refresh-installation.js#L34)  •  **Reviewer:** `cross-cutting-conception`
- **Evidence:** Each script independently encodes its distance to the package root: refresh-installation.js:34 `path.join(__dirname, '..', '..', '..')`, changelog-reader.js:7 `path.join(__dirname, '..', '..', '..', 'CHANGELOG.md')`, utils.js:17 getPackageVersion `require('../../../package.json')` — three separate hardcodings of the same 3-level depth. install-vortex-agents.js:59 uses `__dirname, '..'` and generate-badges-json.js:21 uses `__dirname, '..'` (different depth because different nesting). grep confirms there is NO getPackageRoot() helper anywhere.
- **Impact:** The number of `..` segments silently depends on each file's directory depth, so moving any of these files between folders breaks its package-root resolution with no compile-time signal. getPackageVersion already needs exactly this resolution and re-derives it; the lack of a shared helper means the package's own install root is computed four+ different ways.
- **Suggested fix:** Expose getPackageRoot() once in the base layer (e.g. anchored via require.resolve('<pkg>/package.json') or a single __dirname computation in one known-depth file) and have getPackageVersion, refresh-installation, and changelog-reader consume it instead of re-counting `..`.
- **Verifier note:** Every fact in the finding holds up against the real code. I confirmed refresh-installation.js:34 (`path.join(__dirname, '..', '..', '..')`), utils.js:17 (`require('../../../package.json')`), and changelog-reader.js:7 (`path.join(__dirname, '..', '..', '..', 'CHANGELOG.md')`) — three independent re-encodings of the same 3-level depth, all in scripts/update/lib/. The shallower install-vortex-agents.js:59 and generate-badges-json.js:21 use `__dirname, '..'` correctly because they sit one level up in scripts/. A grep confirms there is NO getPackageRoot() helper; the only centralized root resolver is findProjectRoot() in utils.js, which resolves the *project* root, not the package root — so the project already demonstrates the convention of centralizing root logic, making the absence of a package-root equivalent a legitimate DRY/conception gap a maintainer could act on. I down-rate severity to low rather than medium: the chains are functionally correct today, live only in build/update tooling (not runtime-critical paths), the duplication count is modest (3 semantic sites at one depth), and there is no active bug — the risk is purely latent maintenance fragility that only manifests if a file is relocated. A getPackageRoot() helper anchored once (e.g. via require.resolve) is a reasonable but non-urgent cleanup.
- **Reconciliation:** Swept against the lifecycle backlog and the two non-main branches (`deprecation`, `test/governance-migration-v2`): no coverage found. Genuinely live and unowned — a real defect or accumulated debt, actionable now.

#### 28. Migration preview() emits a hardcoded count of 18 SKILL.md targets instead of deriving it

- **Status:** 🔴 Live — untracked
- **Type:** execution  •  **Location:** [scripts/update/migrations/3.3.x-to-4.0.0.js:84](scripts/update/migrations/3.3.x-to-4.0.0.js#L84)  •  **Reviewer:** `update-engine`
- **Evidence:** When preview() is called with no projectRoot (the migration-runner.previewMigrations path always calls `migration.module.preview()` with zero args, runner line 226), it returns _previewActionsWithCount('up to 18'); the literal 18 also appears in the registry description and the apply() doc. The project-aware path (preview(projectRoot)) correctly derives canonicalCount from the inventory CSV, proving the count is knowable at runtime.
- **Impact:** Violates derive-counts-from-source. The runner's dry-run (`convoke-update --dry-run`) always hits the zero-arg branch and shows the frozen '18', which drifts the moment the v6.3-migration-inventory.csv canonical row count changes (the very file the real apply() iterates). The dry-run preview can therefore misreport the work the real run will do.
- **Suggested fix:** Have migration-runner.previewMigrations pass projectRoot into preview() (it already resolves projectRoot in runMigrations), and make the no-projectRoot branch report 'N canonical inventory targets' computed from the inventory CSV rather than the literal 18. Remove the '18' literal from the static fallback.
- **Verifier note:** All cited facts verify against the real code. In scripts/update/migrations/3.3.x-to-4.0.0.js, the zero-arg preview() branch (line 84) returns _previewActionsWithCount('up to 18') with a hardcoded literal, and the project-aware branch (lines 74-78) proves the count IS derivable at runtime from v6.3-migration-inventory.csv (rows.filter candidate_status === 'canonical'). In migration-runner.js the previewMigrations dry-run path calls migration.module.preview() with zero args (line 226), so `convoke-update --dry-run` always hits the static branch. The literal 18 is also duplicated in the registry description (line 59) and the apply() doc comment (line 11). I confirmed the on-disk inventory currently has exactly 18 canonical rows, so the value is presently correct. This is a genuine derive-counts-from-source nit and the suggested fix (thread projectRoot through previewMigrations) is cheap and reasonable. However impact is minimal and severity is correctly low: it affects ONLY dry-run preview DISPLAY text — the actual apply() always derives the count from the CSV (line 220), so the migration never misbehaves or misreports the real work; and the static value is deliberately hedged as 'up to 18' (an explicit upper-bound estimate documented as intentional in the lines 81-83 comment), so even on drift it reads as an estimate, not a false precise claim. A maintainer could justifiably act on it as polish but could equally defer it as a documented-estimate cosmetic item.
- **Reconciliation:** Swept against the lifecycle backlog and the two non-main branches (`deprecation`, `test/governance-migration-v2`): no coverage found. Genuinely live and unowned — a real defect or accumulated debt, actionable now.

#### 29. guessVersionFromFileStructure returns minor-pinned guesses that can mis-route the migration chain

- **Status:** 🔴 Live — untracked
- **Type:** execution  •  **Location:** [scripts/update/lib/version-detector.js:55](scripts/update/lib/version-detector.js#L55)  •  **Reviewer:** `update-engine`
- **Evidence:** When config.yaml has no version field, getCurrentVersion falls back to guessVersionFromFileStructure, which returns the hardcoded strings '1.1.0' or '1.0.0' based on directory presence. registry.matchesVersionRange then keys off major.minor: '1.1.0' → matches '1.1.x' entry, '1.0.0' → '1.0.x'. A modern install (e.g. 3.x) that lost its version field but still has workflows/_deprecated would be guessed as '1.1.0' and run the entire 1.1.x→…→4.0.0 chain.
- **Impact:** The fallback can only ever return two ancient versions, so any version-field-less install newer than 1.1 is silently misclassified as 1.1.0 and dragged through a long, wrong migration chain (including breaking 1.7→2.0 rename logic) instead of being flagged for reinstall. The structural signals (presence of _deprecated) are not version-discriminating beyond 1.1.
- **Suggested fix:** Treat a config.yaml that exists but lacks a version field as 'partial/corrupted' (route to convoke-install) rather than guessing an ancient version, OR strengthen the structural probe to detect the actual installed generation (skill-dir agents ⇒ >=4.0, _gyre present ⇒ >=3.x). Do not feed a fabricated 1.x version into getMigrationsFor.
- **Verifier note:** Verified against the real code. In scripts/update/lib/version-detector.js, getCurrentVersion (lines 36-47) falls back to guessVersionFromFileStructure when config.yaml exists but has no version field. That function (lines 69-82) returns '1.1.0' if workflows/_deprecated exists, '1.0.0' if workflows/empathy-map exists, else null — so it can only ever produce two ancient versions. The finding's claimed trigger path is real and reachable: in scripts/update/convoke-update.js assessUpdate (lines 34-60), a missing version field does NOT make detectInstallationScenario return 'partial' (that requires config.yaml to be absent, lines 164-167) — a version-less but otherwise intact install with all 7 agents + workflows returns 'complete'. The code then calls registry.getMigrationsFor(currentVersion), whose matchesVersionRange keys off major.minor (registry.js lines 240-254), so '1.1.0' matches the '1.1.x-to-1.3.0' entry and chains forward through the breaking '1.7.x-to-2.0.0' rename migration all the way to 4.0.0. I confirmed the misclassification is concrete on this very repo: the live install at _bmad/bme/_vortex/workflows/ contains BOTH _deprecated AND empathy-map, meaning a modern 3.x/4.x install that lost its version field would be guessed as '1.1.0' and dragged through the entire wrong chain, exactly as described. The structural probe is genuinely non-discriminating beyond 1.1 and the no-version action (convoke-update.js:46) is dead-code-shadowed whenever the guess returns non-null. I rate it low (the trigger requires a corrupted/version-stripped config.yaml, an edge case rather than a normal-operation path), though the destructive potential of running breaking migrations on a modern install leans toward medium; the suggested fix (route a version-field-less config to reinstall, or strengthen the probe to detect skill-dir/_gyre generation) is sound and a maintainer would reasonably act on it.
- **Reconciliation:** Swept against the lifecycle backlog and the two non-main branches (`deprecation`, `test/governance-migration-v2`): no coverage found. Genuinely live and unowned — a real defect or accumulated debt, actionable now.

#### 30. Destructive rmSync on user-supplied output path lacks resolve+contains-check

- **Status:** 🔴 Live — untracked
- **Type:** execution  •  **Location:** [scripts/portability/seed-catalog-repo.js:405](scripts/portability/seed-catalog-repo.js#L405)  •  **Reviewer:** `portability`
- **Evidence:** outputDir resolved from --output via path.resolve(process.cwd(), outputDir) (line 352) with no normalize+contains-check against projectRoot; fs.rmSync(outputDir, { recursive: true, force: true }) at lines 381, 394, 405 on generation/verification failure.
- **Impact:** path-safety-for-destructive-ops requires user paths used for delete to be resolved, normalized, and contains-checked against project root, refusing outside paths. Here the recursive rm is only mitigated by the non-empty guard (line 364) and the dirCreatedByUs flag, not by a containment check — the invariant's required guard is absent. The guards bound impact to script-created empty trees, so severity is low, but the explicit safety check the project mandates is missing.
- **Suggested fix:** Before any rmSync, resolve+normalize outputDir and assert it is inside an allowed root (project root or an explicit staging base); refuse and skip cleanup for outside paths. Keep the existing non-empty and dirCreatedByUs guards as defense-in-depth.
- **Verifier note:** Verified against the real code and the project's own documented invariant. In /Users/amalikamriou/BMAD-Enhanced/scripts/portability/seed-catalog-repo.js, outputDir is resolved with path.resolve(process.cwd(), outputDir) at line 352 but is never normalized or contains-checked against projectRoot — projectRoot is computed at line 356 only to pass into generate(), and is never used to validate outputDir. fs.rmSync(outputDir, {recursive:true, force:true}) fires at lines 381, 394, and 405 on failure paths. The project explicitly mandates the missing guard: project-context.md §path-safety-for-destructive-ops (lines 55-58) requires "A resolve + normalize + contains-check against the project root" plus "An explicit refusal path for /, $HOME, and any path that isn't under the project root." Neither the normalize, the contains-check, nor the explicit refusal exists in this script, so the invariant is genuinely violated — not a documented exception. Severity is correctly low because the non-empty pre-existing-directory guard (lines 364-370) and the dirCreatedByUs flag (line 371) bound the destructive rm to empty trees the script itself created: a user passing a populated path like ~/Documents would be refused before any rm, and rmSync is gated on dirCreatedByUs. So real-world blast radius is small, but the specific safety check the project requires for exactly this class of script is absent and a maintainer would reasonably add it. (Minor aside: line 405's rm is gated only on dirCreatedByUs, not also on fs.existsSync like 381/394, though force:true makes that harmless.)
- **Reconciliation:** Swept against the lifecycle backlog and the two non-main branches (`deprecation`, `test/governance-migration-v2`): no coverage found. Genuinely live and unowned — a real defect or accumulated debt, actionable now.

#### 31. install-scope-check write-op matcher counts matches inside comments and strings; snapshot bakes in a known false positive

- **Status:** 🟡 Salvage — promote to CI
- **Type:** execution  •  **Location:** [scripts/audit/install-scope-check.js:60](scripts/audit/install-scope-check.js#L60)  •  **Reviewer:** `audit-scripts`
- **Evidence:** { file: 'scripts/update/migrations/3.3.x-to-4.0.0.js', expected: 5 }, // comment at line 56-58 admits: '5 matches (4 real writes + 1 comment false-positive at line 394)'. Verified: line 394 of that migration is a JSDoc line ' * (state file) pass through this helper before `fs.writeFileSync` is called.' counted as a write op by WRITE_OP_RE (line 83).
- **Impact:** countWriteOps() runs WRITE_OP_RE over raw line text with no comment/string stripping (catch-all-phase-review: a matcher with no false-positive guard). The control's whole purpose is to detect when a real fs write is added/removed. Because a comment mention counts as +1, the snapshot expected:5 is '4 real + 1 comment'. If a dev removes that comment AND adds a real write in the same change, the count stays 5 and the scope regression passes silently — the exact failure mode the control exists to catch. The expected counts are also a hand-maintained snapshot of source state, brittle to any unrelated edit (e.g. adding a write-op mention to a doc-comment) and updated manually.
- **Suggested fix:** Strip line/block comments and string literals before matching (or only count matches whose line is not comment-only), so the count reflects real call sites. Then re-baseline expected:5 -> 4. Optionally assert against AST/`require('acorn')` tokenization rather than a line regex to make the control robust.
- **Verifier note:** Verified against the real code. scripts/audit/install-scope-check.js line 83 defines WRITE_OP_RE and countWriteOps() (lines 93-105) applies it over raw line text with zero comment/string stripping. The migration file scripts/update/migrations/3.3.x-to-4.0.0.js has exactly 5 matches (confirmed by grep: lines 309, 354, 394, 681, 682), and line 394 is a JSDoc line ' * (state file) pass through this helper before `fs.writeFileSync` is called.' — a comment, not a call site. The script's own snapshot comment (lines 56-58) explicitly admits 'AC1 5 matches (4 real writes + 1 comment false-positive at line 394)', and TRACKED sets expected:5 (line 62), baking in the false positive. The finding's mechanism is real: a comment mention inflates the count, so removing that comment while adding one real write keeps the total at 5 and the count-snapshot check passes silently — the exact masking the control is meant to prevent. I downgrade severity to low (from medium) because: (1) the count-snapshot only drives the soft 'operator review' exit code 1, while the actually scope-relevant detection — writes to BMAD-owned paths — is handled by the independent scanForbiddenPatterns() (exit code 2), which is unaffected by comment counting; (2) the masking requires a specific same-change coincidence (delete that exact JSDoc + add exactly one write); and (3) the script self-documents the false positive so a reviewing operator has context. The suggested fix (strip comments/strings or tokenize, then re-baseline expected:5 -> 4) is sound and worth doing for robustness, but this is a maintainability/robustness defect rather than an active correctness or security hole.
- **Reconciliation:** Independently useful, orphaned only because it was filed under PF1. Mechanically self-contained ($0, no API key, no operator fill-in) and guards a real regression class — migration/install write-ops leaking outside Convoke-owned scope into BMAD-core dirs (`install-scope-check.js:4-18`). **Action: promote `node scripts/audit/install-scope-check.js` (+ committed snapshot) into `npm test`/CI, independent of PF1's fate** — fixing the finding #31 comment/string false-positive as part of the salvage, not as a reason to drop it.

#### 32. parseRecording hardcodes the prompt count (4) instead of deriving from PF1_PROMPTS

- **Status:** 🟣 Blocked — keep (FINISH or waive)
- **Type:** execution  •  **Location:** [scripts/audit/pf1-validation-battery.js:96](scripts/audit/pf1-validation-battery.js#L96)  •  **Reviewer:** `audit-scripts`
- **Evidence:** if (headers.length !== 4) { throw ...expected exactly 4 [Prompt 1..Prompt 4] headers... } const expectedNums = ['1', '2', '3', '4']; // while the module already defines the source-of-truth array: const PF1_PROMPTS = ['Prompt 1','Prompt 2','Prompt 3','Prompt 4'] (line 62), used everywhere else (writeResults, main, dry-run).
- **Impact:** derive-counts-from-source: PF1_PROMPTS is the declared source-of-truth array for the prompt set, but parseRecording independently hardcodes the literal 4 and the ['1','2','3','4'] sequence. If the prompt battery is ever expanded/reduced (the file header and drift-snapshot already contemplate 3- and 5-skill variants), PF1_PROMPTS and the parser will silently disagree: callers will iterate N prompts while parseRecording still demands exactly 4, throwing exitCode 5 on otherwise-valid recordings. drift-snapshot.js (PROMPT_LABELS, PROMPTS_PER_SKILL) has the same split-source shape.
- **Suggested fix:** Derive from the array: `const expectedNums = PF1_PROMPTS.map((_, i) => String(i + 1));` and check `headers.length !== PF1_PROMPTS.length`. Update the thrown message to interpolate PF1_PROMPTS.length so count and message can't drift apart.
- **Verifier note:** Verified directly against the code. At pf1-validation-battery.js:96 parseRecording hardcodes `if (headers.length !== 4)` and at :106 hardcodes `const expectedNums = ['1', '2', '3', '4']`, with the count `4` also baked into the two thrown error messages (:99, :111). Meanwhile PF1_PROMPTS (line 62) is the explicitly-documented source-of-truth array ("the load-bearing array", per the comment at :65) and is used everywhere else for iteration: writeResults (:282, :298), main (:343, :365, :376), and it is exported from the module (:431). So the parser independently restates the prompt count instead of deriving from PF1_PROMPTS, a genuine split-source/DRY coupling: if the battery were ever resized, callers would iterate PF1_PROMPTS.length prompts while parseRecording still demanded exactly 4 and exited with code 5. The suggested fix (`headers.length !== PF1_PROMPTS.length` plus `PF1_PROMPTS.map((_, i) => String(i + 1))` and interpolating the length into the messages) is correct and self-contained. One minor inaccuracy in the finding: the file header/comments contemplate variable AGENT counts (3 Vortex + 1 Gyre control), not variable prompt counts — the 4-prompt set is a deliberate fixed "Decision 4" design (lines 60-71), which is why the practical risk is latent and the severity is correctly low rather than higher. The author was already aware of the labels-as-keys coupling (comment at :59) but did not extend that discipline to the count, making this a reasonable, actionable consistency cleanup.
- **Reconciliation:** **Intent reconstruction overturned the "archive" lean; a coverage-check (2026-06-28) resolved the open question.** PF1 is the **v4.0 behavioral-equivalence release gate (FR36-40, LLM-judge, drift T=4.0, FAIL blocks *release*)**. The earlier open question — "can I97's per-agent personality scoring (FR21-23) satisfy it?" — **resolves to NO**: FR36-40 and FR21-23 are from *different PRDs* measuring *different axes* (PF1 = LLM-judged structural+capability+persona drift gating release; I97 = operator-ranked persona-only gating *merge*), and I97 covers only **2 of PF1's 4 target agents** (Emma, Wade — not Liam, not the Gyre control). **PF1's gate evidence is genuinely unobtained.** `pf1-record-agent.js` was committed 2026-05-31 (after the snapshot called it "not yet created"), so it is mid-resumption. **Action: FINISH the battery (or formally waive/re-scope the gate at the v4.0 release decision) — do NOT archive.** The per-finding fix here still applies when work resumes.

#### 33. archive.js aborts mid-migration and skips INDEX update when two loose root files collide to the same lowercased archive name

- **Status:** 🔴 Live — untracked
- **Type:** execution  •  **Location:** [scripts/archive.js:246](scripts/archive.js#L246)  •  **Reviewer:** `entrypoints`
- **Evidence:** Root files are archived to `_archive/exploratory/${toLowerKebab(f)}` and moved with `fs.move(a.from, a.to, { overwrite: false })`. If two loose root files normalize to the same kebab name (e.g. `Notes.md` and `notes.md`, or `My File.md` and `my-file.md`), the first move succeeds and the second throws (fs-extra rejects an existing dest under overwrite:false). The throw propagates to the top-level `run().catch` (line 282) AFTER some files have already been moved, and crucially BEFORE `appendToIndex(indexPath, indexEntries)` (line 252) runs — so the moved files are physically relocated but have NO traceability row in INDEX.md, contradicting the file's 'append-only traceability log' contract.
- **Impact:** Partial, non-atomic mutation with a broken audit trail. The operator sees a crash, some files moved with no index record, and must reconstruct what happened by hand. The collision is reachable on case-insensitive filesystems (macOS default) or any two files differing only in case/separators.
- **Suggested fix:** Detect target-path collisions before executing (group actions by `a.to`, and either de-duplicate with a numeric suffix or fail the dry-run with a clear 'N files collide to X' message). Alternatively, append each entry to INDEX immediately after its successful move rather than batching after the loop, so a mid-loop failure still leaves an accurate (if partial) index.
- **Verifier note:** I read scripts/archive.js in full. The mechanics hold: root loose files are archived to `_archive/exploratory/${toLowerKebab(f)}` (lines 182-190) and moved one-by-one in a loop with `fs.move(a.from, a.to, { overwrite: false })` (line 246). I confirmed empirically that fs-extra@11.3.4's move with overwrite:false throws "dest already exists." on a pre-existing destination. Because `appendToIndex` is batched AFTER the entire loop (line 252) and a throw propagates to the top-level `run().catch` (line 282) which exits the process, a mid-loop collision leaves earlier files physically moved but writes NO INDEX.md rows at all — contradicting the documented "append-only traceability log" (line 44). So the partial-mutation-with-broken-audit-trail described is genuine. However, the finding overstates reachability in two ways: (1) `toLowerKebab` is literally just `filename.toLowerCase()` (artifact-utils.js:77), it does NOT normalize spaces/separators, so the `My File.md`/`my-file.md` example is false — only case-only collisions (e.g. `Notes.md` vs `notes.md`) can collide; (2) the "macOS default" claim is backwards: on case-insensitive APFS (macOS default) two files differing only by case cannot coexist in the same directory, so the collision is reachable only on case-sensitive filesystems (Linux, case-sensitive APFS), not the operator's likely default. This is an operator-run maintenance script (`npm run archive`), the failure is loud (visible crash), files are relocated not lost, and the trigger is rare — hence low severity. The suggested fix (pre-flight collision detection grouping by `a.to`, or incremental per-move index appends) is sound and actionable.
- **Reconciliation:** Swept against the lifecycle backlog and the two non-main branches (`deprecation`, `test/governance-migration-v2`): no coverage found. Genuinely live and unowned — a real defect or accumulated debt, actionable now.

#### 34. Dead findProjectRoot try/catch in portability CLIs assumes it throws, but it returns null

- **Status:** 🔴 Live — untracked
- **Type:** execution  •  **Location:** [scripts/portability/seed-catalog-repo.js:355](scripts/portability/seed-catalog-repo.js#L355)  •  **Reviewer:** `invariant-sweep`
- **Evidence:** seed-catalog-repo.js:354-358 `let projectRoot; try { projectRoot = findProjectRoot(); } catch (e) { ...return 2; }` and the identical block in catalog-generator.js:317-322. But utils.js:76-89 `findProjectRoot()` returns null when no _bmad ancestor is found — it never throws. So the catch is unreachable and a null projectRoot flows onward into generate(null,...) (seed-catalog:376) / generateCatalog(null) (catalog-generator:327), where path.join(null,'_bmad',...) throws a generic TypeError caught by the downstream try with an opaque message instead of the intended 'could not find project root'.
- **Impact:** Error-handling gap from a wrong assumption about the findProjectRoot() contract: the explicit, user-facing 'could not find project root' diagnostic is dead, and the not-found case surfaces as a confusing downstream TypeError instead. When run outside a Convoke install the operator gets a misleading 'Generation failed: Cannot read properties of null' / 'Error generating catalog' message.
- **Suggested fix:** Replace the try/catch with an explicit null check: `const projectRoot = findProjectRoot(); if (!projectRoot) { process.stderr.write('Error: could not find project root (no _bmad/ directory found).\n'); return 2; }` in both seed-catalog-repo.js and catalog-generator.js.
- **Verifier note:** Verified against the real code. In seed-catalog-repo.js:354-360 and catalog-generator.js:317-323 both CLIs wrap findProjectRoot() in try/catch expecting it to throw with a 'could not find project root' diagnostic. But utils.js:76-89 implements findProjectRoot() as a cwd-upward walk that returns null when no _bmad ancestor exists (line 88) — it never throws for the not-found case. So the catch is unreachable for its intended purpose, and null flows onward into generate(outputDir, null) (seed:376→86) and generateCatalog(null) (catalog:327→94), both of which immediately call path.join(null, '_bmad', ...). That throws a generic TypeError ('Path must be a string. Received null'), caught by the downstream try with the opaque 'Generation failed:' / 'Error generating catalog:' message instead of the intended user-facing diagnostic. The finding's quote, line numbers, and contract analysis all hold up exactly. Severity is correctly low: the failure still returns a non-zero exit code and the safety check for non-empty output dirs is unaffected; the only impact is a misleading error message in the edge case of running outside a Convoke install. The suggested explicit null-check fix is correct and minimal.
- **Reconciliation:** Swept against the lifecycle backlog and the two non-main branches (`deprecation`, `test/governance-migration-v2`): no coverage found. Genuinely live and unowned — a real defect or accumulated debt, actionable now.

#### 35. Unreachable tier-not-supported branch in single-skill export

- **Status:** 🔴 Live — untracked
- **Type:** muda  •  **Location:** [scripts/portability/convoke-export.js:319](scripts/portability/convoke-export.js#L319)  •  **Reviewer:** `portability`
- **Evidence:** if (msg.includes('not standalone') || msg.includes('tier "')) { return { ok: false, exitCode: EXIT_TIER_NOT_SUPPORTED, error: err }; }  — but export-engine.js throws no error containing 'not standalone' or 'tier "' (grep of export-engine.js error strings shows only 'is not in the manifest' and 'SKILL.md not found'). Pipeline skills now export with a notice rather than throwing.
- **Impact:** EXIT_TIER_NOT_SUPPORTED (exit 3) is unreachable in single-skill mode; the matcher checks for an error message the engine never produces. Dead code that also contradicts the help text (line 149 advertises exit 3 'Tier 3 / pipeline requested') for single-skill invocation — a single skill name of a pipeline tier actually succeeds.
- **Suggested fix:** Remove the dead branch, or if single-skill pipeline rejection is desired, look up the skill's tier from the manifest and return EXIT_TIER_NOT_SUPPORTED explicitly. Reconcile the help text's exit-code table with actual behavior.
- **Verifier note:** Verified against the real code. In runSingle (convoke-export.js:319-321) the branch returns EXIT_TIER_NOT_SUPPORTED only when err.message contains 'not standalone' or 'tier "'. The engine (export-engine.js) throws exactly two messages — 'is not in the manifest' (line 88) and 'SKILL.md not found' (line 110) — plus persona-resolution errors; a grep confirms neither 'not standalone' nor 'tier "' appears anywhere in the engine. Critically, exportSkill no longer rejects pipeline-tier skills: line 1014 comments 'all tiers exportable; pipeline skills get a framework-only notice', and lines 1082-1083 emit a notice instead of throwing. This is independently confirmed by tests/lib/portability-export-engine.test.js:124 ('bmad-dev-story (Tier 3 pipeline) exports with framework-only notice'). So the single-skill matcher at line 319 is genuinely dead code, and a single pipeline skill name now succeeds (exit 0) rather than exit 3. The engine docstring at lines 1006-1008 is also stale (still claims it throws for pipeline tier), reinforcing the inconsistency. Two corrections to the finding's framing: (1) EXIT_TIER_NOT_SUPPORTED itself is NOT globally unreachable — it is still produced via the --tier 3/pipeline batch path (validateTier, line 387), exercised by tests/lib/portability-cli-entry-point.test.js:110; the finding correctly scopes the dead branch to single-skill mode but the help text at line 149 ('Tier 3 / pipeline requested') remains accurate for the --tier invocation, so the 'contradicts help text' claim is weaker than stated. (2) This is harmless dead code with no correctness, security, or behavioral-bug impact — nothing misbehaves, an unreachable branch simply lingers — so medium overstates it; low is appropriate. A maintainer would reasonably delete the dead branch and fix the stale docstring, so the finding is real.
- **Reconciliation:** Swept against the lifecycle backlog and the two non-main branches (`deprecation`, `test/governance-migration-v2`): no coverage found. Genuinely live and unowned — a real defect or accumulated debt, actionable now.

#### 36. truncateDescription duplicated verbatim across two modules

- **Status:** 🔴 Live — untracked
- **Type:** muda  •  **Location:** [scripts/portability/generate-adapters.js:22](scripts/portability/generate-adapters.js#L22)  •  **Reviewer:** `portability`
- **Evidence:** generate-adapters.js:22 function truncateDescription(desc) {...} with comment line 20 'Same logic as catalog-generator.js's truncateDescription.' catalog-generator.js:67 defines the identical function.
- **Impact:** Two copies of the same truncation logic drift independently. The comment admits the duplication. Any change to truncation rules (e.g., the 120-char cap) must be made in two places.
- **Suggested fix:** Extract truncateDescription into a shared module (e.g. scripts/portability/format-utils.js or export-engine.js) and import it in both catalog-generator.js and generate-adapters.js.
- **Verifier note:** Verified directly against the code. /Users/amalikamriou/BMAD-Enhanced/scripts/portability/generate-adapters.js:22-30 and /Users/amalikamriou/BMAD-Enhanced/scripts/portability/catalog-generator.js:67-75 define a byte-for-byte identical truncateDescription(desc) function — same '. ' first-sentence split, same 120-char cap, same '...' suffix. The comment at generate-adapters.js:20 explicitly states 'Same logic as catalog-generator.js's truncateDescription', confirming the duplication is known. Both modules live in the same directory and both already depend on export-engine.js (catalog-generator.js imports resolvePersonaSummary/loadAgentManifest from it), so the suggested extraction into a shared module is genuinely actionable with low effort. This is real, semantic (not coincidental) duplication that would drift if the truncation rule changes. Severity is correctly low: the logic is trivial, stable, and the risk is maintenance drift rather than a defect; a maintainer would plausibly consolidate it during nearby cleanup but it is not urgent.
- **Reconciliation:** Swept against the lifecycle backlog and the two non-main branches (`deprecation`, `test/governance-migration-v2`): no coverage found. Genuinely live and unowned — a real defect or accumulated debt, actionable now.

#### 37. generate-adapters reimplements humanizeSkillName inline instead of importing it

- **Status:** 🔴 Live — untracked
- **Type:** muda  •  **Location:** [scripts/portability/generate-adapters.js:62](scripts/portability/generate-adapters.js#L62)  •  **Reviewer:** `portability`
- **Evidence:** Lines 62-69 build displayName via .replace(/^bmad-cis-agent-/,'').replace(/^bmad-agent-/,'').replace(/^bmad-cis-/,'').replace(/^bmad-/,'').split('-').map(s=>s.charAt(0).toUpperCase()+s.slice(1)).join(' ') — identical to export-engine.js:533 humanizeSkillName, which is already exported from export-engine.js.
- **Impact:** Duplicated prefix-stripping/title-casing logic. If the bmad- prefix set or casing changes (export-engine.js humanizeSkillName), the adapter copy silently diverges, producing inconsistent display names across catalog vs adapter files.
- **Suggested fix:** Import { humanizeSkillName } from './export-engine' and use it for the Copilot adapter displayName.
- **Verifier note:** Verified directly against the code. generate-adapters.js:62-69 builds the Copilot adapter displayName with a chain of four .replace() calls (/^bmad-cis-agent-/, /^bmad-agent-/, /^bmad-cis-/, /^bmad-/), then .split('-').map(title-case).join(' ') — this is character-for-character identical to humanizeSkillName in export-engine.js:533-542. humanizeSkillName IS exported (export-engine.js:1149, module.exports list), and both files reside in the same scripts/portability/ directory, so an import would be trivial. generate-adapters.js currently has no require of export-engine.js. The duplication is real and semantically intentional (same prefix set, same casing), so if the bmad- prefix set or casing rules change in humanizeSkillName, the adapter copy silently diverges, producing inconsistent display names. This is a genuine DRY/maintenance smell a maintainer could act on. The only caveat (not a refutation): the inline version takes (skillRow.name || skillName) as input, so the fix is humanizeSkillName(skillRow.name || skillName), not a whole-expression swap — a trivial detail the suggested_fix glosses over. Severity correctly low: it's a latent muda, no current incorrect behavior, and the same file already inlines another shared helper (truncateDescription, per its own comment at line ~18), making this a known stylistic pattern rather than a high-impact defect.
- **Reconciliation:** Swept against the lifecycle backlog and the two non-main branches (`deprecation`, `test/governance-migration-v2`): no coverage found. Genuinely live and unowned — a real defect or accumulated debt, actionable now.

#### 38. No-op .replace in extractWhatYouProduce Goal branch

- **Status:** 🔴 Live — untracked
- **Type:** muda  •  **Location:** [scripts/portability/export-engine.js:818](scripts/portability/export-engine.js#L818)  •  **Reviewer:** `portability`
- **Evidence:** goalMatch[1].toLowerCase().replace(/^[a-z]/, (c) => c) — the replacement callback returns the matched char unchanged, so the .replace is a guaranteed no-op.
- **Impact:** Dead transformation that signals lost intent (likely meant to capitalize or otherwise normalize the leading char). It misleads readers into thinking some casing fix happens.
- **Suggested fix:** Remove the no-op .replace(/^[a-z]/, (c) => c), or implement the intended transformation (e.g., (c)=>c.toUpperCase()) if leading-cap was the goal.
- **Verifier note:** I read scripts/portability/export-engine.js around line 818 directly. The code in the **Goal:** branch is `goalMatch[1].toLowerCase().replace(/^[a-z]/, (c) => c)`. Because `.toLowerCase()` runs first, the leading character is already lowercase, the regex `/^[a-z]/` matches it, and the callback `(c) => c` returns it unchanged — so the `.replace` is a guaranteed no-op with zero effect on the produced string. The quote in the finding is exact and the reasoning holds. It contrasts with the genuinely-functional `.replace` calls in adjacent branches (line 809's brace-stripping, and `humanName.replace(/\s+/g, '-')` on lines 818/824), which makes the no-op stand out as a likely vestige of an intended normalization. This is pure muda/dead transformation, not a correctness or behavioral bug, so "low" severity is correct: a maintainer would reasonably delete it (or implement the intended cap) during cleanup, but nothing breaks if left as-is. No documented exception or dynamic-reference concern applies — it is a self-contained inline expression. Real finding, low impact.
- **Reconciliation:** Swept against the lifecycle backlog and the two non-main branches (`deprecation`, `test/governance-migration-v2`): no coverage found. Genuinely live and unowned — a real defect or accumulated debt, actionable now.

#### 39. writeInventoryCsv is dead in production — CLI duplicates its logic instead of calling it

- **Status:** 🔴 Live — untracked
- **Type:** muda  •  **Location:** [scripts/audit/audit-bmad-init-refs.js:112](scripts/audit/audit-bmad-init-refs.js#L112)  •  **Reviewer:** `audit-scripts`
- **Evidence:** function writeInventoryCsv(entries, outputPath) { ... lines.push([...].join(',')) ... fs.writeFileSync(outputPath, lines.join('\n') + '\n', 'utf8'); } // but _runCli (line 255) does: `const generated = renderInventoryCsv(entries); ... fs.writeFileSync(outputAbs, generated, 'utf8');` — never calling writeInventoryCsv. grep confirms writeInventoryCsv is referenced only in tests/lib/audit-bmad-init-refs.test.js, never by production code.
- **Impact:** writeInventoryCsv and renderInventoryCsv contain the same 8-line serialization loop. Production uses renderInventoryCsv + a bare fs.writeFileSync; writeInventoryCsv exists only to be unit-tested. This is duplicated logic plus an export that production never exercises — the test verifies a code path no caller uses, and the two serializers can drift (one already carries a JSDoc note 'Mirrors writeInventoryCsv's formatting exactly', which is a maintenance hazard rather than a guarantee).
- **Suggested fix:** Have writeInventoryCsv delegate to renderInventoryCsv (`fs.writeFileSync(outputPath, renderInventoryCsv(entries), 'utf8')`) and have _runCli call writeInventoryCsv, so there is one serialization path that both production and tests exercise. Or drop writeInventoryCsv entirely if the project standard is render-then-write at the call site.
- **Verifier note:** Verified directly in /Users/amalikamriou/BMAD-Enhanced/scripts/audit/audit-bmad-init-refs.js. writeInventoryCsv (line 112) and renderInventoryCsv (line 141) contain identical 8-line serialization loops; renderInventoryCsv even carries the JSDoc "Mirrors writeInventoryCsv's formatting exactly" (line 135). _runCli (lines 230, 255) uses renderInventoryCsv + a bare fs.writeFileSync(outputAbs, generated) and never calls writeInventoryCsv. A grep across the repo confirms writeInventoryCsv is referenced only by tests/lib/audit-bmad-init-refs.test.js and the story doc — no production caller. The export is still public (line 270) but production never exercises it, so the unit test verifies an unused path, and the two serializers can silently drift. There is no dynamic-dispatch or registry indirection that would resurrect this (it's a plain named export with require.main-guarded CLI). This is genuine muda a maintainer would reasonably clean up (delegate writeInventoryCsv to renderInventoryCsv and route _runCli through it, or delete writeInventoryCsv). Severity is correctly low: no correctness or security impact, purely a duplication/drift maintenance hazard.
- **Reconciliation:** Swept against the lifecycle backlog and the two non-main branches (`deprecation`, `test/governance-migration-v2`): no coverage found. Genuinely live and unowned — a real defect or accumulated debt, actionable now.

#### 40. Duplicated kebab-boundary initiative scanner kept in two files that must stay in sync

- **Status:** 🔴 Live — untracked
- **Type:** muda  •  **Location:** [scripts/lib/portfolio/portfolio-engine.js:43](scripts/lib/portfolio/portfolio-engine.js#L43)  •  **Reviewer:** `lib-portfolio`
- **Evidence:** _scanCorpus() (portfolio-engine.js:43-60) is byte-for-byte equivalent in logic to _scanCorpusForInitiative() (artifact-utils.js:503-528): same length-desc candidate sort, same escape `/[.*+?^${}()|[\]\\]/g`, same boundary regex `(?:^|[^a-z0-9-])${escaped}(?:$|[^a-z0-9-])`, same alias resolution. The portfolio copy's own comment (lines 34-37) acknowledges it 'Mirrors _scanCorpusForInitiative ... kept local to avoid a cross-module dependency.'
- **Impact:** Two sources of truth for the load-bearing kebab-boundary correctness invariant (the documented 'pre-gyre must not match gyre' behavior). A future fix to the boundary class in one file silently diverges from the other, reintroducing the very false-match bug the lookarounds exist to prevent. The 'avoid a cross-module dependency' rationale is weak — portfolio already imports four functions from artifact-utils (portfolio-engine.js:12-18).
- **Suggested fix:** Export _scanCorpusForInitiative from artifact-utils.js (or a small shared scanner module) and import it in portfolio-engine.js, deleting the local copy.
- **Verifier note:** Verified against the real code. portfolio-engine.js:43-60 (`_scanCorpus`) and artifact-utils.js:503-528 (`_scanCorpusForInitiative`) are genuinely byte-for-byte equivalent in logic: identical length-descending candidate sort, identical escape `/[.*+?^${}()|[\]\\]/g`, identical boundary regex `(?:^|[^a-z0-9-])${escaped}(?:$|[^a-z0-9-])`, and identical alias-resolution branch. The portfolio copy's own comment (lines 33-37) explicitly states it "Mirrors `_scanCorpusForInitiative` ... kept local to avoid a cross-module dependency." I confirmed `_scanCorpusForInitiative` is NOT in artifact-utils' module.exports block (line 2142+), so it is a private helper and the local copy is a deliberate clone, not a coincidental constant. The finding's claim that portfolio already depends on artifact-utils is correct (lines 12-18 import readTaxonomy, scanArtifactDirs, parseFrontmatter, inferArtifactType, inferInitiative), which does weaken the "avoid cross-module dependency" rationale. I found no project-context.md or arch-doc exception sanctioning this duplication. The divergence-of-a-load-bearing-invariant concern is real. I downgraded severity from medium to low: this is maintainability muda (two sources of truth for ~17 lines), not an active correctness bug; the divergence is hypothetical and the suggested fix (export from artifact-utils, import in portfolio-engine, delete the copy) is trivial and low-risk. Note the related dir-scan at line 156 legitimately cannot reuse the scanner due to differing boundary semantics — that is correctly NOT duplication and is out of scope for this fix.
- **Reconciliation:** Swept against the lifecycle backlog and the two non-main branches (`deprecation`, `test/governance-migration-v2`): no coverage found. Genuinely live and unowned — a real defect or accumulated debt, actionable now.

#### 41. git-recency rule spawns one git subprocess per artifact, scaling O(artifacts) in process spawns

- **Status:** 🔴 Live — untracked
- **Type:** muda  •  **Location:** [scripts/lib/portfolio/rules/git-recency-rule.js:31](scripts/lib/portfolio/rules/git-recency-rule.js#L31)  •  **Reviewer:** `lib-portfolio`
- **Evidence:** applyGitRecencyRule loops over every artifact for the initiative and runs execFileSync('git', ['log', '-1', '--format=%as', '--', relativePath]) once per artifact (lines 31-46), only to keep the single maximum date. This rule is invoked once per initiative in generatePortfolio (portfolio-engine.js:383), so total git invocations are O(total governed+ungoverned artifacts) on every portfolio run.
- **Impact:** Redundant work: the per-initiative result is a single 'most recent commit date', obtainable with one `git log -1 --format=%as -- <path1> <path2> ...` over all the initiative's paths instead of N separate spawns. On large repos this is the dominant cost of the read-only portfolio command. Unlike the migration suggester (which caps git queries via MAX_GIT_SUGGESTER_QUERIES), this path has no cap and no batching.
- **Suggested fix:** Pass all of the initiative's relativePaths to a single `git log -1 --format=%as --` invocation (git reports the newest commit touching any of them), or batch across all initiatives once and index by path. Eliminates the per-file spawn loop.
- **Verifier note:** Verified directly: git-recency-rule.js lines 31-46 loop over every artifact for an initiative and run a separate execFileSync('git', ['log','-1','--format=%as','--', relativePath]) per artifact, retaining only the single maximum date. portfolio-engine.js:383 calls applyGitRecencyRule once per initiative inside the per-initiative loop, so total git subprocess spawns are O(total artifacts across all initiatives) on every portfolio run. The finding's contrast is also accurate — artifact-utils.js:477 defines MAX_GIT_SUGGESTER_QUERIES=50 to cap that other git path, while this rule has neither a cap nor batching. This is genuine redundant work a maintainer could act on by passing all of an initiative's paths to one `git log -1 --format=%as -- <paths...>` invocation. Caveat: the rule also tracks latestFile (the most-recent filename, consumed at line 52 for state.lastArtifact.file); a single batched git log reports the newest date but not which path produced it, so the fix is not a pure drop-in and would need --name-only parsing or accept reduced precision on lastArtifact.file. Severity is correctly low because this is a read-only listing command on a non-hot path, governed-artifact counts are realistically in the low hundreds, and the git calls are wrapped in try/catch for untracked files — so the cost is real muda but bounded and non-critical.
- **Reconciliation:** Swept against the lifecycle backlog and the two non-main branches (`deprecation`, `test/governance-migration-v2`): no coverage found. Genuinely live and unowned — a real defect or accumulated debt, actionable now.

#### 42. INTERNALS list duplicates a subset of the shared FORBIDDEN_STRINGS

- **Status:** 🔴 Live — untracked
- **Type:** muda  •  **Location:** [tests/lib/portability-seed-catalog.test.js:82](tests/lib/portability-seed-catalog.test.js#L82)  •  **Reviewer:** `test-architecture`
- **Evidence:** Line 82: `const INTERNALS = ['_bmad/', 'bmad-init', '.claude/hooks', '{project-root}'];` — every entry is a member of FORBIDDEN_STRINGS in scripts/portability/test-constants.js (framework-paths + bmad-init). This 'zero BMAD internals across staging tree' check hand-rolls a subset of the canonical forbidden list instead of importing it.
- **Impact:** shared-test-constants violation: the same 'these tokens must never leak into exports' concept is maintained in two places. If the shared list gains a new internal path/directive, the seed-catalog staging-tree scan will not catch it, so the export pipeline can regress with this test still green.
- **Suggested fix:** Import FORBIDDEN_STRINGS from scripts/portability/test-constants.js and iterate over it here too (or, if a deliberate subset is required, derive INTERNALS by filtering the shared list with a documented predicate rather than re-typing literals).
- **Verifier note:** Verified against the real code. tests/lib/portability-seed-catalog.test.js:82 hand-rolls `const INTERNALS = ['_bmad/', 'bmad-init', '.claude/hooks', '{project-root}']`, and every one of those four literals is indeed a member of the canonical FORBIDDEN_STRINGS in scripts/portability/test-constants.js (lines 29, 32-34). The shared-constant pattern is unambiguously the established convention: three sibling tests (portability-export-engine, portability-catalog-generator, portability-export-all) and two scripts (seed-catalog-repo.js, validate-exports.js) all import and iterate FORBIDDEN_STRINGS. Critically, seed-catalog-repo.js:185 — the very script whose output this test scans — already validates against the full list, so this "zero BMAD internals across staging tree" check is strictly weaker than the production code it guards: it misses bmad-help, bmad-speak, the Claude tool names, and the micro-file directives. The finding's regression risk is real (adding a new internal to the shared list would not be caught by this scan), and the test-constants.js header explicitly states "Import this instead of duplicating the list," making this a documented-intent violation a maintainer would act on. I lowered severity from medium to low because the four hand-coded internals happen to cover the highest-frequency path leaks for catalog output, the production export path already enforces the complete list, and the fix is a trivial mechanical refactor with no correctness impact today.
- **Reconciliation:** Swept against the lifecycle backlog and the two non-main branches (`deprecation`, `test/governance-migration-v2`): no coverage found. Genuinely live and unowned — a real defect or accumulated debt, actionable now.

#### 43. Stale cross-reference comment points at a non-existent module (scripts/lib/csv-utils.js)

- **Status:** 🔴 Live — untracked
- **Type:** muda  •  **Location:** [scripts/portability/manifest-csv.js:5](scripts/portability/manifest-csv.js#L5)  •  **Reviewer:** `cross-cutting-conception`
- **Evidence:** manifest-csv.js:5 documents: 'It is intentionally separate from `scripts/lib/csv-utils.js` (Team Factory's CSV writer utility)'. That path does not exist — verified `ls scripts/lib/csv-utils.js` returns no file. The actual Team Factory CSV utility lives at _bmad/bme/_team-factory/lib/utils/csv-utils.js. The migration file's comment (3.3.x-to-4.0.0.js:640) points to the correct path, so the two comments disagree.
- **Impact:** The justification comment for keeping a duplicate parser cites a module that was never at that path, so a future maintainer trying to reconcile the duplicates is misdirected. It also weakens the (already questionable) rationale for the third parseCsvRow copy.
- **Suggested fix:** Correct the comment to reference _bmad/bme/_team-factory/lib/utils/csv-utils.js (or, better, remove the duplicate entirely per the consolidation finding above).
- **Verifier note:** Verified every factual claim. manifest-csv.js:5 reads "It is intentionally separate from `scripts/lib/csv-utils.js` (Team Factory's CSV writer utility)". `ls scripts/lib/csv-utils.js` returns no file — that path does not exist. The actual Team Factory CSV utility is at `_bmad/bme/_team-factory/lib/utils/csv-utils.js`, which is the path referenced by every other consumer: convoke-doctor.js:332, audit/audit-bmm-dependencies.js:13, and the migration comment at scripts/update/migrations/3.3.x-to-4.0.0.js:639. So the comment is genuinely stale/wrong, and it does contradict the correct path used elsewhere, which would misdirect a maintainer trying to reconcile the parseCsvRow duplicates. This is a real but cosmetic documentation defect — a one-line comment fix with no functional impact (the code itself requires nothing from the wrong path; it is a doc-only cross-reference). Hence low severity, matching the finding's own rating. A maintainer would plausibly act on it during a consolidation pass.
- **Reconciliation:** Swept against the lifecycle backlog and the two non-main branches (`deprecation`, `test/governance-migration-v2`): no coverage found. Genuinely live and unowned — a real defect or accumulated debt, actionable now.

#### 44. pf1-record-agent.js: orphaned operator CLI with no code, test, or CI references

- **Status:** 🟣 Blocked — keep (FINISH or waive)
- **Type:** muda  •  **Location:** [scripts/audit/pf1-record-agent.js:1](scripts/audit/pf1-record-agent.js#L1)  •  **Reviewer:** `muda-triage`
- **Evidence:** A grep for `pf1-record-agent` across all *.js/*.json/*.yml/*.yaml in the repo (excluding node_modules, exported-skills, and the file's own self-references/comments) returns ZERO hits. It is not in package.json `bin` or `scripts`, not invoked by any .github workflow (ci.yml/badges.yml), not required by any script, and has no test file (tests/lib/pf1-record-agent.test.js does not exist). It is only referenced in Story-4.3 markdown runbooks under _bmad-output/ (e.g. v63-4-3-recording-protocol.md).
- **Impact:** This is a manual operator script for the PF1 validation recording cycle (Story 4.3). Per project memory, Story 4.3 is STALLED and 4.0 ships on the v6.3 baseline as planned. The script has no automated coverage and no programmatic entry point — it is dormant tooling tied to an in-flight-but-blocked gate, drifting away from the code it inspects (its header pins a `claude -p` invariant from a spike that may already be stale).
- **Suggested fix:** Confirm with the release owner whether the PF1 manual-recording cycle is still a release gate for 4.0/4.1. If yes, wire it into the documented runbook as a maintained tool and add a smoke test asserting arg-parse + exit codes. If the cycle was superseded (Path B+ re-scope moved control validation to install-scope-check.js), delete pf1-record-agent.js along with the stalled-story scaffolding.
- **Verifier note:** Verified directly: scripts/audit/pf1-record-agent.js (11KB, executable, authored 2026-05-29) exists, and a grep across *.js/*.json/*.yml/*.yaml returns ZERO references except the file's own header comment/usage strings (lines 21-24, 105). It is absent from package.json bin (which DOES wire sibling audit scripts validate-marketplace.js and audit-skill-dirs.js) and from scripts. No test file exists (tests/lib/pf1-record-agent.test.js confirmed missing; no tests reference pf1-record). It is referenced only in Story-4.3 markdown runbooks under _bmad-output/. The finding's superseding hypothesis is corroborated by the file's own header (lines 50-53): "BMAD-agent control validation replaced by mechanical install-scope-check.js" — and scripts/audit/install-scope-check.js does exist. The resumption snapshot confirms Story 4.3 is in-progress/stalled, gated on operator session-time, so this is genuinely dormant tooling tied to a blocked gate with no programmatic entry point and no coverage. The finding is real and a maintainer would act on it, but I down-grade to LOW severity: it is a harmless, intentional manual operator script (not broken/dead code, no correctness or security impact), and it is an active-but-stalled Story 4.3 deliverable rather than abandoned cruft — so the correct action is the conditional release-owner confirmation the suggested_fix already specifies, not an unconditional delete.
- **Reconciliation:** **Intent reconstruction overturned the "archive" lean; a coverage-check (2026-06-28) resolved the open question.** PF1 is the **v4.0 behavioral-equivalence release gate (FR36-40, LLM-judge, drift T=4.0, FAIL blocks *release*)**. The earlier open question — "can I97's per-agent personality scoring (FR21-23) satisfy it?" — **resolves to NO**: FR36-40 and FR21-23 are from *different PRDs* measuring *different axes* (PF1 = LLM-judged structural+capability+persona drift gating release; I97 = operator-ranked persona-only gating *merge*), and I97 covers only **2 of PF1's 4 target agents** (Emma, Wade — not Liam, not the Gyre control). **PF1's gate evidence is genuinely unobtained.** `pf1-record-agent.js` was committed 2026-05-31 (after the snapshot called it "not yet created"), so it is mid-resumption. **Action: FINISH the battery (or formally waive/re-scope the gate at the v4.0 release decision) — do NOT archive.** The per-finding fix here still applies when work resumes.

#### 45. config-merger.js: CONFIG_SCHEMA export is dead (used only internally, referenced by no other module or test)

- **Status:** 🔴 Live — untracked
- **Type:** muda  •  **Location:** [scripts/update/lib/config-merger.js:416](scripts/update/lib/config-merger.js#L416)  •  **Reviewer:** `muda-triage`
- **Evidence:** `CONFIG_SCHEMA` is defined at line 207, consumed internally at line 238 (`for (const rule of CONFIG_SCHEMA)`), and re-exported in `module.exports` at line 416. An exact grep for `CONFIG_SCHEMA` across scripts/ + index.js + tests/ returns hits ONLY inside config-merger.js itself (lines 207, 238, 416). No other source file and no test references the exported symbol. (By contrast, the file's other flagged exports — mergeConfig, writeConfig, readExcludedAgents, extractUserPreferences — all have real cross-file/test references and are false positives.)
- **Impact:** CONFIG_SCHEMA is a legitimate internal constant, but exporting it from the module's public surface implies an external contract that does not exist. It is an unused export entry only (the constant itself is live), so blast radius is tiny — but it invites future code to depend on an internal detail and bloats the module's apparent API.
- **Suggested fix:** Drop `CONFIG_SCHEMA` from the `module.exports` object at line 416, keeping it as a module-local const. If a test legitimately needs to assert on the schema, add that test and the export together; otherwise remove the export.
- **Verifier note:** Verified directly in scripts/update/lib/config-merger.js: CONFIG_SCHEMA is defined as a module-local const at line 207, consumed only internally by validateConfig at line 238, and re-exported in module.exports at line 416 (lines confirmed by Read). A repo-wide grep for CONFIG_SCHEMA (excluding node_modules) returns hits ONLY at those three lines inside config-merger.js. The three external consumers (validator.js, refresh-installation.js, migration-runner.js) and three test files (config-merger.test.js, config-merger-negative.test.js, yaml-comment-preservation.test.js) all require the configMerger module object but use only mergeConfig/validateConfig/extractUserPreferences/writeConfig/addMigrationHistory — never the schema. A targeted grep for property/dynamic access (.CONFIG_SCHEMA, configMerger[, ['CONFIG) returned nothing, ruling out the usual dynamic-reference false positive. So the exported symbol is genuinely unused dead public surface while the underlying constant stays live. The finding is real and correctly scoped as low severity: the fix is a trivial one-line removal from module.exports with zero functional blast radius — pure API hygiene a maintainer would plausibly accept.
- **Reconciliation:** Swept against the lifecycle backlog and the two non-main branches (`deprecation`, `test/governance-migration-v2`): no coverage found. Genuinely live and unowned — a real defect or accumulated debt, actionable now.

---

*Reproduce the muda scan: `npx knip` (config at [knip.json](../knip.json)). Migration files under `scripts/update/migrations/` are knip false positives (dynamic load via `registry.js`). Intent reconstruction sourced `convoke-epic-bmad-v6.3-adoption.md`, `convoke-epic-bmad-v6.4-v6.8-absorption.md`, `convoke-arch-bmad-v6.4-v6.8-absorption.md`, and the v6.3 PRD functional-requirements.*
